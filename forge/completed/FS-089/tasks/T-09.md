# TASK

## Title

Add automated coverage for CDM defaults, read-only permissions, and format-vs-source rendering

---

## ID

T-09

---

## Spec

FS-089

---

## Spec Rev

Rev: 1

---

## Agent

task

---

## Status

done

---

## Depends On

- T-02
- T-04
- T-05
- T-06
- T-07
- T-08

---

## Implements

- AE-01
- AE-02
- AE-04
- AE-05
- AE-06
- AE-08
- AE-09
- AE-12

---

## Summary

Create or update test suites and fixtures that validate the new schema model behavior across backend/adapters and UI surfaces, with emphasis on CDM default availability, read-only restrictions, and display semantics.

---

## Scope

### In Scope

- Add fixtures for CDM and user-created schema variants across statuses/source kinds.
- Add tests for:
  - default CDM presence,
  - Add Schema user-created-only behavior,
  - CDM read-only action gating,
  - card/list format display using JSON/XML not source-kind labels,
  - card/list toggle/filter/sort behavior.

### Out of Scope

- New test framework setup.
- Performance/load testing infrastructure changes.

---

## Relevant Areas

- `ui/src/features/schemas/**/*.test.tsx`
- `ui/src/features/projects/**/*.test.tsx`
- `tests/**` backend/adapter suites touching schema metadata and seed behavior

---

## Change Requirements

- Coverage must map directly to FS-089 acceptance examples.
- Fixtures must include inferred and error edge states.
- Tests must exercise duplicate-name disambiguation and zero-field contextual rendering where implemented.

---

## Verification Requirements

- Run targeted UI and backend test suites for touched areas.
- Run lint/typecheck for affected packages.

---

## Acceptance Checks

- Automated tests exist and pass for CDM default presence and read-only behavior.
- Automated tests exist and pass for Add Schema user-created-only constraints.
- Automated tests exist and pass for format-vs-source display semantics.
- Automated tests exist and pass for card/list toggle/filter/sort baseline behavior.

---

## Blockers / Risks

- Existing test fixtures may encode legacy scope/sync assumptions and require coordinated updates.

---

## Notes

- Keep assertions resilient to date formatting differences by validating semantic content rather than exact timestamps when possible.
