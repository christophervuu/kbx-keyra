# TASK

## Title

Add cross-surface integration coverage for CDM consistency contract

---

## ID

T-06

---

## Spec

FS-078

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01
- T-03
- T-04
- T-05

---

## Implements

- `AE-01`
- `AE-02`
- `AE-03`
- `AE-04`
- `AE-05`
- `AE-06`
- `AE-07`

---

## Summary

Add targeted cross-surface verification that the same CDM schema record resolves to the same origin/status/action behavior in Project Overview, Schema Library, and Schema Detail.

---

## Scope

### In Scope

- Add/extend integration-level tests (UI and/or API contract tests) to validate cross-surface consistency.
- Validate deterministic fallback behavior for legacy/unknown status values.
- Validate re-sync result propagation consistency across supported surfaces.

### Out of Scope

- Introducing new feature behavior beyond FS-078 contract.
- Full end-to-end browser-suite redesign.

---

## Relevant Areas

- `ui/src/features/projects/components/__tests__/*.test.tsx`
- `ui/src/features/schemas/components/__tests__/*.test.tsx`
- shared adapter/contract tests under `ui/src/lib/api/**/__tests__` ?
- optional backend contract tests under `tests/lambda/schema/**/*.test.ts` ?

---

## Change Requirements

- Verification coverage must assert consistency by comparing output expectations across all three surfaces for same fixture records.
- Coverage must include at least one case per canonical CDM status.
- Coverage must include CDM action policy checks for allowed/disallowed actions.
- Coverage must assert Schema Library navigation-first behavior (no inline Re-sync) and Schema Detail no-Unlink behavior.

---

## Verification Requirements

- Run touched test suites.
- Run typecheck/lint for affected packages.
- Ensure acceptance-example traceability in test naming/comments where practical.

---

## Acceptance Checks

- Automated tests fail on any cross-surface CDM label/status/action drift.
- Legacy/unknown status fallback behavior is explicitly tested.
- Re-sync status transition consistency is covered by tests.
- Tests, typecheck, and lint pass.

---

## Blockers / Risks

- Existing test fixtures may need normalization to represent consistent schema records across multiple surface test contexts.

---

## Notes

- Keep this task verification-focused; avoid introducing unrelated UI polish changes.
