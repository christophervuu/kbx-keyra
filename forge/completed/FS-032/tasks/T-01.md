# TASK

## Title

Update route path constant and route registration for Test Lab

---

## ID

T-01

---

## Spec

FS-032

---

## Spec Rev

Rev: 1

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- none

---

## Implements

- AE-10
- AE-08

---

## Summary

Change the `PATHS.MAPPING_TEST` constant from `/test` to `/test-lab`, update the route registration in `App.tsx` to use the new path segment and import the renamed route page component, and rename the route page wrapper file from `MappingAdvancedTesting.tsx` to `MappingTestLab.tsx`.

---

## Scope

### In Scope

- Update `PATHS.MAPPING_TEST` value in `ui/src/routes/paths.ts` from `'/projects/:projectId/mappings/:mappingId/test'` to `'/projects/:projectId/mappings/:mappingId/test-lab'`
- Rename `ui/src/routes/pages/MappingAdvancedTesting.tsx` to `ui/src/routes/pages/MappingTestLab.tsx`
- Update the default export function name from `MappingAdvancedTesting` to `MappingTestLab`
- Update the JSDoc comment in the route page wrapper to reference `/test-lab` instead of `/test`
- Update `App.tsx` to import from the new file path and use the new route path
- Update the import in the route page wrapper from `AdvancedTestingPage` to `TestLabPage` (the component will be renamed in T-03; use the new name now — T-03 will create the file)

### Out of Scope

- Renaming the feature component (`AdvancedTestingPage.tsx` → `TestLabPage.tsx`) — that is T-03
- Updating link text in `InlinePreviewStrip` — that is T-04
- Layout or breadcrumb changes — that is T-02

---

## Relevant Areas

- `ui/src/routes/paths.ts`
- `ui/src/routes/pages/MappingAdvancedTesting.tsx` (rename to `MappingTestLab.tsx`)
- `ui/src/App.tsx`

---

## Change Requirements

After this task:

- `PATHS.MAPPING_TEST` equals `'/projects/:projectId/mappings/:mappingId/test-lab'`
- The route in `App.tsx` matches the path `/projects/:projectId/mappings/:mappingId/test-lab`
- The import in `App.tsx` references `MappingTestLab` from `@/routes/pages/MappingTestLab`
- The route page wrapper file exists at `ui/src/routes/pages/MappingTestLab.tsx` with function name `MappingTestLab`
- The old file `ui/src/routes/pages/MappingAdvancedTesting.tsx` no longer exists
- The route page wrapper imports `TestLabPage` from `@/features/mappings/components/TestLabPage` (the actual component rename happens in T-03; this import will resolve once T-03 completes)

**Sequencing note:** Because T-01 references the not-yet-renamed component import (`TestLabPage`), this task should be coordinated with T-03. The simplest approach: temporarily keep the import pointing at `AdvancedTestingPage` until T-03 is done, then update. Alternatively, if T-03 runs first or concurrently, use the new name directly. Either way, the final state must reference `TestLabPage`.

---

## Verification Requirements

- `pnpm typecheck` passes in `ui/` (may need T-03 to complete for the import to resolve)
- `pnpm lint` passes in `ui/`
- The old route path `/test` no longer appears in `paths.ts` or `App.tsx`
- The new route path `/test-lab` is present in both files

---

## Acceptance Checks

- `PATHS.MAPPING_TEST` value is `'/projects/:projectId/mappings/:mappingId/test-lab'` (AE-10)
- Route registration in `App.tsx` uses the new path
- `MappingAdvancedTesting.tsx` no longer exists; `MappingTestLab.tsx` exists at the same location
- `forge/architecture/project-structure.md` updated if the route pages section references the old filename

---

## Blockers / Risks

- The import of `TestLabPage` in the route wrapper will not resolve until T-03 renames the feature component. If running T-01 independently, temporarily keep the old import name to avoid typecheck failure.

---

## Notes

- This is the foundation task. Other tasks depend on the route path being updated.
- The route page wrapper is a thin component (16 lines) — the rename is straightforward.
