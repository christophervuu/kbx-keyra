# TASK

## Title

Update ui-application.md architecture document for Test Lab rename and layout changes

---

## ID

T-06

---

## Spec

FS-032

---

## Spec Rev

Rev: 1

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01
- T-02
- T-03
- T-04
- T-05

---

## Implements

- AE-02
- AE-03
- AE-10

---

## Summary

Update `forge/architecture/ui-application.md` to reflect the Test Lab rename: update the routing table, the "Advanced Testing Page" section title and content, component names in the module structure and hierarchy, and document the "focused workspace" layout pattern in AppLayout. Update `INDEX.md` date if needed.

---

## Scope

### In Scope

- In `forge/architecture/ui-application.md`:
  - **Routing table**: Change `/projects/:projectId/mappings/:mappingId/test` → `/projects/:projectId/mappings/:mappingId/test-lab`; change description from "Advanced Testing (FS-021 T-06)" to "Test Lab (FS-021 T-06, FS-032)"
  - **Module Structure**: Rename `AdvancedTestingPage.tsx` → `TestLabPage.tsx` with updated description; rename route page file reference
  - **Section title**: Rename "Advanced Testing Page (FS-021 T-06)" to "Test Lab Page (FS-021 T-06, FS-032)"
  - **Section content**: Replace all "Advanced Testing" text with "Test Lab"; update component names (`AdvancedTestingPage` → `TestLabPage`, `AdvancedTestingInner` → `TestLabInner`, `MappingAdvancedTesting` → `MappingTestLab`)
  - **Layout Route Pattern section**: Document the "focused workspace" pattern — mention that both Mapping Editor and Test Lab routes suppress breadcrumbs and use full-bleed layout via `isFocusedWorkspace` in AppLayout
  - **Component hierarchy** (if "Advanced Testing" components are listed): Update names
  - **InlinePreviewStrip section**: Update any references to "Advanced Testing" link text
- In `forge/architecture/INDEX.md`:
  - Update `Last Updated` date for `ui-application.md` entry if changed

### Out of Scope

- Changes to any other architecture document
- Changes to production code
- Changes to `project-structure.md` — that should be handled inline by the ui-task agents if they create/rename files

---

## Relevant Areas

- `forge/architecture/ui-application.md`
- `forge/architecture/INDEX.md`

---

## Change Requirements

After this task:

- No occurrences of "Advanced Testing" remain in `ui-application.md` (replaced with "Test Lab")
- No occurrences of `AdvancedTestingPage` or `MappingAdvancedTesting` remain in `ui-application.md`
- The routing table shows the `/test-lab` path
- The layout section documents the focused workspace pattern covering both routes
- `INDEX.md` `Last Updated` date reflects the update

---

## Verification Requirements

- Grep `ui-application.md` for "Advanced Testing" — zero matches
- Grep `ui-application.md` for `AdvancedTestingPage` — zero matches
- Review the routing table entry for correctness
- Review the focused workspace layout documentation for accuracy

---

## Acceptance Checks

- All "Advanced Testing" references replaced with "Test Lab" in `ui-application.md`
- All component name references updated
- Route path updated to `/test-lab`
- Focused workspace layout pattern documented
- `INDEX.md` last updated date for `ui-application.md` is current

---

## Blockers / Risks

- none

---

## Notes

- This task should run after all UI tasks are complete so the architecture document reflects the final implemented state.
- The architecture doc is large (~900+ lines). Search comprehensively for all occurrences.
