# TASK

## Title

Update test files for renamed components, test-ids, and paths

---

## ID

T-05

---

## Spec

FS-032

---

## Spec Rev

Rev: 1

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- T-01
- T-02
- T-03
- T-04

---

## Implements

- AE-01
- AE-02
- AE-07
- AE-09

---

## Summary

Update all test files to reflect the renamed components, updated test-ids, and new route path. This includes renaming the `AdvancedTestingPage.test.tsx` file, updating test descriptions, test-id lookups, import paths, and assertions in both the renamed test file and `InlinePreviewStrip.test.tsx`.

---

## Scope

### In Scope

- Rename `ui/src/features/mappings/components/AdvancedTestingPage.test.tsx` → `ui/src/features/mappings/components/TestLabPage.test.tsx`
- Update all test descriptions referencing "Advanced Testing" to "Test Lab"
- Update all test-id lookups:
  - `advanced-testing-page` → `test-lab-page`
  - `advanced-testing-topbar` → `test-lab-topbar`
- Update import statement from `AdvancedTestingPage` to `TestLabPage`
- In `InlinePreviewStrip.test.tsx`:
  - Update test description `'renders Advanced Testing link in toolbar'` → `'renders Test Lab link in toolbar'`
  - Update test-id lookup `strip-advanced-testing-link` → `strip-test-lab-link`
  - Update test description referencing "Advanced Testing link" → "Test Lab link"
  - Update test-id lookup `strip-status-bar-advanced-testing-link` → `strip-status-bar-test-lab-link`
  - Update any link text assertions from "Open Advanced Testing" to "Open Test Lab"
- Search all test files under `ui/src/` for any remaining references to "Advanced Testing", "advanced-testing", or the old `/test` route path and update them

### Out of Scope

- Writing new tests
- Changing test logic or assertions beyond what's needed for the rename
- E2E test updates (FS-019 is still in draft and not yet implemented)

---

## Relevant Areas

- `ui/src/features/mappings/components/AdvancedTestingPage.test.tsx` (rename to `TestLabPage.test.tsx`)
- `ui/src/features/mappings/components/InlinePreviewStrip.test.tsx`
- Any other test files that reference the old names (search comprehensively)

---

## Change Requirements

After this task:

- `AdvancedTestingPage.test.tsx` no longer exists; `TestLabPage.test.tsx` exists
- All test-id lookups in test files use the new `test-lab-*` prefix
- All test descriptions reference "Test Lab" not "Advanced Testing"
- All tests pass with `pnpm test` in `ui/`
- No test file contains the string "Advanced Testing" or "advanced-testing" (case-insensitive search)

---

## Verification Requirements

- `pnpm test` passes in `ui/` — all existing tests continue to pass
- `pnpm typecheck` passes in `ui/`
- `pnpm lint` passes in `ui/`
- Grep for `advanced.testing` (case-insensitive regex) across `ui/src/` returns zero matches

---

## Acceptance Checks

- `TestLabPage.test.tsx` exists and all tests pass
- `AdvancedTestingPage.test.tsx` no longer exists
- `InlinePreviewStrip.test.tsx` tests pass with updated test-ids and link text assertions
- `pnpm test` in `ui/` exits with zero failures
- `pnpm typecheck` and `pnpm lint` pass
- Zero occurrences of "advanced-testing" or "Advanced Testing" in any file under `ui/src/`

---

## Blockers / Risks

- If T-03 or T-04 introduced subtle behavioral changes alongside the rename, tests may need assertion updates beyond simple string replacement. This is unlikely given the scope of those tasks.

---

## Notes

- This task is the verification gate for the entire rename. Running the full test suite here validates that all prior tasks are consistent.
- Search broadly — there may be test helpers, fixtures, or mock data referencing the old names.
