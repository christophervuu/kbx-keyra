# TASK

## Title

Execute backend/AWS integration remediations from scope audit and verify unaffected areas

---

## ID

T-09

---

## Spec

FS-087

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-02
- T-04
- T-08

---

## Implements

- `AE-11`
- `AE-12`
- `AE-12a`
- `AE-13`

---

## Summary

Apply and validate required backend/AWS integration changes identified during audit (DynamoDB/S3/OpenSearch/GitHub/deployments/IAM assumptions), and explicitly document no-change findings where subsystems are unaffected.

---

## Scope

### In Scope

- Implement required remediations in backend integration code/config for scope-dependent behavior.
- Update query/index filtering semantics where scope was used as access filter.
- Verify deployment snapshot and runtime behaviors are explicit-reference based and scope-independent.
- Document explicit unaffected subsystems with rationale.

### Out of Scope

- UI component terminology changes.
- New infrastructure capabilities beyond audit-required changes.

---

## Relevant Areas

- `src/lambda/schema/query-schema-nodes.ts` ?
- `src/lib/schema/opensearch/*` ?
- `src/lib/persistence/*`
- `src/lambda/schema/*`
- deployment-related code under `src/lambda/deploy*` / `src/lib/deployment*` ?
- `template.yaml` / env var assumptions ?

---

## Change Requirements

- Remove scope-based schema access assumptions in integration paths.
- Remove `projectId` schema-ownership assumptions that block cross-project schema access in keys/indexes/query paths.
- Preserve or adapt storage/index path conventions only where technically necessary and non-user-facing.
- Ensure deployment snapshots continue to lock explicit schema references and provenance.
- Record explicit no-change outcomes for AWS areas where audit found no required modifications.

---

## Verification Requirements

- Integration tests for changed backend paths.
- Targeted regression tests around deployment snapshot creation and schema resolution.
- Verification notes mapping each audited area to changed/unaffected status.

---

## Acceptance Checks

- All audited backend/AWS scope dependencies are either remediated or explicitly marked unaffected.
- Deployment behavior remains stable and scope-independent.
- No hidden scope-based filtering remains in schema integration paths.

---

## Blockers / Risks

- Risk: undocumented implicit assumptions in environment config or secondary services could be missed; require explicit checklist closure.

---

## Notes

- Keep this task tightly aligned to T-02 audit findings to prevent unbounded backend refactors.
- Applied backend/AWS integration remediations aligned to T-02 findings and verified unaffected surfaces:
  - Deployment CDM guard now normalizes legacy schema origins via persistence normalization (`local|published -> uploaded`) before CDM gating checks, preventing false CDM classification drift while preserving explicit-reference deploy behavior.
  - Project list schema-count aggregation now uses canonical linked-schema normalization (trim + dedupe + linked-first fallback to legacy refs) to keep linkage semantics deterministic under legacy payloads.
  - CDM link handler linked-schema ID normalization aligned with canonical trimming/deduplication behavior to avoid malformed/duplicate linkage propagation.
- Explicit unaffected outcomes (verified by code + tests):
  - DynamoDB PK/SK/GSI ownership model remains schemaId-centric for schema entities (no projectId ownership migration required).
  - S3 schema keying remains `schemas/{schemaId}/...` (scope-independent).
  - OpenSearch schema query/index filtering remains schemaId-term based only (no scope/project ownership filter).
  - Deployment snapshots remain explicit schema-reference/provenance based (`sourceSchemaId`/`targetSchemaId` + optional `cdmSchemaTraceability`) with no scope dependency.
  - IAM/env wiring in `template.yaml` requires no FS-087 scope-model changes.

### Verification

- Targeted backend/integration tests:
  - `pnpm test tests/lambda/project/list-projects.test.ts tests/lambda/schema/link-cdm-schema.test.ts tests/lambda/deployment/deployment-handlers.test.ts` ✅ (42/42)
  - `pnpm test tests/lib/schema/opensearch/query.test.ts` ✅ (6/6)
- Root typecheck:
  - `pnpm typecheck` ❌ pre-existing Node ambient typing/config issues in this workspace (`node:*`, `process`, `Buffer`) unrelated to T-09 changes.
