# TASK

## Title

Update project-structure.md for new Home Dashboard files

---

## ID

T-09

---

## Spec

FS-049

---

## Spec Rev

Rev: 1

---

## Agent

task

---

## Status

done

---

## Depends On

- T-08

---

## Implements

- AE-01

---

## Summary

Update `forge/architecture/project-structure.md` to reflect all new files and any deleted files in the `features/home/` module resulting from the FS-049 implementation. This ensures the architecture document accurately represents the current repository state.

---

## Scope

### In Scope

- Review `ui/src/features/home/` directory for new files created in T-01 through T-08
- Update the `features/home/` section in `project-structure.md` to include:
  - New components: `NeedsAttention.tsx`, `ContinueWhereYouLeftOff.tsx`, `ActivityPlaceholder.tsx`
  - New hooks: `use-recent-activity.ts`
  - Deleted files: `DashboardTabs.tsx`
- Verify all other file references in the home section are still accurate

### Out of Scope

- Changes to any other section of `project-structure.md`
- Changes to `ui-application.md` (no architecture-level changes in FS-049)
- Changes to `INDEX.md` (no new architecture documents created)

---

## Relevant Areas

- `forge/architecture/project-structure.md`
- `ui/src/features/home/` (reference for current file list)

---

## Change Requirements

After this task is complete:

- `project-structure.md` `features/home/` section accurately lists all current files
- New files are documented with brief descriptions consistent with the existing style
- Deleted files (`DashboardTabs.tsx`) are removed from the listing
- No other sections of `project-structure.md` are modified

---

## Verification Requirements

- Visual review: `project-structure.md` home section matches actual `features/home/` directory
- No other sections inadvertently changed

---

## Acceptance Checks

- `project-structure.md` reflects all new files in `features/home/`
- `DashboardTabs.tsx` is not listed
- New component and hook files are listed with descriptions
- Document formatting is consistent with existing entries

---

## Blockers / Risks

- Depends on all implementation tasks being complete to know the final file list.

---

## Notes

- Check the actual directory listing of `ui/src/features/home/` at the time of execution to ensure accuracy.
- The `project-structure.md` home section currently documents the FS-014 implementation. Update it to reflect the FS-049 changes while preserving entries for files that remain unchanged.
