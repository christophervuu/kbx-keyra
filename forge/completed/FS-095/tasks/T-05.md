# TASK

## Title

Run regression coverage sweep for parameter-aware actions and preserved layout behavior

---

## ID

T-05

---

## Spec

FS-095

---

## Spec Rev

Rev: 1

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- T-02
- T-03
- T-04

---

## Implements

- AE-03
- AE-05
- AE-06
- AE-07
- AE-08

---

## Summary

Add and run targeted regression coverage to confirm parameter-aware action behavior works end-to-end without regressing recipe-first UX, condensed row-editing layout, and safe hydration fallback behavior.

---

## Scope

### In Scope

- Add/update tests spanning MappingEditor, SmartBuilderPanel, and smart-builder lib modules for AE coverage.
- Run lint, typecheck, and targeted relevant test suites.
- Confirm condensed target panel and builder-width behavior remain unchanged while parameterized actions are used.
- Confirm non-decomposable expression fallback remains Advanced-first and safe.

### Out of Scope

- New feature work beyond verification-driven bug fixes found during sweep.
- Broad unrelated test refactors.

---

## Relevant Areas

- `ui/src/routes/pages/MappingEditor.test.ts`
- `ui/src/features/mappings/components/SmartBuilderPanel.test.tsx`
- `ui/src/features/mappings/components/ScalarFieldBuilder.test.tsx`
- `ui/src/features/mappings/components/MappingEditorPage.test.tsx`
- `ui/src/features/mappings/lib/*smart-builder*.test.ts`

---

## Change Requirements

- Regression coverage must map back to FS-095 acceptance examples.
- Verify parameterized action paths alongside existing one-click action paths.
- Verify condensed mode and recipe-first interaction contracts remain intact.
- Verification commands and outcomes must be captured in task execution notes when completed.

---

## Verification Requirements

- Automated tests covering AE-linked scenarios in touched areas.
- `pnpm -C ui lint`
- `pnpm -C ui typecheck`
- `pnpm -C ui test` (targeted suites minimum; broader run if feasible)

---

## Acceptance Checks

- Automated coverage exists for AE-03/05/06/07/08 scenarios.
- Condensed target panel and builder width allocation behavior remain passing in tests.
- Hydration fallback behavior remains safe for non-decomposable parameterized expressions.
- Lint/typecheck/test commands pass for touched scope.

---

## Blockers / Risks

- Existing flaky UI tests could obscure regression signal; isolate deterministic assertions where needed.

---

## Notes

- This is verification-focused and may include small bug fixes directly required to satisfy checks.

## Execution Notes

- Reviewed FS-095 AE coverage against targeted suites:
  - `src/routes/pages/MappingEditor.test.ts`
  - `src/features/mappings/components/SmartBuilderPanel.test.tsx`
  - `src/features/mappings/components/ScalarFieldBuilder.test.tsx`
  - `src/features/mappings/components/MappingEditorPage.test.tsx`
  - `src/features/mappings/lib/smart-builder-state.test.ts`
  - `src/features/mappings/lib/smart-builder-action-resolver.test.ts`
- Regression-only fixes made in `ScalarFieldBuilder.test.tsx` to align stale assertions with current UI contracts (required-asterisk token, enrichment terminology, Builder/Editor toggle posture, collapsed details panel behavior, suggest tooltip copy) and to remove obsolete BuilderFeedbackArea integration expectations.
- Verification outcomes:
  - `pnpm -C ui lint` → fails due to existing repo-wide baseline issues unrelated to FS-095 scope.
  - `pnpm -C ui typecheck` → pass.
  - `pnpm -C ui exec eslint <FS-095 touched files>` → pass.
  - `pnpm -C ui test <FS-095 targeted suites>` → pass (6/6 files, 211/211 tests).
- Acceptance check mapping:
  - AE-03/05/06/07/08 coverage remains passing in targeted MappingEditor/SmartBuilder/state/resolver/component suites.
  - Condensed target panel and builder-width allocation regression coverage remains passing via `MappingEditorPage.test.tsx`.
  - Non-decomposable hydration fallback remains covered/passing in smart-builder state + MappingEditor tests.
