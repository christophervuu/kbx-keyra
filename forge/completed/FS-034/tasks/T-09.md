# TASK

## Title
Update ui-application.md architecture document for test case management

---

## ID
T-09

---

## Spec
FS-034

---

## Spec Rev
Rev: 2

---

## Agent
task

---

## Status
done

---

## Depends On
- T-07

---

## Implements
- (does not directly implement acceptance examples — architecture documentation)

---

## Summary
Update `forge/architecture/ui-application.md` to document the new test case management architecture: `TestRunResult` type, `useTestRunResults` hook, `useBatchExecution` hook, `TestCaseListPanel` component, scratchpad vs saved-case selection model, and batch execution model. Update `INDEX.md` last-updated date.

---

## Scope

### In Scope
- Add a "Test Case Management" section to `ui-application.md` documenting:
  - `TestRunResult` type and storage model (`keyra:testresults:{mappingId}`)
  - `useTestRunResults` hook contract
  - `useBatchExecution` hook contract
  - `TestCaseListPanel` component contract and selection model
  - Scratchpad vs saved-case UX model
  - Batch execution sequential model
- Update the Module Structure section to include new files
- Update the Advanced Testing Page section to reference new components
- Update `INDEX.md` last-updated date for `ui-application.md`

### Out of Scope
- Code changes
- Creating a new architecture document (this extends an existing one)

---

## Relevant Areas
- `forge/architecture/ui-application.md`
- `forge/architecture/INDEX.md`

---

## Change Requirements
- Document `TestRunResult` type shape and storage key pattern
- Document `useTestRunResults` hook: inputs, outputs, persistence model
- Document `useBatchExecution` hook: inputs, outputs, sequential execution model, cancellation
- Document `TestCaseListPanel` component: props contract, selection model, scratchpad entry, status badges
- Document the selection model: `selectedTestCaseId: string | null` (null = scratchpad)
- Update Module Structure to include new hook files and component files
- Update Advanced Testing Page section to reference `TestCaseListPanel` replacing `TestCaseManager`

---

## Verification Requirements
- `INDEX.md` last-updated date is current
- Architecture document accurately reflects implemented state from T-01 through T-08

---

## Acceptance Checks
- `ui-application.md` contains a "Test Case Management" section
- `INDEX.md` entry for `ui-application.md` has updated last-updated date
- Module Structure reflects all new files

---

## Blockers / Risks
- none

---

## Notes
This task should be executed after T-07 to ensure the architecture document reflects the final implemented state. Load the completed task files for T-01 through T-08 to understand what was actually implemented.
