# TASK

## Title
Update ConnectedInlinePreviewStrip to use extended useTestCases API

---

## ID
T-08

---

## Spec
FS-034

---

## Spec Rev
Rev: 2

---

## Agent
ui-task

---

## Status
done

---

## Depends On
- T-01

---

## Implements
- AE-14

---

## Summary
Update the `ConnectedInlinePreviewStrip` to use the extended `useTestCases` API. This is a light-touch update — the inline strip's test case selector continues to work but now has access to any test cases created via the new management UI. No layout or behavioral changes to the strip itself.

---

## Scope

### In Scope
- Verify `ConnectedInlinePreviewStrip` continues to work with extended `useTestCases` hook
- Update imports if the hook signature or return type changes
- Ensure test cases created on the Advanced Testing page are visible in the strip's selector

### Out of Scope
- Adding the full test case list UI to the inline strip
- Adding batch execution to the inline strip
- Adding run result badges to the inline strip

---

## Relevant Areas
- `ui/src/features/mappings/components/ConnectedInlinePreviewStrip.tsx`

---

## Change Requirements
- The strip already uses `useTestCases(mappingId)` and accesses `testCases`, `loadTestCase`, `saveTestCase`
- If `UseTestCasesResult` type changes shape, update the destructuring
- No new UI behavior — this is a compatibility check

---

## Verification Requirements
- Existing strip tests pass
- TypeScript strict typecheck passes
- ESLint passes

---

## Acceptance Checks
- `pnpm run typecheck` passes in `ui/`
- `pnpm run lint` passes in `ui/`
- Existing `ConnectedInlinePreviewStrip` behavior is unchanged

---

## Blockers / Risks
- none

---

## Notes
This may be a no-op if the hook extension is purely additive (new methods, no breaking changes). The task exists to ensure explicit verification.
