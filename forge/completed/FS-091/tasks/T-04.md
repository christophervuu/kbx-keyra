# TASK

## Title

Migrate querySchemaNodes endpoint to DynamoDB-only retrieval path

---

## ID

T-04

---

## Spec

FS-091

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01
- T-02

---

## Implements

- AE-04

---

## Summary

Switch `POST /schemas/:id/query` to use the new Dynamo retrieval stack while preserving existing request/response and error semantics.

---

## Scope

### In Scope

- Replace OpenSearch query invocation in schema query handler.
- Preserve validation and response envelope behavior.
- Maintain optional filter handling compatibility.

### Out of Scope

- UI changes to schema search consumers.
- Infra-level OpenSearch resource deletion.

---

## Relevant Areas

- `src/lambda/schema/query-schema-nodes.ts`
- `src/lib/schema/types.ts`
- `src/lib/schema/dynamo/*`
- `tests/lambda/schema/query-schema-nodes.test.ts`

---

## Change Requirements

- Endpoint contract remains backward-compatible.
- Handler emits retrieval-stage telemetry fields for Dynamo path.
- OpenSearch is no longer called when mode is `dynamodb`.

---

## Verification Requirements

- Route-level tests for request validation and 400/404/500 semantics.
- Contract tests to ensure response shape compatibility.
- Lint/typecheck/test for touched modules.

---

## Acceptance Checks

- `querySchemaNodes` passes existing contract tests with Dynamo-only retrieval enabled.
- Error semantics are unchanged for invalid and missing-schema cases.
- No OpenSearch dependency in active Dynamo mode path.

---

## Blockers / Risks

- Behavior drift in score ordering may affect consumer expectations.

---

## Notes

- Keep legacy response fields (`score`, result attributes) stable unless explicitly versioned.
