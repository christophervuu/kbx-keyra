# TASK

## Title

Remove OpenSearch runtime infrastructure and ingestion writes

---

## ID

T-08

---

## Spec

FS-091

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-07

---

## Implements

- AE-01
- AE-07

---

## Summary

Decommission OpenSearch resources and related runtime wiring after cutover gates pass, ensuring production operation no longer depends on OpenSearch.

---

## Scope

### In Scope

- Remove OpenSearch collection/resource declarations from IaC.
- Remove OpenSearch env vars and IAM permissions from runtime stack.
- Remove ingestion-time OpenSearch write/index/delete code paths.
- Remove now-obsolete OpenSearch runtime config toggles once no longer needed.

### Out of Scope

- Retrieval quality tuning improvements.
- Post-cutover cost analysis documentation.

---

## Relevant Areas

- `template.yaml`
- `src/lib/schema/opensearch/*`
- `src/lambda/schema/process-batch.ts`
- `src/lambda/schema/ingest-schema.ts`
- `src/lambda/schema/orchestration-tasks.ts`
- `src/lib/schema/index.ts`
- Infra/deploy scripts ?

---

## Change Requirements

- Runtime path has zero OpenSearch dependency.
- Build/deploy configuration no longer expects OpenSearch endpoint/resources.
- Legacy feature flags are cleaned up or constrained to non-production only as explicit transitional policy.

---

## Verification Requirements

- IaC validation/diff checks proving OpenSearch resource removal.
- Runtime integration tests pass with OpenSearch unset.
- Lint/typecheck/test for touched modules.

---

## Acceptance Checks

- No OpenSearch runtime calls remain in production-serving code paths.
- Stack can deploy without OpenSearch resources/permissions.
- Ingestion succeeds without attempting OpenSearch writes.

---

## Blockers / Risks

- Hidden transitive imports or config assumptions may fail only at deploy/runtime.

---

## Notes

- Execute only after explicit cutover approval from T-07 outputs.
- Verification evidence captured during execution:
  - `pnpm vitest run tests/lambda/schema/ingest-schema.test.ts tests/lambda/schema/process-batch.test.ts tests/lambda/schema/orchestration-tasks.test.ts tests/lambda/schema/sync-cdm-schema.test.ts tests/lambda/schema/query-schema-nodes.test.ts tests/integration/schema-ingestion/inline-path.test.ts tests/integration/schema-ingestion/step-functions-path.test.ts tests/lib/schema/retriever.test.ts tests/lib/schema/types.test.ts tests/lambda/integration/fs-057-api.test.ts` (pass; integration suite skipped where gated by env).
  - `env -u OPENSEARCH_ENDPOINT pnpm vitest run tests/lambda/schema/query-schema-nodes.test.ts tests/lambda/schema/ingest-schema.test.ts tests/lambda/schema/process-batch.test.ts` (pass with OpenSearch unset).
  - `pnpm eslint ...` on touched schema/runtime TS files (pass).
  - `sam validate --template template.yaml` (pass).
  - `sam validate --template template.yaml --lint` reports pre-existing W3660 API Gateway drift warnings unrelated to OpenSearch decommission resources.
  - `pnpm typecheck` remains blocked by repository-wide Node typings/config issues (`node:*`, `process`, `Buffer`) and additional pre-existing non-T-08 typing failures; no T-08-specific typecheck regressions were identified from targeted test/lint scope.
