# TASK

## Title

Add shadow-mode comparison telemetry and retrieval observability

---

## ID

T-06

---

## Spec

FS-091

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01
- T-03

---

## Implements

- AE-05
- AE-08

---

## Summary

Instrument retrieval stages and shadow-mode comparisons so migration quality/latency can be evaluated safely before full cutover.

---

## Scope

### In Scope

- Emit stage metrics (`candidate_count`, `retrieval_ms`, `rerank_ms`, `topk_hit_depth`, etc.).
- Add shadow diff logging for rank overlap/timing deltas.
- Add sampling strategy/config for shadow telemetry volume control.
- Ensure telemetry correlation identifiers are preserved across AI handlers.

### Out of Scope

- Cost dashboard implementation outside existing telemetry sinks.
- Final OpenSearch decommission.

---

## Relevant Areas

- `src/lib/ai/telemetry.ts`
- `src/lambda/ai/auto-map.ts`
- `src/lambda/ai/suggest-expression.ts`
- `src/lambda/ai/smart-fix.ts`
- `src/lambda/ai/validate-mappings.ts`
- `src/lambda/schema/query-schema-nodes.ts`
- `tests/lambda/ai/*.test.ts`

---

## Change Requirements

- Shadow mode captures parity metrics without impacting primary responses.
- Retrieval telemetry fields are consistent across all retrieval consumers.
- Metrics support schema-size segmentation for p95 tracking.
- Canonical parity KPI is Top-K Jaccard overlap at K=10 (target >= 0.70); NDCG@10 delta (target >= -0.10) is emitted as safety metric.

---

## Verification Requirements

- Unit tests for telemetry payload shape and optional-field behavior.
- Handler tests for shadow-mode emission and non-fatal secondary failures.
- Lint/typecheck/test for touched modules.

---

## Acceptance Checks

- Retrieval stage metrics are emitted in both Dynamo and shadow runs.
- Shadow comparison logs include Top-K Jaccard overlap, NDCG@10 delta, and timing delta fields.
- Missing telemetry sink/errors do not break request handling.

---

## Blockers / Risks

- High-cardinality telemetry dimensions may increase observability cost/noise.

---

## Notes

- Keep metric names stable and documented for rollout runbooks.
