# TASK

## Title

Freeze and guardrail deprecated HybridAdapter legacy loop (one-cycle retention)

---

## ID

T-03

---

## Spec

FS-068

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01

---

## Implements

- AE-04

---

## Summary

Enforce one-release-cycle deprecated retention for `HybridAdapter`/legacy `ai-api-client` while keeping canonical adapter integration unambiguous. Ensure no active routing uses hybrid, preserve instantiation warning, and add guardrails that block new callsites.

---

## Scope

### In Scope

- Ensure `HybridAdapter` is deprecated-only and not part of active runtime routing.
- Preserve warning-on-instantiation behavior.
- Add lint/check guardrails that prohibit new `HybridAdapter` callsites.
- Keep/update exports/tests to reflect deprecated-retained status for one release cycle.
- Ensure bootstrap path points to canonical adapters only.

### Out of Scope

- Backend Lambda changes.
- UI feature behavior changes unrelated to adapter path retirement.

---

## Relevant Areas

- `ui/src/lib/api/hybrid-adapter.ts`
- `ui/src/lib/api/ai-api-client.ts`
- `ui/src/lib/api/index.ts`
- `ui/src/lib/api/bootstrap.ts`
- `ui/src/lib/api/__tests__/hybrid-adapter.test.ts`

---

## Change Requirements

- Hybrid loop is not an active integration option in production code paths.
- Hybrid artifacts are clearly marked deprecated and retained for one cycle only.
- Guardrails prevent new callsites/import additions.
- Tests and exports are updated to remove ambiguity.

---

## Verification Requirements

- Adapter bootstrap/export tests confirm only canonical path is active.
- Guardrail checks fail when new `HybridAdapter` callsites are introduced.
- Deprecated files have no new production callsite adoption.

---

## Acceptance Checks

- No active adapter routing path depends on `HybridAdapter`.
- `HybridAdapter` remains deprecated-retained for one release cycle with warning-on-instantiation behavior.
- Guardrail checks/lint block new hybrid callsites.
- `pnpm --filter @keyra/ui exec vitest run src/lib/api/bootstrap.test.ts src/lib/api/__tests__/hybrid-adapter.test.ts` passes for touched adapter tests.
- `pnpm exec vitest run tests/scripts/check-ui-ai-guardrails.test.ts` passes.
- `pnpm run lint:ui:ai-guardrails` passes.

---

## Blockers / Risks

- Guardrails may initially flag existing benign legacy references and need curated allowlist/baseline handling.

---

## Notes

- Drift review: task updated from Rev 1 to Rev 2 decisions (retain deprecated HybridAdapter for one cycle, preserve warning, block new callsites via guardrails).
- Do not expand hybrid usage while retained; this task enforces freeze + guardrails, not net-new hybrid functionality.
