# TASK

## Title

Verify and align UI AI hook call sites to adapter-only contracts

---

## ID

T-04

---

## Spec

FS-068

---

## Spec Rev

Rev: 2

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- T-01
- T-02

---

## Implements

- AE-05

---

## Summary

Confirm mapping AI UI hooks/components remain adapter-driven and compatible with updated HTTP/offline behavior. Apply minimal UI-surface adjustments only where needed for deterministic user-facing behavior.

---

## Scope

### In Scope

- Audit and validate `useExplainRule`, `useSuggestExpression`, and `useAutoMapWorkspace` call paths.
- Update hook tests/messages only if needed to match finalized adapter contracts.
- Ensure no direct AI endpoint `fetch()` calls exist in UI feature surfaces.

### Out of Scope

- Major UX redesign or net-new UI feature behavior.
- Adapter transport implementation work.

---

## Relevant Areas

- `ui/src/features/mappings/hooks/use-explain-rule.ts`
- `ui/src/features/mappings/hooks/use-suggest-expression.ts`
- `ui/src/features/mappings/hooks/use-auto-map-workspace.ts`
- `ui/src/features/mappings/hooks/*.test.ts`

---

## Change Requirements

- UI hooks continue to invoke adapter methods exclusively.
- User-facing error mapping remains deterministic for offline/transport failures after adapter changes.
- Hook tests reflect current adapter contract behavior.

---

## Verification Requirements

- Hook unit tests pass for AI hook suites.
- Static search confirms no direct AI endpoint fetch usage in mapped UI feature surfaces.

---

## Acceptance Checks

- Explain/Suggest/Auto-Map hooks are adapter-only.
- Hook tests pass after adapter contract changes (including FEATURE_NOT_ENABLED passthrough assertions).
- No direct browser AI-call path is introduced (verified via static search in `ui/src/features/mappings`).

---

## Blockers / Risks

- If adapter error semantics change significantly, UI message assertions may require coordinated updates.

---

## Notes

- Drift review: task updated from Rev 1 to Rev 2; no code changes required because hooks/tests already enforce adapter-only path and FEATURE_NOT_ENABLED passthrough behavior.
- Keep UI diffs minimal and contract-focused.
