# TASK

## Title

Update ui-application.md architecture for FS-027 changes

---

## ID

T-13

---

## Spec

FS-027

---

## Spec Rev

Rev: 1

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01
- T-04
- T-05
- T-06
- T-07
- T-08
- T-09
- T-10
- T-12

---

## Implements

- (architecture maintenance — no direct AE mapping)

---

## Summary

Update `forge/architecture/ui-application.md` to reflect the architectural changes introduced by FS-027: dirty-state detection model, static value in Value mode state, clear mapping action, object summary panel enhancements, builder header layout changes, and object coverage using leaf fields.

---

## Scope

### In Scope

- Update `ScalarFieldBuilder` component description to reflect: header compression, mode toggle placement, conditional suggested sources, clear mapping action, live result display integration
- Update `ExpressionBuilderState` Value mode documentation to include `inputType` and `staticValue`
- Update `useTargetStatus` hook contract to document leaf-field recursive coverage
- Update `ObjectSummaryPanel` description to reflect actionable summary with navigation
- Update `useMappingEditor` to document `clearRule` / `deleteRuleByTarget` action
- Note removal of Direct Copy button
- Update `INDEX.md` last-updated date for `ui-application.md`

### Out of Scope

- Changes to other architecture documents
- Changes to the architecture that are not directly implemented in FS-027 tasks

---

## Relevant Areas

- `forge/architecture/ui-application.md`
- `forge/architecture/INDEX.md`

---

## Change Requirements

- In the ScalarFieldBuilder section: update description to reflect compressed header (type badge left, toggle in header row, no standalone builder/editor row), conditional suggested sources, clear mapping action, and live result display.
- In the Expression Builder Architecture section: update `ValueModeState` to include `inputType: 'source' | 'static'` and `staticValue: string`.
- In the `useTargetStatus` Hook Contract section: change coverage derivation from "direct children's status" to "recursive leaf descendant counting."
- In the Node-Type-Specific Right Panel Pattern section: expand `ObjectSummaryPanel` description to cover child field navigation.
- In the Two-Tier Save Model section: note dirty-state detection against per-field `lastAppliedExpression`.
- Add `useMappingEditor` additions for clear/delete-by-target-path.
- Note Direct Copy button removal.
- Update `INDEX.md` entry for `ui-application.md` with new last-updated date and coverage notes.

---

## Verification Requirements

- Architecture document accurately reflects implemented behavior from all FS-027 tasks.
- `INDEX.md` entry is updated.
- No contradictions with prior architecture content (update in place, don't duplicate).

---

## Acceptance Checks

- `forge/architecture/ui-application.md` reflects all FS-027 architectural changes
- `forge/architecture/INDEX.md` last-updated date for ui-application.md is current
- Document is internally consistent (no contradictions with FS-020–FS-025 content)
- No dead references to removed components/features

---

## Blockers / Risks

- Depends on implementation tasks completing so final architectural details are confirmed.

---

## Notes

- This task should be executed last after all implementation tasks are done, so the architecture document reflects actual implemented behavior rather than planned behavior.
- Update in place — modify existing sections rather than appending redundant new sections.
