# TASK

## Title

Compress builder header and remove unnecessary chrome rows

---

## ID

T-07

---

## Spec

FS-027

---

## Spec Rev

Rev: 1

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- none

---

## Implements

- `AE-14`

---

## Summary

Remove the standalone "Expression Builder/Editor" row from the builder panel, move the Builder | Editor toggle into the target property header row, move the target type badge to the left side, and hide the Suggested Sources section entirely when there are no suggestions. This recovers vertical space for the actual authoring surface.

---

## Scope

### In Scope

- Remove the dedicated "Expression Builder/Editor" label row
- Move Builder | Editor segmented toggle into the target property header row (right-aligned or inline)
- Move target property type badge to the left side of the header row
- Conditionally render Suggested Sources section: hide entirely when suggestions list is empty
- Verify overall vertical space recovery

### Out of Scope

- Changes to the Builder | Editor toggle behavior or mode switching logic
- Changes to suggested sources computation logic
- Changes to the expression authoring surface itself
- Changes to the Apply button placement

---

## Relevant Areas

- `ui/src/features/mappings/components/ScalarFieldBuilder.tsx`
- `ui/src/features/mappings/components/UnifiedExpressionBuilder.tsx` ?

---

## Change Requirements

- Remove any standalone row/div that only contains the "Expression Builder/Editor" label text.
- Target property header row layout:
  - Left: type badge (string/number/boolean/etc.) + target field name/path
  - Right: Builder | Editor toggle (segmented control or tabs)
- Suggested Sources section: wrap in conditional render — only show when `suggestedSources.length > 0`.
- Ensure the header row is compact: single line, proper alignment, no excess padding.
- Net effect should be visible vertical space recovery when measured against current layout.

---

## Verification Requirements

- Component test: no standalone "Expression Builder/Editor" row rendered.
- Component test: Builder | Editor toggle is rendered within the header row.
- Component test: type badge is on the left side of the header.
- Component test: Suggested Sources is not rendered when suggestions list is empty.
- Component test: Suggested Sources is rendered when suggestions exist.
- Visual verification: measurable vertical space recovery.
- Typecheck passes.

---

## Acceptance Checks

- No standalone "Expression Builder/Editor" row exists
- Builder | Editor toggle is in the target property header row
- Target type badge is on the left side of the header
- Suggested Sources hidden when empty
- Suggested Sources shown when populated
- `tsc --noEmit` passes
- Lint passes

---

## Blockers / Risks

- none

---

## Notes

- The header row should be a single `flex` row with items: `[type badge] [field name/path] [spacer/grow] [Builder|Editor toggle]`.
- Suggested Sources conditional: `{suggestedSources.length > 0 && <SuggestedSources ... />}`.
