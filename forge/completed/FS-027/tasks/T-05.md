# TASK

## Title

Remove Direct Copy button from builder panel

---

## ID

T-05

---

## Spec

FS-027

---

## Spec Rev

Rev: 1

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- none

---

## Implements

- `AE-15`

---

## Summary

Remove the dedicated Direct Copy button from the Builder/Editor panel. Direct source mapping remains supported implicitly: selecting a source field with no transforms produces a `source("path")` expression automatically.

---

## Scope

### In Scope

- Remove the Direct Copy button element from `ScalarFieldBuilder` or `SourceChipPicker`
- Ensure selecting a source field without adding transforms still produces `source("path")` (already implicit)
- Remove any associated click handler or state logic for Direct Copy if it exists in isolation

### Out of Scope

- Changes to the source selection flow
- Changes to how `source("path")` expressions are generated
- Changes to drag-and-drop behavior

---

## Relevant Areas

- `ui/src/features/mappings/components/UnifiedExpressionBuilder.tsx`
- `ui/src/features/mappings/lib/expression-builder-state.ts` ?

---

## Change Requirements

- Locate the Direct Copy button in `UnifiedExpressionBuilder.tsx` — it renders in Value mode when exactly one source field is selected and no transforms are present.
- Remove the button and its associated click handler.
- Verify that the existing behavior of selecting a source field with an empty transform pipeline still generates `source("path")` via the expression generator — this is the implicit direct copy path and already works without the button.
- Reclaim the space previously used by the button.
- No functional regression to direct source mapping behavior.

---

## Verification Requirements

- Component test: builder panel does not render a "Direct Copy" button.
- Component test: selecting a source field with no transforms produces `source("path")` expression.
- Typecheck passes.

---

## Acceptance Checks

- No "Direct Copy" button rendered in the builder panel
- Direct source mapping via source selection + empty pipeline still works
- `tsc --noEmit` passes
- Lint passes

---

## Blockers / Risks

- none

---

## Notes

- The Direct Copy button is in `UnifiedExpressionBuilder.tsx`, rendered in Value mode when exactly one source is selected and no transforms present.
- This is a straightforward removal. The main risk is accidentally breaking the implicit direct-copy flow, which should be verified by existing tests or a new focused test.
