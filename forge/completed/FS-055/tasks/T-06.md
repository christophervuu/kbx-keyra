# TASK

## Title

Update ui-application.md architecture to reflect HttpAdapter and new bootstrap behavior

---

## ID

T-06

---

## Spec

FS-055

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-04

---

## Implements

- `AE-01`
- `AE-02`

---

## Summary

Update `forge/architecture/ui-application.md` to document the new `HttpAdapter` as the active backend-mode adapter, the updated bootstrap behavior, the HTTP client utility module, and the error normalization pattern. Also update `forge/architecture/INDEX.md` with the new last-updated date.

---

## Scope

### In Scope

- Update the **Adapter Pattern** section in `ui-application.md`:
  - Add `HttpAdapter` to the Implementations list as the Phase 1 backend adapter
  - Update Bootstrap description to show `HttpAdapter` selection (not `HybridAdapter`)
  - Mark `HybridAdapter` as retained-for-reference/legacy
  - Document the HTTP client utility module and its error normalization contract
  - Document `AdapterMethodNotImplementedError` pattern for non-CRUD placeholders
- Update the **Module Structure** section to include new files:
  - `ui/src/lib/api/http-adapter.ts`
  - `ui/src/lib/api/http-client.ts`
  - `ui/src/lib/api/errors.ts` (if created)
- Update `forge/architecture/INDEX.md` last-updated date for `ui-application.md`
- Update `phase-1-readiness.md` section 5 (AI Showcase Transition Path) to note that `HybridAdapter` is no longer the bootstrap default

### Out of Scope

- Documenting backend API endpoint design
- Changes to other architecture documents beyond mentioned updates
- Documenting auth/interceptor patterns (not yet implemented)

---

## Relevant Areas

- `forge/architecture/ui-application.md` — primary update target
- `forge/architecture/phase-1-readiness.md` — minor update (section 5)
- `forge/architecture/INDEX.md` — date update

---

## Change Requirements

After this task:

- `ui-application.md` Adapter Pattern section accurately describes:
  - `HttpAdapter` as the current Phase 1 backend adapter (CRUD over HTTP + placeholders for unimplemented methods)
  - Updated bootstrap: `VITE_API_URL` → `HttpAdapter`, absent → `LocalStorageAdapter`
  - `HybridAdapter` status as retained legacy (no longer bootstrap-selected)
  - HTTP client utility module purpose and error contract
  - `AdapterMethodNotImplementedError` pattern and its relationship to `AppError`
- Module Structure section lists new files
- `INDEX.md` shows updated date for `ui-application.md`
- `phase-1-readiness.md` section 5 notes updated bootstrap

---

## Verification Requirements

- Architecture documents are factually accurate against implementation
- No broken markdown formatting
- INDEX.md date is current
- Cross-references between documents are consistent

---

## Acceptance Checks

- `ui-application.md` Adapter Pattern section is updated with HttpAdapter documentation
- `ui-application.md` Module Structure section includes new file entries
- `INDEX.md` `ui-application.md` row shows current date
- `phase-1-readiness.md` section 5 bootstrap behavior note is updated
- All markdown renders correctly (no broken links or formatting)
- `forge/architecture/project-structure.md` updated if new files were created in locations not already documented

---

## Blockers / Risks

- none

---

## Notes

- This task should run after T-04 so it can document the final implemented state rather than the planned state.
- Keep the documentation concise and consistent with existing style in the architecture docs.
