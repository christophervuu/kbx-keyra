# TASK

## Title

Implement reusable AI suggestion card/panel pattern with generated-vs-committed distinction

---

## ID

T-02

---

## Spec

FS-074

---

## Spec Rev

Rev: 1

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- T-01

---

## Implements

- `AE-01`
- `AE-04`
- `AE-06`

---

## Summary

Build and/or normalize a reusable suggestion review card/panel UI pattern that consistently presents generated state, comparison context against existing rule content, and explicit action affordances across suggestion-bearing surfaces.

---

## Scope

### In Scope

- Shared suggestion card/panel component contract and visual semantics.
- Generated/not-committed badges and status copy.
- Existing-vs-generated comparison block where suggestion targets existing rule content.
- Shared action row layout for Accept/Edit/Dismiss (or subset by feature capability).

### Out of Scope

- Batch accept orchestration logic.
- Backend contract changes.

---

## Relevant Areas

- `ui/src/features/mappings/components/WorkspaceSuggestionCard.tsx`
- `ui/src/features/mappings/components/WorkspaceSuggestionPreview.tsx`
- `ui/src/features/mappings/components/SuggestExpressionInline.tsx`
- `ui/src/features/mappings/components/ScalarFieldBuilder.tsx`
- `ui/src/features/mappings/components/*Explain*/*.tsx ?`
- `ui/src/features/mappings/components/*SmartFix*/*.tsx ?`
- `ui/src/features/mappings/**/*.test.tsx`

---

## Change Requirements

- Define a reusable visual/action pattern that can be applied across AI suggestion UIs.
- Ensure generated-vs-committed distinction is explicit and consistent.
- Surface comparison context when suggestion overlays existing expression/rule content.
- Keep action affordances explicit and consistent with contract from T-01.

---

## Verification Requirements

- Component tests for rendering states (generated/accepted/edited/dismissed/stale as applicable).
- Tests for comparison visibility and label semantics.
- Manual spot-check across multiple AI surfaces for consistency.

---

## Acceptance Checks

- Reusable suggestion card/panel pattern is implemented and consumed by targeted surfaces.
- Generated-vs-committed distinction is visible and consistent.
- Existing-vs-generated comparison context is available where applicable.
- Tests mapped to `AE-01`, `AE-04`, and `AE-06` pass.

---

## Blockers / Risks

- Existing feature-specific components may require incremental migration to avoid regressions.

---

## Notes

- Explain Rule may use a reduced action subset while retaining generated-state labeling consistency.
