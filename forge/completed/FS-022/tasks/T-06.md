# TASK

## Title

Update ui-application.md architecture for toolbar consolidation, resizable panels, and UX refinements

---

## ID

T-06

---

## Spec

FS-022

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01
- T-02
- T-03
- T-04
- T-05

---

## Implements

- AE-01 (indirectly — documents the architectural change)

---

## Summary

Update `forge/architecture/ui-application.md` to reflect all layout changes from FS-022: GlobalToolbar removal, control relocation, Focus mode removal, resizable panel model, test case selector, Rules View search, and unconditional auto-preview on Apply. Update the component hierarchy, module structure, and any affected hook contracts. Update `INDEX.md` last-updated date.

---

## Scope

### In Scope

- Update the "Mapping Editor Architecture" section of `ui-application.md`.
- Update the layout diagram to remove the GlobalToolbar row. The diagram should show only NavBar + EditorTopBar above the three-column area.
- Update the slot props list: remove `toolbarContent` slot.
- Update the component hierarchy to remove `GlobalToolbar` and `BreadcrumbNav`.
- Add `sort`, `onSortChange`, `view`, `onViewToggle` to the `TargetWorklist` description.
- Document Auto-map button relocation to `EditorTopBar`.
- Remove all references to Focus mode, breadcrumb mode, and `BreadcrumbNav` behavior.
- Add a "Resizable Panel Layout" subsection documenting: `useResizableLayout` hook contract, drag handle behavior, min/max constraints, collapse behavior, localStorage persistence key and schema.
- Update the "Inline Preview Strip" subsection to document the test case selector dropdown (in both InlinePreviewStrip and BottomArea) and unconditional auto-preview behavior (no toggle).
- Add Rules View search description to the RuleList section or create a subsection.
- Update the Module Structure to add `use-resizable-layout.ts` and remove `GlobalToolbar.tsx`.
- Update `forge/architecture/INDEX.md` last-updated date for `ui-application.md`.

### Out of Scope

- Modifying any source code.
- Updating `project-structure.md` (handled by task agents in T-01/T-02 if new files are created).
- Updating any other architecture documents.

---

## Relevant Areas

- `forge/architecture/ui-application.md`
- `forge/architecture/INDEX.md`

---

## Change Requirements

**Layout diagram update:** Replace the current three-row layout diagram (NavBar / EditorTopBar / GlobalToolbar / columns / bottom) with a two-row layout:

```
NavBar
EditorTopBar (+ Auto-map placeholder)
Source Panel | Target Panel (+ sort, view toggle, search, filters) | Builder Panel
InlinePreviewStrip / BottomArea
```

Include resize handles in the diagram.

**Slot props:** Remove `toolbarContent`. Document that sort/view controls are now internal to TargetWorklist.

**Component hierarchy:** Remove `GlobalToolbar` and `BreadcrumbNav` entries. Add `ResizeHandle` if it was extracted as a separate component.

**Resizable Panel Layout subsection:**
- Document `useResizableLayout` hook: inputs (none), outputs (`layout`, handle props, collapse/expand actions).
- Document localStorage schema (`keyra:editor-layout`).
- Document min-width constraints per panel.
- Document collapse behavior: Source and Bottom collapsible; Target and Builder not collapsible.
- Document fallback behavior on missing/corrupt localStorage.

**Inline Preview Strip updates:**
- Remove `autoPreview` toggle documentation.
- Document unconditional auto-preview on Apply.
- Document test case selector: `testCases` and `onLoadTestCase` props, dropdown behavior, empty state.

**Rules View search:**
- Document search bar in RuleList: internal state, debounced substring match across target/expression/type, DnD disabled during search, match count display.

**View Toggle Pattern:** Update to note the toggle is now in TargetWorklist's toolbar area, not in GlobalToolbar.

**Breadcrumb Drill-Down Mode:** Remove this entire subsection (Focus mode is eliminated).

---

## Verification Requirements

- Architecture document is internally consistent (no references to removed components).
- `INDEX.md` last-updated date for `ui-application.md` is updated.
- No source code changes.

---

## Acceptance Checks

- [ ] `ui-application.md` layout diagram shows two header rows (NavBar + EditorTopBar), no GlobalToolbar.
- [ ] `GlobalToolbar` and `BreadcrumbNav` removed from component hierarchy.
- [ ] Resizable Panel Layout subsection documents hook contract, constraints, collapse, and persistence.
- [ ] Inline Preview Strip subsection documents test case selector and unconditional auto-preview.
- [ ] Rules View search documented.
- [ ] Breadcrumb Drill-Down Mode subsection removed.
- [ ] View Toggle Pattern updated to reference TargetWorklist toolbar.
- [ ] `INDEX.md` last-updated date updated.

---

## Blockers / Risks

- none

---

## Notes

- Read the completed T-01 through T-05 implementations before writing the architecture update to ensure accuracy.
- If any component was renamed or new files were created during T-01–T-05, reflect those in the Module Structure.
