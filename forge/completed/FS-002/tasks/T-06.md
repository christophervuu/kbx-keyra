# TASK

## Title

Update mapping-engine.md with parser architecture

---

## ID

T-06

---

## Spec

FS-002

---

## Spec Rev

Rev: 1

---

## Agent

task

---

## Status

done

---

## Depends On

- T-04

---

## Implements

- AE-02 (documents the parse() API contract)
- AE-04 (documents AST node hierarchy)

---

## Summary

Update `forge/architecture/mapping-engine.md` to document the DSL parser's role in the engine pipeline, the AST node type hierarchy, the `parse()` API contract, and the internal module structure of `src/engine/dsl/`. This replaces the "(future)" placeholder for the `dsl/` module with concrete architecture documentation reflecting the implemented parser.

---

## Scope

### In Scope

- Update the Module Structure section to replace `dsl/ (future)` with actual file listing:
  ```
  dsl/
    index.ts            Barrel export + parse() public API
    types.ts            AST node types, Token types, ParseOptions, ParseResult
    tokenizer.ts        Lexer — expression string → Token[]
    parser.ts           Recursive descent parser — Token[] → AstNode
  ```
- Update the Internal Module Boundaries table:
  - Change `dsl/` row from "(future)" to actual responsibility and import list
- Add a new section: **DSL Parser** with subsections:
  - **Pipeline**: tokenize → parse → validate (when registry provided)
  - **AST Node Hierarchy**: document the discriminated union and all six node types
  - **parse() API Contract**: document the public function signature, ParseOptions, ParseResult
  - **Error Production**: which codes the parser emits (E001, E002, E003, E004) and at which stage
- Update the "Future Entry Points" note about `parse()` to reflect it now exists
- Update the Execution Pipeline diagram's "Parse" step to note it uses the `dsl/` module
- Update the `Last Updated` date in `forge/architecture/INDEX.md`

### Out of Scope

- Changes to `forge/architecture/project-structure.md` (only if new directories were created — `dsl/` already exists)
- Changes to any source code
- Changes to any other architecture documents

---

## Relevant Areas

- `forge/architecture/mapping-engine.md` — primary file to update
- `forge/architecture/INDEX.md` — update `Last Updated` date for mapping-engine.md

---

## Change Requirements

After this task is complete:

- `forge/architecture/mapping-engine.md` documents the `dsl/` module with:
  - Actual file listing (no more "future" placeholder)
  - Correct import rules (imports from `types/`, `diagnostics/`, `registry/`)
  - The parse() function signature and behavior
  - The AST node type hierarchy
  - The tokenization → parsing → validation pipeline
  - Error codes emitted at each stage
- The "Future Entry Points" section is updated (parse() is now a real entry point)
- `forge/architecture/INDEX.md` has an updated `Last Updated` date for mapping-engine.md
- The document remains accurate and internally consistent

---

## Verification Requirements

- The document is valid Markdown
- The module structure matches the actual files created in T-01 through T-04
- The import rules are consistent with the existing Internal Module Boundaries section
- The AST node types match what was defined in T-01
- The parse() API signature matches what was implemented in T-04

---

## Acceptance Checks

- `forge/architecture/mapping-engine.md` updated with DSL parser documentation
- No "(future)" placeholder remains for the `dsl/` module
- AST node hierarchy is documented
- parse() API contract is documented
- Error production stages are documented
- `forge/architecture/INDEX.md` Last Updated date is current
- Document is internally consistent

---

## Blockers / Risks

- none

---

## Notes

- This task should be executed after T-04 so the architecture document reflects the actual implementation rather than the spec's intent. If the implementation deviated from the spec in any way during T-01–T-04, this document should reflect reality.
- Keep the documentation concise and structural — it's a reference document, not a tutorial. Match the existing style of the mapping-engine.md document.
