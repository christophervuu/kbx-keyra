# TASK

## Title

Remove deprecated TransformPipeline components

---

## ID

T-10

---

## Spec

FS-029

---

## Spec Rev

Rev: 2

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- T-09

---

## Implements

- (cleanup — does not directly implement an AE but enables spec completeness)

---

## Summary

Remove the deprecated `TransformPipeline` and `TransformPipelineStep` components and their associated tests now that the new Source Card builder is fully integrated. Clean up any dead imports, unused state types from the old pipeline model, and the old `pipeline-expression-generator.ts` if fully superseded.

---

## Scope

### In Scope

- Delete `ui/src/features/mappings/components/TransformPipeline.tsx` and its test file.
- Delete `ui/src/features/mappings/components/TransformPipelineStep.tsx` and its test file.
- Remove old pipeline-specific types from `expression-builder-state.ts` (e.g. old `TransformStep`, old `ValueModeState` shape if still present as dead code).
- Remove or archive `pipeline-expression-generator.ts` if the new generator (T-07) fully replaces it.
- Remove or archive `pipeline-decomposer.ts` if the new decomposer (T-08) fully replaces it.
- Remove all dead imports referencing deleted modules.
- Update barrel/index exports.

### Out of Scope

- Functional changes to the new builder.
- Changes to Conditional or Value Map modes.
- Changes to `TransformFunctionPicker` (still used by new builder).

---

## Relevant Areas

- `ui/src/features/mappings/components/TransformPipeline.tsx` (delete)
- `ui/src/features/mappings/components/TransformPipelineStep.tsx` (delete)
- `ui/src/features/mappings/lib/pipeline-expression-generator.ts` (delete or archive)
- `ui/src/features/mappings/lib/pipeline-decomposer.ts` (delete or archive)
- `ui/src/features/mappings/lib/expression-builder-state.ts` (remove dead types)
- `ui/src/features/mappings/index.ts` (update barrel)

---

## Change Requirements

- All references to deleted files must be removed.
- No runtime imports of deleted modules remain.
- The build, typecheck, and all tests must pass after removal.
- If any other module still imports from the deleted files (e.g. tests), those imports must be updated or the tests removed/rewritten.

---

## Verification Requirements

- Full typecheck passes (`tsc --noEmit`).
- Full test suite passes (no broken imports).
- Build passes (`pnpm build` or equivalent).
- Lint passes.
- No references to deleted file paths remain in the codebase.

---

## Acceptance Checks

- `TransformPipeline.tsx` and `TransformPipelineStep.tsx` no longer exist.
- No dead imports or references to deleted modules in any file.
- Typecheck passes.
- All tests pass.
- Build passes.
- Lint passes.

---

## Blockers / Risks

- Must be done AFTER T-09 confirms the new builder is fully functional.
- Risk: some module may reference old pipeline code in an unexpected place. A full-repo grep for deleted filenames should be done.

---

## Notes

This is a cleanup task. Do a thorough search for all imports of the deleted modules before removal. Check test files, barrel exports, and any documentation references.
