# TASK

## Title

Implement LocalStorageAdapter and HttpAdapter value-table parity

---

## ID

T-05

---

## Spec

FS-096

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01
- T-03

---

## Implements

- AE-10
- AE-12
- AE-13
- AE-17
- AE-23

---

## Summary

Implement adapter-level value-table operations so offline/local and backend modes expose equivalent behavior for management, revision pinning, resolve-before-save embedding, and adoption workflows.

---

## Scope

### In Scope

- Implement LocalStorageAdapter value-table storage/revision/usage behavior.
- Implement HttpAdapter method mappings for all value-table API routes.
- Ensure resolve-before-save mapping flow embeds pinned resolved entries in draft/save payloads.
- Add adapter parity and regression tests.

### Out of Scope

- React UI presentation logic.
- Engine internals.

---

## Relevant Areas

- `ui/src/lib/api/local-storage-adapter.ts`
- `ui/src/lib/api/http-adapter.ts`
- `ui/src/lib/api/http-client.ts` (if request utility updates needed)
- `ui/src/lib/api/*.test.ts`

---

## Change Requirements

- Offline/local mode must provide deterministic behavior matching backend contract semantics.
- Adapter errors must map to canonical app-error handling paths.
- Adoption flow must not silently repin revisions.

---

## Verification Requirements

- Adapter tests verify parity for CRUD/revision/adoption/resolve operations.
- Integration-style tests verify mapping config contains resolved table data before preview/save in offline mode.
- Lint/typecheck passes.

---

## Acceptance Checks

- LocalStorageAdapter and HttpAdapter expose equivalent value-table capabilities.
- Mapping save payload includes pinned revision + resolved entries after selection/adoption.
- Offline preview with project-table-backed rules works without backend calls.

---

## Blockers / Risks

- Drift between local adapter emulation and backend edge-case behavior.

---

## Notes

- Keep method naming and response envelopes aligned with `ApiAdapter` contracts from T-01.
