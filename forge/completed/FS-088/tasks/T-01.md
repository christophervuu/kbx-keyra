# TASK

## Title

Replace Create Mapping wizard with single-page layout shell

---

## ID

T-01

---

## Spec

FS-088

---

## Spec Rev

Rev: 2

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- none

---

## Implements

- `AE-01`
- `AE-02`
- `AE-16`

---

## Summary

Refactor `CreateMappingPage` from step-based wizard composition into a full-page, sectioned setup layout that matches current Dashboard/Project Overview design conventions and removes the legacy step indicator flow.

---

## Scope

### In Scope

- Remove stepper/wizard state and rendering from `CreateMappingPage`.
- Implement full-page section order: Header, Mapping Details, Schema Selection, Schema Summary, Start From, Footer.
- Align spacing, cards, typography, and page-width usage with current redesigned surfaces.
- Preserve route and cancel navigation behavior.

### Out of Scope

- Detailed schema card behavior/content wiring.
- Add-schema modal integration.
- Start mode submission branching logic.
- Backend/model changes.

---

## Relevant Areas

- `ui/src/features/projects/components/CreateMappingPage.tsx`
- `ui/src/features/projects/components/__tests__/CreateMappingPage.test.tsx`
- `ui/src/routes/pages/CreateMapping.tsx`

---

## Change Requirements

- Wizard-only UI elements (step indicator and next/back stepping) are removed.
- Page header uses copy from spec (`Create Mapping` + subtitle).
- Render placeholders/section containers for all required sections so subsequent tasks can incrementally wire behavior.
- Maintain existing test id root (`page-create-mapping`) unless tests are intentionally updated with documented replacements.

---

## Verification Requirements

- Add/update tests asserting no wizard step progression UI remains.
- Verify section headings exist in expected order.
- Run UI typecheck/lint and targeted tests for Create Mapping page.

---

## Acceptance Checks

- `AE-01` and `AE-02` pass with automated coverage.
- Legacy stepper UI is absent from page render.
- Out-of-scope surfaces listed in `AE-16` are not introduced in this task.

---

## Blockers / Risks

- Existing test suite tightly coupled to step-based DOM structure will require broad assertion updates.

---

## Notes

- Keep this task layout-focused; do not mix in submission-flow branching work.
