# TASK

## Title

Update ui-application.md architecture document for target-driven layout

---

## ID

T-13

---

## Spec

FS-020

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01
- T-02
- T-05
- T-07
- T-08
- T-12

---

## Implements

- (architecture documentation — no acceptance example directly)

---

## Summary

Update `forge/architecture/ui-application.md` to document the new target-driven three-column Mapping Editor layout, replacing the current 8-panel grid documentation. This includes the new component hierarchy, slot model, hook contracts, interaction patterns, and view toggle architecture.

---

## Scope

### In Scope

- Update the "Mapping Editor Architecture" section in `ui-application.md`:
  - Replace 8-panel grid description with three-column + bottom area layout
  - Document new component hierarchy: `MappingEditorPage` → `GlobalToolbar` + `SourceSchemaPanel` + `TargetWorklist` + Builder/Editor (conditional) + `BottomArea`
  - Document view toggle pattern (Target View vs Rules View)
  - Document node-type-specific builder pattern (scalar/object/array)
  - Document `useTargetStatus` hook contract
  - Document `useArrayBuilder` hook contract
  - Document drag-and-drop pattern
  - Document breadcrumb drill-down mode
- Update the Module Structure section to reflect new files
- Update "Last Updated" date in INDEX.md
- Preserve all other sections of `ui-application.md` (adapter pattern, state management, routing, etc.)

### Out of Scope

- Changing other architecture documents
- Documenting implementation details of individual components
- Creating a new architecture document (this updates the existing one)

---

## Relevant Areas

- `forge/architecture/ui-application.md` — primary update target
- `forge/architecture/INDEX.md` — date update

---

## Change Requirements

After this task is complete:

- "Mapping Editor Architecture" section accurately describes the three-column layout
- Old 8-panel grid description is replaced (not left as stale documentation)
- New hook contracts documented: `useTargetStatus`, `useArrayBuilder`
- View toggle architecture documented
- Node-type-specific right panel pattern documented
- Bottom area (preview/diagnostics) relocation documented
- Source panel DnD pattern documented
- Module structure section updated with new files
- INDEX.md `Last Updated` column reflects today's date for `ui-application.md`

---

## Verification Requirements

- Architecture document is internally consistent (no references to old 8-panel grid)
- No stale Panel 3/4/5/6 references in the Mapping Editor section
- INDEX.md date updated
- Document renders valid Markdown

---

## Acceptance Checks

- `forge/architecture/ui-application.md` Mapping Editor section reflects three-column layout
- No stale references to old 8-panel layout in the Mapping Editor section
- New hooks and patterns documented
- Module structure includes new files
- `forge/architecture/INDEX.md` has updated date
- Valid Markdown throughout

---

## Blockers / Risks

- Must wait for implementation tasks to stabilize before writing final architecture documentation — initial draft can be written from spec, refined after implementation

---

## Notes

- This task runs after implementation tasks are complete (or near-complete) so the documentation reflects the actual implementation rather than planned design.
- Keep existing architecture sections (adapter, state management, routing, Schema Detail, Expression Builder) unchanged unless they contain stale cross-references to the old mapping editor layout.
- The Expression Builder Architecture section may need minor updates to note that it now renders within the ScalarFieldBuilder context rather than a standalone Panel 4 slot.
