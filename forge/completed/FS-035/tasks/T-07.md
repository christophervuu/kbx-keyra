# TASK

## Title

Update ui-application.md with Test Lab execution feedback architecture

---

## ID

T-07

---

## Spec

FS-035

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01
- T-02
- T-03
- T-04
- T-05
- T-06

---

## Implements

- (architecture update — no specific AE)

---

## Summary

Update the `forge/architecture/ui-application.md` document to reflect the Test Lab execution feedback additions from FS-035: execution summary bar, categorized diff mismatches, diff-first UX, per-test indicators, batch execution, and suite summary. Update `INDEX.md` last-updated date.

---

## Scope

### In Scope

- Add or extend the "Advanced Testing Page" section in `ui-application.md` to document:
  - `ExecutionSummaryBar` component and its props/placement
  - Pass/fail derivation logic (`deriveExecutionVerdict`)
  - Extended diff mismatch categories and `DiffSummary` type
  - Diff-first UX behavior (auto-tab-switch, diff summary in summary bar)
  - Per-test result tracking (`TestCaseResult` type, `testResults` state map)
  - Batch execution hook (`useBatchExecution`) and `SuiteSummary` component
- Update the module structure tree to include new files
- Update `forge/architecture/INDEX.md` last-updated date for `ui-application.md`

### Out of Scope

- Changes to any source code
- Changes to other architecture documents
- Changes to the spec or other task files

---

## Relevant Areas

- `forge/architecture/ui-application.md`
- `forge/architecture/INDEX.md`

---

## Change Requirements

### Module Structure Update

Add new files to the module structure tree under the mappings feature:

```
components/
  preview/
    ExecutionSummaryBar.tsx    Execution summary bar (pass/fail, duration, diagnostics, version context)
    SuiteSummary.tsx           Batch execution suite summary with per-test rows
hooks/
  use-batch-execution.ts      Sequential batch test case execution hook
lib/
  execution-result-utils.ts   Pass/fail derivation (deriveExecutionVerdict) + diff summary formatting
```

### Advanced Testing Page Section

Extend the existing AdvancedTestingPage documentation to cover:

1. **Execution Summary Bar** — placement (between top bar and tabs), props contract, verdict derivation rules
2. **Diff-first UX** — auto-tab-switch behavior when expected output exists, diff summary label in summary bar, diff tab badge
3. **Categorized Diff Mismatches** — the six `DiffChangeType` categories, `DiffSummary` type, classification logic summary
4. **Per-Test Indicators** — `TestCaseResult` type, `testResults` state map at AdvancedTestingPage level, TestCaseManager rendering
5. **Batch Execution** — `useBatchExecution` hook contract, "Run All" button, sequential execution model, progress tracking, `SuiteSummary` component and its integration

### Diff Infrastructure Section

Add or extend documentation about the diff type system to note the six mismatch categories and `DiffSummary` addition.

### INDEX.md

Update `Last Updated` for `ui-application.md` to the current date.

---

## Verification Requirements

- Architecture document accurately reflects the implemented components, hooks, types, and behavior
- Module structure tree includes all new files from FS-035
- INDEX.md last-updated date is current
- No inaccuracies relative to the implemented code

---

## Acceptance Checks

- `ui-application.md` documents ExecutionSummaryBar, SuiteSummary, useBatchExecution, deriveExecutionVerdict, categorized diff types, diff-first UX, and per-test indicators
- Module structure tree is updated with new files
- `INDEX.md` last-updated date for `ui-application.md` is updated
- Document is internally consistent

---

## Blockers / Risks

- Depends on all other tasks completing so the architecture document reflects the final implementation

---

## Notes

- This task should be executed last, after all implementation tasks are complete, so the architecture document accurately reflects what was built.
- Reference the existing AdvancedTestingPage documentation pattern for style and level of detail consistency.
