# TASK

## Title

Add UI safety regression checks for validation-before-display and no-auto-commit

---

## ID

T-04

---

## Spec

FS-075

---

## Spec Rev

Rev: 2

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- T-01

---

## Implements

- `AE-05`
- `AE-06`

---

## Summary

Implement UI-focused regression coverage proving AI-generated DSL suggestions are validation-gated before apply-ready display and that no AI lifecycle path mutates mapping state without explicit user accept/apply.

---

## Scope

### In Scope

- Hook/component tests for Suggest, Smart Fix, and Auto-Map suggestion lifecycles.
- Assertions for invalid suggestion rendering and apply-readiness gating.
- Assertions for generate/refresh/retry/failure non-mutation behavior.
- Shared test helpers for pre/post draft-state comparison where useful.

### Out of Scope

- New UX redesign work.
- Backend test harness changes unrelated to UI assertions.

---

## Relevant Areas

- `ui/src/features/mappings/hooks/use-suggest-expression.ts`
- `ui/src/features/mappings/hooks/use-auto-map-workspace.ts`
- `ui/src/features/mappings/hooks/*smart-fix* ?`
- `ui/src/features/mappings/components/SuggestExpressionInline.tsx`
- `ui/src/features/mappings/components/WorkspaceSuggestionCard.tsx`
- `ui/src/features/mappings/**/*.test.tsx`

---

## Change Requirements

- Ensure tests prove validation metadata is present before apply-ready affordances are shown.
- Ensure tests prove no state mutation on non-accept lifecycle events.
- Ensure tests cover explicit accept/apply as the only mutation path.

---

## Verification Requirements

- Targeted UI test suites pass for in-scope surfaces.
- Assertions map directly to `AE-05` and `AE-06`.
- Typecheck remains clean for touched UI files.

---

## Acceptance Checks

- Validation-before-display regressions are covered and passing.
- No-auto-commit regressions are covered and passing.
- Explicit accept/apply remains the only tested mutation path for AI suggestions.

---

## Blockers / Risks

- Surface-specific state models may require helper normalization for stable tests.

---

## Notes

- Prefer black-box behavior assertions over implementation-detail coupling.
