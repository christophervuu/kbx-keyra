# TASK

## Title

Add prompt regression goldens and non-deterministic eval thresholds for AI features

---

## ID

T-02

---

## Spec

FS-075

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01

---

## Implements

- `AE-01`
- `AE-02`

---

## Summary

Implement prompt regression/golden coverage and a bounded non-deterministic evaluation policy so prompt/runtime changes are measured and gated for quality drift.

---

## Scope

### In Scope

- Create curated golden prompt corpus by AI feature.
- Define expected outputs/rubric and drift thresholds.
- Add repeatable eval runner and drift report output.
- Define golden maintenance/versioning rules.

### Out of Scope

- Feature UX behavior changes.
- Backend endpoint contract refactors unrelated to eval harness.

---

## Relevant Areas

- `src/lib/ai/*`
- `tests/lambda/ai/*.test.ts`
- `tests/*ai*/* ?`
- `scripts/*prompt* ?`
- `package.json ?`

---

## Change Requirements

- Add deterministic golden fixtures where possible and rubric-based non-deterministic checks where needed.
- Enforce explicit threshold policy for pass/fail.
- Emit actionable drift diagnostics tied to prompt IDs/features.

---

## Verification Requirements

- Golden/eval command runs locally and in CI mode.
- Controlled prompt change produces expected drift signal.
- Results map to feature matrix from T-01.

---

## Acceptance Checks

- Golden corpus exists for in-scope Phase 2 AI features.
- Non-deterministic eval thresholds are documented and enforced.
- Drift report output is generated and actionable.

---

## Blockers / Risks

- Model variance may require calibration to avoid noisy failures.

---

## Notes

- Keep threshold policy explicit and revision-controlled.
