# TASK

## Title

Define empty states, loading skeleton, and placeholder scaffolds for all Project Overview sections

---

## ID

T-06

---

## Spec

FS-050

---

## Spec Rev

Rev: 2

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- T-02
- T-03

---

## Implements

- `AE-11`
- `AE-12`
- `AE-15`

---

## Summary

Update the loading skeleton, empty states, and placeholder scaffolds for the refined Project Overview page layout. The skeleton should reflect the target layout (header + summary row + mappings area + schemas area). Empty states for no-mappings and no-schemas should include contextual CTAs. Scaffold placeholders for deployment summary values in the summary row should use muted styling.

---

## Scope

### In Scope

- Update `ProjectOverviewSkeleton` to reflect the new page layout (header area, summary row, mappings table, schemas grid)
- Update no-mappings empty state in `MappingListSection` (preserve CTA, update visual treatment)
- Update no-schemas empty state in `SchemaManagementSection` (preserve CTAs, update visual treatment)
- Ensure summary row scaffold placeholders (stale deployments, ready to deploy) render with muted/disabled styling
- Ensure breadcrumb shows "Loading..." during loading state

### Out of Scope

- Creating the loading skeleton from scratch (it already exists as `ProjectOverviewSkeleton`)
- Functional changes to how data loads
- Changes to the error state or not-found state components
- Placeholder sections for features not yet specified (activity feed, etc.)

---

## Relevant Areas

- `ui/src/features/projects/components/ProjectOverviewSkeleton.tsx` (update to match new layout)
- `ui/src/features/projects/components/MappingListSection.tsx` (no-mappings empty state)
- `ui/src/features/projects/components/SchemaManagementSection.tsx` (no-schemas empty state)
- `ui/src/features/projects/components/ProjectSummaryRow.tsx` (scaffold placeholders — verify from T-03)
- `ui/src/features/projects/components/ProjectOverviewPage.tsx` (loading state rendering)

---

## Change Requirements

### Loading Skeleton Update

Update `ProjectOverviewSkeleton.tsx` to reflect the new page layout:

```
Skeleton structure:
+--------------------------------------------------------------------+
| [████████████ title] [██ btn] [██ btn] [█]                          |   ← Header area
| [██ description line] [██ date] [██ date] [██ tags]                 |
+--------------------------------------------------------------------+
| [██ metric] [██ metric] [██ metric] [██ metric] [██ link]           |   ← Summary row
+--------------------------------------------------------------------+
| [████ Mappings heading]                                              |
| [████████████████████████████████████████████]                       |   ← Table header
| [████████████████████████████████████████████]                       |   ← Table rows
| [████████████████████████████████████████████]                       |
| [████████████████████████████████████████████]                       |
+--------------------------------------------------------------------+
| [████ Schemas heading]                                               |
| [████████] [████████] [████████]                                    |   ← Schema cards
+--------------------------------------------------------------------+
```

- Use `animate-pulse` Tailwind classes consistent with existing skeleton patterns
- `role="status"` + sr-only "Loading project..." text
- Preserve `data-testid` conventions

### No-Mappings Empty State

The existing empty state in `MappingListSection` shows a dashed border container with icon and CTA. Update to ensure:

- Icon: `GitBranch` or `Map` (appropriate mapping icon)
- Heading: "No mappings yet"
- Subtext: "Create your first mapping to start transforming data between your schemas."
- CTA button: "Create Mapping" (primary button, triggers existing `onCreate` callback)
- Visual treatment: centered within the section area

### No-Schemas Empty State

The existing empty state in `SchemaManagementSection` shows a dashed border container with icon and CTAs. Update to ensure:

- Icon: `Database` or `FileJson`
- Heading: "No schemas attached"
- Subtext: "Upload a schema or link an existing one from the Schema Library to get started."
- CTA buttons: "Upload Schema" and "Link Schema" (triggering existing callbacks)
- Visual treatment: centered, lighter weight than the mappings empty state (secondary)

### Summary Row Scaffold Values

Verify that the `ProjectSummaryRow` (from T-03) renders scaffold metrics with:

- Muted text color (e.g., `text-gray-400`)
- "—" dash character instead of a count
- `aria-label` indicating the metric is a placeholder (e.g., "Stale Deployments: not yet available")

This may already be handled in T-03 — this task verifies and adjusts if needed.

### Breadcrumb Loading State

Verify that during the loading state, the breadcrumb shows "Loading..." for the project name segment (handled by T-01's `useBreadcrumbLabel` hook). No additional work should be needed here — just verify the integration.

---

## Verification Requirements

- Component test for `ProjectOverviewSkeleton`:
  - Renders with `role="status"` and sr-only text
  - Skeleton structure includes: header area shapes, summary row shapes, mapping table shapes, schema card shapes (AE-15)
  - No tab bar skeleton (consistent with tab removal in T-02)
- Component test for no-mappings empty state:
  - Icon, heading, subtext, and CTA button render (AE-11)
  - CTA button triggers the create mapping callback
  - Summary row shows "0 Mappings" (verified in T-03 tests)
- Component test for no-schemas empty state:
  - Icon, heading, subtext, and CTA buttons render (AE-12)
  - CTA buttons trigger existing callbacks
- Verify breadcrumb shows "Loading..." during loading (integration with T-01)
- `cd ui && pnpm typecheck` — zero errors
- `cd ui && pnpm lint` — zero errors

---

## Acceptance Checks

- Loading skeleton reflects the target layout (header, summary row, mappings area, schemas area) (AE-15)
- Skeleton has `role="status"` and sr-only loading text
- No-mappings empty state shows icon, heading, subtext, and CTA (AE-11)
- No-schemas empty state shows icon, heading, subtext, and CTAs (AE-12)
- Summary row scaffold placeholders have muted styling
- Breadcrumb shows "Loading..." during loading state
- Typecheck passes
- Lint passes

---

## Blockers / Risks

- none

---

## Notes

- The skeleton component currently (`ProjectOverviewSkeleton.tsx`) mimics the old 4-section layout (A-B-C-D). It needs to be updated to reflect the new layout with header, summary row, mappings-first, schemas-second ordering.
- Empty state components can be kept simple — they are placeholders that guide users to the primary actions. They do not need animation or complex visual treatment.
