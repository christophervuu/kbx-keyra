# TASK

## Title

Update tests for refined Project Overview page

---

## ID

T-07

---

## Spec

FS-050

---

## Spec Rev

Rev: 2

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- T-01
- T-02
- T-03
- T-04
- T-05
- T-06

---

## Implements

- `AE-01`
- `AE-02`
- `AE-03`
- `AE-04`
- `AE-05`
- `AE-06`
- `AE-07`
- `AE-08`
- `AE-09`
- `AE-10`
- `AE-11`
- `AE-12`
- `AE-13`
- `AE-14`
- `AE-15`
- `AE-16`
- `AE-17`
- `AE-18`

---

## Summary

Update all existing Project Overview component tests and add new tests to verify the refined page layout, breadcrumb name resolution, summary/status row, enhanced mapping rows, refined schema cards, empty states, and loading skeleton. Ensure all acceptance examples from FS-050 have automated test coverage.

---

## Scope

### In Scope

- Update existing tests in `ui/src/features/projects/components/__tests__/` that may be broken by layout changes
- Add tests for new components: `BreadcrumbContext`/`useBreadcrumbLabel`, `ProjectSummaryRow`, recently-edited affordance, `ProjectHeader`/overflow menu
- Add tests verifying acceptance examples AE-01 through AE-18
- Ensure all tests pass: `cd ui && pnpm test`

### Out of Scope

- E2E tests (covered by FS-019 infrastructure, not this spec)
- Manual testing (documented in spec but not automated here)
- Tests for components outside the projects feature module (except breadcrumb infrastructure tests)

---

## Relevant Areas

- `ui/src/features/projects/components/__tests__/` (all test files)
- `ui/src/components/layout/__tests__/` ? (breadcrumb tests — may need to create this directory)
- `ui/src/features/projects/components/__tests__/ProjectOverviewPage.test.tsx`
- `ui/src/features/projects/components/__tests__/MappingRow.test.tsx`
- `ui/src/features/projects/components/__tests__/MappingListSection.test.tsx`
- `ui/src/features/projects/components/__tests__/SchemaCard.test.tsx`
- `ui/src/features/projects/components/__tests__/SchemaManagementSection.test.tsx`
- `ui/src/features/projects/components/__tests__/ProjectStateComponents.test.tsx`

---

## Change Requirements

### Breadcrumb Tests

Add tests for `BreadcrumbContext` and `useBreadcrumbLabel`:

- **AE-01**: Render Breadcrumbs with a registered label → verify label appears instead of raw segment
- **AE-02**: Render Breadcrumbs with undefined label → verify "Loading..." appears
- **AE-03**: Render Breadcrumbs with no registered label → verify raw segment appears (fallback)
- Test cleanup: verify label is removed on unmount
- Test multiple labels: verify coexistence for different segments

### ProjectOverviewPage Tests

Update `ProjectOverviewPage.test.tsx`:

- **AE-04**: Verify header contains project name, primary action buttons, overflow menu
- **AE-06**: Verify mappings section precedes schemas section in DOM order (use DOM query order or testid positioning)
- **AE-15**: Verify loading skeleton renders header, summary row, mappings, and schemas skeleton shapes
- **AE-16**: Verify "Create Mapping" and "Add Schema" in header; overflow menu contains Deployments/Settings/Duplicate/Delete
- Verify `data-testid="page-project-overview"` preserved
- Update any tests that relied on the old 4-section layout (A-B-C-D ordering)

### ProjectSummaryRow Tests

Add tests for `ProjectSummaryRow`:

- **AE-05**: Verify counts display correctly (5 mappings, 3 schemas, 1 error with red accent)
- **AE-18**: Verify 0 errors shows neutral styling (no red accent)
- Verify scaffold metrics show placeholder values
- Verify "View Deployments" link with correct href

### MappingRow Tests

Update `MappingRow.test.tsx`:

- **AE-07**: Verify condensed "Not deployed" when all statuses are `not-deployed`
- **AE-07**: Verify individual badges when any status differs
- **AE-08**: Verify filled badge styling for `ready` (green), `has-errors` (red), `draft` (slate)
- **AE-14**: Verify deploy badge/label click navigates to deployment page (not a deploy action)
- **AE-17**: Verify Test Lab action link with correct href

### MappingListSection Tests

Update `MappingListSection.test.tsx`:

- **AE-09**: Verify recently-edited card appears when recent activity matches a project mapping
- **AE-10**: Verify recently-edited card is hidden when no matching activity
- **AE-09**: Verify "Resume" link navigates to Mapping Editor
- **AE-11**: Verify no-mappings empty state (icon, heading, subtext, CTA)

### SchemaCard Tests

Update `SchemaCard.test.tsx`:

- **AE-13**: Verify origin badges (CDM=blue, Published=purple, Local=gray)
- **AE-13**: Verify scope badges (Global/Project)
- **AE-13**: Verify sync status indicators
- Verify "Used by N mappings" count display

### SchemaManagementSection Tests

Update `SchemaManagementSection.test.tsx`:

- **AE-12**: Verify no-schemas empty state (icon, heading, subtext, CTAs)
- Verify section heading has lighter weight than mappings heading (class assertion)

### Skeleton Tests

Update `ProjectStateComponents.test.tsx`:

- **AE-15**: Verify skeleton renders target layout structure (header, summary row, mappings, schemas)
- Verify `role="status"` and sr-only text

---

## Verification Requirements

- All tests pass: `cd ui && pnpm test`
- Every AE from the spec has at least one test case
- No existing tests are deleted — only updated or supplemented
- `cd ui && pnpm typecheck` — zero errors
- `cd ui && pnpm lint` — zero errors

---

## Acceptance Checks

- All acceptance examples AE-01 through AE-18 have corresponding test cases
- All existing project feature tests pass (updated as needed)
- All new test files follow existing naming conventions
- `cd ui && pnpm test` exits 0
- `cd ui && pnpm typecheck` exits 0
- `cd ui && pnpm lint` exits 0

---

## Blockers / Risks

- Tests that mock `useProjectOverview` or render `ProjectOverviewPage` with a router context may need significant updates due to the layout restructure. Budget extra time for test setup changes.
- Testing the recently-edited affordance requires mocking `localStorage` for `keyra:recent-activity`. Use the existing test patterns from FS-049.

---

## Notes

- This task is the verification gate — it should run after all other implementation tasks (T-01 through T-06) are complete.
- If existing tests fail due to layout changes made in earlier tasks, those tasks should fix the immediately-affected tests. This task covers comprehensive AE coverage and any tests that were deferred.
- Test naming should reference AE IDs for traceability (e.g., `it('AE-07: shows condensed deploy badge when all not-deployed', ...)`).
