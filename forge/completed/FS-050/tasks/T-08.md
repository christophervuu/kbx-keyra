# TASK

## Title

Update project-structure.md for new Project Overview files

---

## ID

T-08

---

## Spec

FS-050

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-07

---

## Implements

- (infrastructure — no direct AE mapping)

---

## Summary

Update `forge/architecture/project-structure.md` to reflect any new files or structural changes made to the `ui/src/features/projects/` and `ui/src/components/layout/` directories during FS-050 implementation. This ensures the architecture documentation stays current with the actual repository layout.

---

## Scope

### In Scope

- Update `project-structure.md` to document new files created in FS-050:
  - `ui/src/components/layout/BreadcrumbContext.tsx` (breadcrumb name resolution context)
  - `ui/src/features/projects/components/ProjectSummaryRow.tsx` (summary/status row)
  - `ui/src/features/projects/components/ProjectHeader.tsx` (new header component, if created)
  - Any other new files created during T-01 through T-06
- Update existing entries if components were renamed or retired (e.g., `ProjectActionsSection` deprecated)
- Update INDEX.md `Last Updated` date for `project-structure.md` if changes are material

### Out of Scope

- Changes to `ui-application.md` (no new architectural patterns introduced — this spec extends existing patterns)
- Creating new architecture documents (no new subsystem introduced)
- Changes to any architecture document other than `project-structure.md`

---

## Relevant Areas

- `forge/architecture/project-structure.md`
- `forge/architecture/INDEX.md`

---

## Change Requirements

### project-structure.md Updates

Scan the actual files created/modified during T-01 through T-07 and update the `ui/` section of `project-structure.md`:

**Expected new entries under `ui/src/components/layout/`:**
- `BreadcrumbContext.tsx` — Breadcrumb name resolution context + `useBreadcrumbLabel` hook + `BreadcrumbProvider`

**Expected new/updated entries under `ui/src/features/projects/components/`:**
- `ProjectSummaryRow.tsx` — Compact project health metrics row (mapping/schema/error counts, scaffold deploy counts, deployments link)
- `ProjectHeader.tsx` — Consolidated header with project identity, inline editing, primary actions, overflow menu (if created as a new component)
- Note any retired/deprecated components (e.g., `ProjectActionsSection.tsx` if no longer rendered)

**Expected new/updated test entries:**
- Any new test files under `__tests__/`

### INDEX.md Updates

- Update the `Last Updated` date for `project-structure.md` entry if the document is changed
- No new rows needed (no new architecture document created)

---

## Verification Requirements

- `project-structure.md` accurately reflects the actual files in `ui/src/features/projects/` and `ui/src/components/layout/`
- INDEX.md `Last Updated` date is current
- No entries reference files that don't exist
- No new files exist that are undocumented

---

## Acceptance Checks

- `project-structure.md` includes entries for all new files created in FS-050
- Deprecated/retired components are noted
- INDEX.md date is updated
- Document content is accurate against actual repo state

---

## Blockers / Risks

- none

---

## Notes

- This task should be done after all implementation tasks are complete (T-01 through T-07) so the documentation accurately reflects the final state.
- Run `ls -la ui/src/features/projects/components/` and `ls -la ui/src/components/layout/` to verify actual file list before updating the document.
