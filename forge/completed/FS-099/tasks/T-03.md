# TASK

## Title

Define RenderableOutput and output-path index contracts for JSON Output branch

---

## ID

T-03

---

## Spec

FS-099

---

## Spec Rev

Rev: 3

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01

---

## Implements

- AE-09
- AE-29

---

## Summary

Create explicit discriminated output contract types for Output rendering (JSON branch in FS-099), including canonical `serializedText`, node count/size metadata, and output-path index entries used by selection and search.

---

## Scope

### In Scope

- Define `RenderableOutput` JSON branch and `OutputPathEntry`/`OutputPathIndex` contracts.
- Build formatter/path-index generation in single traversal where practical.
- Ensure copy/download use complete `serializedText`.

### Out of Scope

- XML branch implementation.
- UI-level rendering interactions.

---

## Relevant Areas

- `ui/src/lib/types/domain.ts` (or feature-local output contract module)
- `ui/src/features/mappings/lib/*` (new output contract/formatter module)
- `ui/src/features/mappings/**/*.test.ts`

---

## Change Requirements

- Contract must be explicit, discriminated, and format-extensible.
- No generic XML-as-JSON object contract requirement in FS-099.

---

## Verification Requirements

- Unit tests for contract construction and metadata correctness.
- Tests proving copy payload equals full serialized text in limited mode.

---

## Acceptance Checks

- Contract types compile and are consumed by Output view.
- AE-29 copy-completeness behavior is enforced by tests.

---

## Blockers / Risks

- Type placement may affect multiple viewer/test modules.

---

## Notes

- Keep naming aligned with repository conventions if exact interface names differ.
