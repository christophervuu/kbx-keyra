# TASK

## Title

Update architecture documentation for suggest-expression Lambda

---

## ID

T-04

---

## Spec

FS-044

---

## Spec Rev

Rev: 1

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01

---

## Implements

- AE-01

---

## Summary

Update `forge/architecture/ai-runtime.md` to document the `suggest-expression.ts` Lambda as a second consumer of the shared AI runtime, and update `forge/architecture/project-structure.md` to reflect the new files created by T-01 and T-03.

---

## Scope

### In Scope

- Update `ai-runtime.md` Module Structure section to list `suggest-expression.ts`
- Update `ai-runtime.md` Lambda Handler Pattern section to mention the second consumer
- Update `project-structure.md` to list `suggest-expression.ts` under `src/lambda/ai/`
- Update `project-structure.md` to list new test files and fixture directories
- Update `ai-runtime.md` `Last Updated` date
- Update `INDEX.md` `Last Updated` date for `ai-runtime.md` if changed

### Out of Scope

- Changes to the handler implementation
- Changes to the shared AI runtime
- Changes to any other architecture documents

---

## Relevant Areas

- `forge/architecture/ai-runtime.md`
- `forge/architecture/project-structure.md`
- `forge/architecture/INDEX.md`

---

## Change Requirements

### ai-runtime.md

1. **Module Structure** section: Add `suggest-expression.ts` alongside `explain-rule.ts`:
   ```
   src/
     lambda/
       ai/
         explain-rule.ts       First consumer Lambda — thin handler shell
         suggest-expression.ts Second consumer Lambda — NL to rule handler
   ```

2. **Lambda Handler Pattern** section: Update the description to reference both Lambdas as following the same pattern. The example can remain `explain-rule.ts` but the text should note that `suggest-expression.ts` follows an identical structure with different required fields and promptId.

3. **Constraints** section: No change needed — the existing constraint about Step Functions already lists `suggest-expression` as a lightweight endpoint.

### project-structure.md

1. Add `suggest-expression.ts` under the `src/lambda/ai/` listing:
   ```
   ai/               AI lambdas
     explain-rule.ts  AI explain-rule lambda handler consuming shared runtime
     suggest-expression.ts  AI suggest-expression lambda handler consuming shared runtime
   ```

2. Add test file listings (if not already documented at a granular level).

### INDEX.md

Update the `Last Updated` date for `ai-runtime.md` to `2026-05-11` (or current date at time of execution) if it changed.

---

## Verification Requirements

- `ai-runtime.md` lists both `explain-rule.ts` and `suggest-expression.ts` in the Module Structure
- `project-structure.md` lists `suggest-expression.ts` under `src/lambda/ai/`
- `INDEX.md` dates are current
- No content removed — only additions

---

## Acceptance Checks

- `forge/architecture/ai-runtime.md` Module Structure includes `suggest-expression.ts`
- `forge/architecture/ai-runtime.md` Lambda Handler Pattern references both consumer Lambdas
- `forge/architecture/project-structure.md` lists `suggest-expression.ts` under `src/lambda/ai/`
- `forge/architecture/INDEX.md` `Last Updated` for `ai-runtime.md` is current
- No existing content was removed or semantically changed

---

## Blockers / Risks

- none

---

## Notes

- This is a lightweight documentation update. The architecture of the shared AI runtime itself does not change — only the list of consumers grows by one.
- The ai-runtime.md already mentions suggest-expression in the Constraints section ("Step Functions are not used for lightweight AI endpoints (explain-rule, suggest-expression, smart-fix)"), confirming this Lambda was anticipated by the architecture.
