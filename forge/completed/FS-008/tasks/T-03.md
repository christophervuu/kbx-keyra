# TASK

## Title

Define the ApiAdapter TypeScript interface

---

## ID

T-03

---

## Spec

FS-008

---

## Spec Rev

Rev: 1

---

## Agent

task

---

## Status

done

---

## Depends On

- T-02

---

## Implements

- AE-01 (adapter contract exists for provider to supply)
- AE-08 (defines methods that LocalStorageAdapter must implement)
- AE-11

---

## Summary

Define the full `ApiAdapter` TypeScript interface exactly as specified in Section 8.2 of the product spec. This interface is the single contract that all downstream feature code programs against — components never access data except through this adapter.

---

## Scope

### In Scope

- `ui/src/lib/api/types.ts` — the ApiAdapter interface and any method-specific input/result types not already in domain types
- Barrel export from `ui/src/lib/api/index.ts`

### Out of Scope

- LocalStorageAdapter implementation (T-05)
- HttpAdapter implementation (Phase 1, not this spec)
- AdapterProvider context (T-05)
- Domain types (T-02 — already defined)

---

## Relevant Areas

- `ui/src/lib/api/types.ts`
- `ui/src/lib/api/index.ts`
- `specs/PRODUCT-TECHNICAL.md` Section 8.2

---

## Change Requirements

Define the `ApiAdapter` interface with all method groups from Section 8.2:

```typescript
interface ApiAdapter {
  // Schemas
  listSchemas(): Promise<SchemaMetadata[]>;
  getSchema(id: string): Promise<SchemaDetail>;
  createSchema(input: CreateSchemaInput): Promise<SchemaMetadata>;
  deleteSchema(id: string): Promise<void>;

  // Mappings
  listMappings(projectId: string): Promise<MappingMetadata[]>;
  getMapping(id: string): Promise<MappingConfig>;
  createMapping(input: CreateMappingInput): Promise<MappingMetadata>;
  updateMapping(id: string, config: MappingConfig): Promise<MappingMetadata>;
  deleteMapping(id: string): Promise<void>;
  duplicateMapping(id: string, newName: string): Promise<MappingMetadata>;

  // Projects
  listProjects(): Promise<ProjectMetadata[]>;
  getProject(id: string): Promise<ProjectDetail>;
  createProject(input: CreateProjectInput): Promise<ProjectMetadata>;
  updateProject(id: string, input: UpdateProjectInput): Promise<ProjectMetadata>;
  deleteProject(id: string): Promise<void>;

  // Templates
  listTemplates(): Promise<TemplateMetadata[]>;
  getTemplate(id: string): Promise<TemplateDetail>;

  // Deployment
  getDeploymentContext(mappingId: string): Promise<DeploymentContext>;
  deploy(mappingId: string, environment: Environment): Promise<DeploymentRecord>;
  promote(mappingId: string, from: Environment, to: Environment): Promise<DeploymentRecord>;
  rollback(mappingId: string, environment: Environment, targetVersion: number): Promise<DeploymentRecord>;
  getDeploymentDiff(mappingId: string, fromVersion: number, toVersion: number): Promise<DeploymentDiff>;

  // GitHub: CDM Repo (read-only)
  listCdmSchemas(path?: string): Promise<GitHubFile[]>;
  linkCdmSchema(input: LinkCdmSchemaInput): Promise<SchemaMetadata>;
  syncCdmSchema(schemaId: string): Promise<SchemaSyncResult>;

  // GitHub: Non-CDM Repo (read-write)
  listPublishedSchemas(path?: string): Promise<GitHubFile[]>;
  publishSchemaToGitHub(schemaId: string, input: PublishSchemaInput): Promise<void>;
  linkPublishedSchema(input: LinkPublishedSchemaInput): Promise<SchemaMetadata>;

  // AI
  autoMap(input: AutoMapInput): Promise<AutoMapResult>;
  suggestExpression(input: SuggestExpressionInput): Promise<SuggestExpressionResult>;
  explainRule(input: ExplainRuleInput): Promise<string>;
  smartFix(input: SmartFixInput): Promise<SmartFixResult>;
  validateMappings(input: ValidateMappingsInput): Promise<ValidationReport>;

  // Schema Search
  querySchemaNodes(schemaId: string, query: string): Promise<SchemaSearchResult[]>;

  // Activity
  listActivity(projectId?: string, limit?: number): Promise<ActivityEntry[]>;

  // Preview
  previewOnServer(mappingId: string, input: ServerPreviewInput): Promise<ServerPreviewResult>;
}
```

Also define any input/result types not already in domain types:
- `LinkCdmSchemaInput`, `SchemaSyncResult`
- `PublishSchemaInput`, `LinkPublishedSchemaInput`
- `AutoMapInput`, `AutoMapResult`
- `SuggestExpressionInput`, `SuggestExpressionResult`
- `ExplainRuleInput`
- `SmartFixInput`, `SmartFixResult`
- `ValidateMappingsInput`, `ValidationReport`
- `SchemaSearchResult`
- `ServerPreviewInput`, `ServerPreviewResult`
- `ProjectDetail`, `TemplateDetail`

Types should import from `@/lib/types` for domain entities.

---

## Verification Requirements

- `pnpm typecheck` passes
- The interface is importable from `@/lib/api`
- All methods have correct parameter and return types

---

## Acceptance Checks

- `ui/src/lib/api/types.ts` contains the complete ApiAdapter interface
- All method groups from Section 8.2 are present
- All referenced types are defined (either in this file or imported from domain types)
- `pnpm typecheck` passes with zero errors
- `ui/src/lib/api/index.ts` exports the ApiAdapter interface

---

## Blockers / Risks

- none

---

## Notes

- The interface must be complete — even though Phase 0 won't implement all methods fully in LocalStorageAdapter, the contract must be defined for type-safety downstream
- AI and GitHub input/result types can be minimal stubs (just enough for the interface to compile) since they're not used in Phase 0
- Import domain types from T-02 — do not redefine them here
