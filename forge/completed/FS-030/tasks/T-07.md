# TASK

## Title

Update ui-application.md for transform chain model

---

## ID

T-07

---

## Spec

FS-030

---

## Spec Rev

Rev: 1

---

## Agent

task

---

## Status

done

---

## Depends On

- T-06

---

## Implements

- (architecture documentation; no direct AE mapping)

---

## Summary

Update `forge/architecture/ui-application.md` to document the transform chain model introduced by FS-030: the `TransformChainStep`/`InlineTransform` chain type, expression generation from chains, decomposition chain-walking algorithm, type compatibility enforcement, and the `CHAINABLE_TRANSFORMS` set. Update `INDEX.md` coverage description.

---

## Scope

### In Scope

- Add a "Transform Chain Model" subsection under the "Expression Builder Architecture" section in `ui-application.md`
- Document:
  - `TransformChainStep` and `InlineTransform` (chain) types
  - Chain state locations: `SourceWithTransformState.transform` and `ArgumentSlot.transform`
  - `CHAINABLE_TRANSFORMS` set and its distinction from `SINGLE_INPUT_TRANSFORMS`
  - Expression generation: iterative wrapping of chain steps
  - Decomposition: chain-walking algorithm, backward-compat heuristic, non-linear fallback
  - Type compatibility enforcement: `getChainOutputType`, `getCompatibleChainableTransforms`
  - UI rendering: vertical pipeline in SourceCard
- Update the module structure listing to include any new utility files (e.g., `transform-chain-utils.ts`)
- Update the `INDEX.md` entry for `ui-application.md` to mention FS-030 transform chain model in the Covers column
- Update `Last Updated` date in `INDEX.md`

### Out of Scope

- Code changes (documentation only)
- Other architecture documents

---

## Relevant Areas

- `forge/architecture/ui-application.md`
- `forge/architecture/INDEX.md`

---

## Change Requirements

**New subsection in ui-application.md** under "Expression Builder Architecture":

```markdown
#### Transform Chain Model (FS-030)

InlineTransform evolved from a single function wrapper to a chain of TransformChainStep entries...
```

Content must cover:
1. Type definitions and their relationships
2. Chain ordering (innermost-first: first step applied to source, last step produces output)
3. CHAINABLE_TRANSFORMS set (full list)
4. Generation algorithm (iterative wrapping)
5. Decomposition algorithm (outermost-to-innermost walk, backward-compat heuristic)
6. Type compatibility pattern (output type -> picker filtering)
7. SourceCard vertical pipeline rendering pattern

**INDEX.md update:**

Update the `ui-application.md` row's Covers column to include `FS-030: transform chain pipeline model` and set `Last Updated` to the completion date.

---

## Verification Requirements

- Architecture document is internally consistent
- Module structure listing matches actual files created by T-01 through T-06
- INDEX.md entry is updated
- No markdown formatting errors

---

## Acceptance Checks

- `ui-application.md` contains a "Transform Chain Model (FS-030)" subsection
- The subsection documents all 7 areas listed above
- `INDEX.md` row for `ui-application.md` includes FS-030 reference and updated date
- Module structure listing includes any new files created

---

## Blockers / Risks

- none

---

## Notes

This is a documentation-only task. Load the latest `ui-application.md` before making changes to avoid overwriting concurrent updates from other specs.
