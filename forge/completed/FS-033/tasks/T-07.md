# TASK

## Title

Update ui-application.md architecture for Test Lab multi-panel layout

---

## ID

T-07

---

## Spec

FS-033

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-03

---

## Implements

- AE-01

---

## Summary

Update the `forge/architecture/ui-application.md` document to reflect the redesigned Advanced Testing Page layout. Replace the current two-panel + tabbed description with the new multi-panel simultaneous layout, resizable main left/right split, responsive breakpoints, ResultPanel component pattern, useTestLabLayout hook contract, ExecutionSummaryBar, and resizable divider behavior.

---

## Scope

### In Scope

- Update the "Advanced Testing Page (FS-021 T-06)" section in `ui-application.md`
- Document the new layout: 2x2 grid (wide), vertical stack (medium), tab fallback (narrow)
- Document `ResultPanel` component and its props contract
- Document `useTestLabLayout` hook contract
- Document `ExecutionSummaryBar` component
- Document responsive breakpoint behavior
- Document localStorage persistence key and shape
- Document resizable divider behavior
- Update the Module Structure section to include new files
- Update `INDEX.md` last-updated date if warranted

### Out of Scope

- Changes to other architecture sections
- Creating a new architecture document (this is an update to an existing one)

---

## Relevant Areas

- `forge/architecture/ui-application.md` (update)
- `forge/architecture/INDEX.md` (update last-updated date)

---

## Change Requirements

1. **Replace the Advanced Testing Page section** with updated content reflecting:

   - New layout description (2x2 grid, vertical stack, tab fallback) with ASCII diagram showing resizable main split
   - Resizable main left/right split (default 35/65, clamped [0.2, 0.5])
   - Breakpoint definitions: wide (>= 1280px), medium (1024-1279px), narrow (< 1024px)
   - `ResultPanel` component role and props
   - `ExecutionSummaryBar` component role and props
   - `useTestLabLayout` hook contract (inputs, outputs, mainSplit/columnSplit/rowSplit, persistence key, clamping rules)
   - Panel visibility rules (which panels are always visible, which are collapsible)
   - Resizable divider behavior and constraints
   - `PreviewProvider` isolation note (unchanged)

2. **Update Module Structure** to include:
   - `ui/src/features/mappings/components/preview/ResultPanel.tsx`
   - `ui/src/features/mappings/components/preview/ExecutionSummaryBar.tsx`
   - `ui/src/features/mappings/hooks/use-test-lab-layout.ts`

3. **Update INDEX.md** last-updated date for `ui-application.md` to current date and expand the coverage description to mention multi-panel Test Lab layout.

---

## Verification Requirements

- Architecture document accurately reflects the implemented layout
- Module structure includes all new files
- INDEX.md last-updated date is current
- No broken references or stale information in updated sections

---

## Acceptance Checks

- Advanced Testing Page section in `ui-application.md` describes the multi-panel layout
- ResultPanel, ExecutionSummaryBar, and useTestLabLayout are documented
- Responsive breakpoint behavior is documented
- localStorage persistence is documented
- Module Structure updated with new files
- INDEX.md updated

---

## Blockers / Risks

- none

---

## Notes

- This task should be executed after T-03 is complete so the architecture document reflects the actual implemented behavior
- Preserve the existing `PreviewProvider` isolation documentation — it's still accurate
