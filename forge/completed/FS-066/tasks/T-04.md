# TASK

## Title

Normalize shared AI error handling to canonical backend envelope

---

## ID

T-04

---

## Spec

FS-066

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01
- T-02

---

## Implements

- `AE-06`
- `AE-08`

---

## Summary

Unify AI failure normalization so provider/runtime/validation/config failures consistently map to canonical backend error envelope semantics and stable error codes.

---

## Scope

### In Scope

- Define/update AI error classification mapping from shared runtime failures.
- Ensure handler-facing error outputs can be translated directly to backend envelope conventions.
- Normalize transient/non-transient characteristics and HTTP-status mapping guidance.
- Add tests for core failure categories and expected normalized outputs.

### Out of Scope

- Non-AI endpoint error model changes.
- UI error copy changes.

---

## Relevant Areas

- `src/lib/ai/types.ts`
- `src/lib/ai/output-parser.ts`
- `src/lib/ai/model-client.ts`
- `src/lib/ai/invoke-ai.ts`
- `src/lambda/ai/*`
- `src/lambda/shared/errors.ts`
- `src/lambda/shared/response.ts`
- `tests/lib/ai/*`
- `tests/lambda/ai/*`

---

## Change Requirements

- Shared layer must produce deterministic normalized error outputs for known failure classes.
- Mapping must align with `backend-api.md` envelope and retryable semantics.
- Handlers should not carry bespoke per-feature provider error mapping logic.

---

## Verification Requirements

- Unit tests for normalization of validation/config/provider/parse/timeout/rate-limit failures.
- Handler-level tests ensuring normalized outputs map to canonical envelope responses.
- Typecheck/lint pass for shared and handler modules touched.

---

## Acceptance Checks

- [x] AI failure categories are normalized in shared layer with stable error-code mapping.
- [x] Handler responses for AI failures align with canonical backend envelope semantics.
- [x] Tests cover normalization matrix across major failure classes.
- [x] `pnpm typecheck` and `pnpm lint` pass for touched areas.

---

## Blockers / Risks

- Existing endpoint-specific mapping behavior may conflict and need coordinated migration.

---

## Notes

- Favor backward-compatible code transitions where existing callers already handle specific codes.
