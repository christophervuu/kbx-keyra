# TASK

## Title

Expand Project Overview test coverage for simplified workspace behavior

---

## ID

T-05

---

## Spec

FS-086

---

## Spec Rev

Rev: 2

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- T-01
- T-02
- T-03
- T-04

---

## Implements

- `AE-01`
- `AE-02`
- `AE-03`
- `AE-04`
- `AE-05`
- `AE-06`
- `AE-07`

---

## Summary

Consolidate and add deterministic automated tests validating the full FS-086 acceptance envelope across layout, linked schemas interaction, row actions/deploy navigation, preserved actions, and breadcrumb behavior.

---

## Scope

### In Scope

- Add/adjust Project Overview and related component tests for all FS-086 acceptance examples.
- Stabilize fixtures/mocks so tests reflect full-width single-column behavior and linked-schema interaction paths.
- Ensure tests cover both typical and edge conditions (e.g., no schemas, has-errors rows).

### Out of Scope

- New end-to-end framework work.
- Refactoring unrelated feature tests.

---

## Relevant Areas

- `ui/src/features/projects/components/__tests__/ProjectOverviewPage.test.tsx`
- `ui/src/features/projects/components/__tests__/MappingListSection.test.tsx`
- `ui/src/components/layout/layout.test.tsx` ?
- any new test files under `ui/src/features/projects/components/__tests__/`

---

## Change Requirements

- Ensure each AE from FS-086 is mapped to at least one deterministic automated assertion.
- Remove/replace stale assertions that assume right-rail/default schema-grid layout.
- Verify deploy action tests assert navigation behavior and absence of inline deployment side effects.
- Verify linked-schema format labels and metadata display assertions.

---

## Verification Requirements

- Run targeted projects/layout test files updated in this task.
- Run `pnpm --filter ui test` (or repo-standard equivalent) to ensure no regressions in touched suite.
- Run UI lint and typecheck checks for touched test and source files.

---

## Acceptance Checks

- Automated tests pass for all `AE-01` through `AE-07` coverage claims.
- No remaining assertions require right-rail/default schema-card grid presence.
- Test suite remains stable and deterministic under local reruns.

---

## Blockers / Risks

- Risk: large existing test file may require significant fixture updates; keep changes readable and scenario-grouped.

---

## Notes

- This task is verification-heavy and may include only minimal production code changes needed to make assertions reliable.
