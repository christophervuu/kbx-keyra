# TASK

## Title

Update ui-application.md architecture document for Array Builder redesign

---

## ID

T-14

---

## Spec

FS-043

---

## Spec Rev

Rev: 1

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01
- T-04
- T-07

---

## Implements

- AE-01 (architecture documentation)

---

## Summary

Update the `forge/architecture/ui-application.md` document to reflect the redesigned Array Builder architecture, replacing the existing `useArrayBuilder` hook contract and `ArrayMappingBuilder` component documentation with the new chain-aligned two-layer builder model, new component hierarchy, new state model, and new hook contracts.

---

## Scope

### In Scope

- Replace the `useArrayBuilder Hook Contract` section with the new `useArrayBuilderState` hook contract
- Replace `ArrayMappingBuilder` references in the component hierarchy with the new `ArrayBuilder` component and its sub-components
- Document the new state model: `ArrayBuilderState`, `CollectionState`, `ItemTemplateState`, `CompletionStatus`
- Document the expression generator and decomposer files
- Document the mode switching model and validation model
- Document the cross-array lookup helper
- Document nested array builder context
- Update the Module Structure section to reflect new/replaced files
- Update INDEX.md `Last Updated` date

### Out of Scope

- Changes to other architecture documents
- Documenting implementation details beyond the architecture level

---

## Relevant Areas

- `forge/architecture/ui-application.md`
- `forge/architecture/INDEX.md`

---

## Change Requirements

- Replace the `### \`useArrayBuilder\` Hook Contract` section (currently documenting the 4-step wizard) with a new section documenting:
  - `useArrayBuilderState(targetPath, schemas, getDraftExpression)` hook
  - Input/output contract
  - State model summary (ArrayBuilderState, CollectionState, ItemTemplateState)
  - Mode switching behavior
  - Draft integration
  - Completion derivation
- Update the component hierarchy under `### Component Hierarchy` to replace:
  ```
  ├── ArrayMappingBuilder    (array node)
  ```
  with the new hierarchy:
  ```
  ├── ArrayBuilder           (array node)
  │   ├── ArrayModeSelector
  │   ├── MapCollectionEditor / FilterMapCollectionEditor / BuildFromValuesEditor / MergeBranchesEditor / CustomExpressionEditor
  │   ├── ItemTemplateEditor
  │   │   ├── ItemFieldRow[] (with inline ChainBuilder)
  │   │   ├── CrossArrayLookupEditor
  │   │   └── Nested ArrayBuilder (recursive)
  │   └── ArrayResultPreview
  ```
- Update the Module Structure under `## Module Structure` to list new files:
  - `array-builder-state.ts`
  - `array-expression-generator.ts` (updated)
  - `array-decomposer.ts`
  - `array-validation.ts`
  - New component files
  - `use-array-builder-state.ts`
- Document the two-layer builder model (collection + item) and how it aligns with the chain builder model
- Document the v1 scope model for arrays: root source, current item, parent item, static
- Update INDEX.md `Last Updated` date for `ui-application.md`

---

## Verification Requirements

- Updated architecture document accurately reflects the new component hierarchy
- New state model is documented at the architecture level
- Hook contract is documented with inputs/outputs
- Module structure lists all new files
- INDEX.md `Last Updated` date is current
- No stale references to old `ArrayMappingBuilder` or `useArrayBuilder` remain

---

## Acceptance Checks

- `useArrayBuilder Hook Contract` section is replaced with `useArrayBuilderState` contract
- Component hierarchy includes all new Array Builder sub-components
- Module structure includes all new files
- Two-layer builder model is documented
- Mode switching model is summarized
- Validation model is summarized
- INDEX.md `Last Updated` date is updated
- No references to old 4-step wizard pattern remain in the architecture document

---

## Blockers / Risks

- This task depends on T-01 (types), T-04 (core component), and T-07 (item template) being designed so the architecture document accurately reflects the implementation. It does not require those tasks to be fully complete.

---

## Notes

- This is an architecture update task. It documents the system as designed/implemented — it does not change application code.
- Follow the existing documentation patterns in `ui-application.md` for hook contracts, component hierarchies, and module structure listings.
- Keep the architecture documentation at the right level of abstraction — document the contracts, boundaries, and model; do not duplicate implementation details that belong in the code.
