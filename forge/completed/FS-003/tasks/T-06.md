# TASK

## Title

Update mapping-engine.md architecture document

---

## ID

T-06

---

## Spec

FS-003

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-03

---

## Implements

- AE-01 (indirectly — documents the system that implements all AEs)

---

## Summary

Update `forge/architecture/mapping-engine.md` to document the evaluator's role in the execution pipeline, the `EvaluationContext` structure, the scope stack mechanism, the null propagation dispatch model, and the new files added to the `dsl/` module.

---

## Scope

### In Scope

- Add an "Expression Evaluator" section to `mapping-engine.md`
- Document `evaluate()` API contract (input/output types)
- Document `EvaluationContext` structure and its role, including the `evaluate` callback for re-entrant calls
- Document the scope stack mechanism (push/pop/read pattern)
- Document ExecutionContext construction — how evaluator populates `currentItem`/`parentItem` from scope stack before dispatch
- Document the null propagation dispatch model (centralized, `handlesNull` bypass, E005 halt-on-mismatch)
- Document trace collection with configurable verbosity (`TraceVerbosity`)
- Document `resolvePath()` utility purpose and location
- Update the module structure diagram to include `evaluator.ts` and `resolve-path.ts` in `dsl/`
- Update the internal module boundaries table if needed
- Update the execution pipeline diagram to show evaluate as a sub-step within Execute
- Update `Last Updated` in `forge/architecture/INDEX.md`

### Out of Scope

- Documenting specific DSL function behaviors
- Documenting the full execute() pipeline (FS-007)
- Creating a new architecture document (the evaluator is part of the existing mapping engine subsystem)
- Modifying any source code

---

## Relevant Areas

- `forge/architecture/mapping-engine.md` — primary document to update
- `forge/architecture/INDEX.md` — update Last Updated date
- `src/engine/dsl/evaluator.ts` — source of truth for implementation
- `src/engine/dsl/resolve-path.ts` — source of truth for path resolution

---

## Change Requirements

After this task:

- `mapping-engine.md` has a new section (after "DSL Parser") documenting:
  - **Expression Evaluator** heading
  - `evaluate(node, context)` contract — what it accepts, what it returns
  - `EvaluationContext` fields and their purposes, including `evaluate` callback for re-entrant calls from array functions
  - ExecutionContext construction: evaluator populates `currentItem`/`parentItem` from scope stack before dispatch — implementations receive pre-resolved values
  - Scope stack: structure, push/pop lifecycle (owned by array functions), read pattern via ExecutionContext
  - Null propagation: default behavior, exception list, `handlesNull` mechanism
  - Type mismatch: E005 halts the function call (same pattern as E002/E003)
  - Trace collection: configurable verbosity (`"functions"` default, `"all"` for literals)
  - Recursion depth tracking
  - Diagnostic collection during evaluation
  - `resolvePath()` utility: purpose, supported path syntax
- Module structure diagram updated to show:
  - `dsl/evaluator.ts` — recursive expression evaluator
  - `dsl/resolve-path.ts` — dot/bracket path resolution utility
- Internal module boundaries table updated if the evaluator introduces new import patterns
- `INDEX.md` `Last Updated` date for mapping-engine.md reflects today's date

---

## Verification Requirements

- `mapping-engine.md` accurately reflects the implemented evaluator behavior
- Module structure diagram matches actual file layout
- No conflicts with existing content
- `INDEX.md` date updated

---

## Acceptance Checks

- `mapping-engine.md` contains "Expression Evaluator" section
- EvaluationContext structure documented
- Scope stack mechanism documented
- Null propagation dispatch model documented
- Module structure diagram includes evaluator.ts and resolve-path.ts
- `INDEX.md` Last Updated column reflects current date for mapping-engine.md
- Document is internally consistent with existing content

---

## Blockers / Risks

- none

---

## Notes

- This task can begin after T-03 is done (to document actual implementation rather than speculative design). However, it can be started earlier in draft form if needed.
- Keep the documentation concise and reference the DSL specification for full null propagation rules rather than duplicating the complete exception list.
- The evaluator section should be placed between the "DSL Parser" section and the "Type System Summary" section in the document, maintaining logical pipeline order.
