# TASK

## Title

Update ui-application.md architecture for Auto-Map review drawer

---

## ID

T-08

---

## Spec

FS-046

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01
- T-02
- T-03
- T-04
- T-05
- T-06
- T-07

---

## Implements

- Architecture documentation coverage for the Auto-Map review drawer pattern

---

## Summary

Update `forge/architecture/ui-application.md` and `forge/architecture/project-structure.md` to document the Auto-Map review drawer architecture: types, hook contract, component hierarchy, integration pattern, and how it extends the existing showcase AI integration pattern to multi-suggestion, section-level review.

---

## Scope

### In Scope

1. **`ui-application.md` updates:**
   - Add `AutoMapReviewDrawer` and `SuggestionReviewCard` to the Mapping Editor Component Hierarchy section
   - Add `useAutoMapReview` hook to the hooks listing with contract summary
   - Update the Showcase AI Integration Pattern section to include the Auto-Map review drawer as a third slice
   - Document the drawer composition pattern (route-page level, mutual exclusion with VersionHistoryDrawer)
   - Document the suggestion review state model
   - Update Module Structure to include new files

2. **`project-structure.md` updates:**
   - Add new files to the `ui/src/features/mappings/` listing:
     - `components/AutoMapReviewDrawer.tsx`
     - `components/SuggestionReviewCard.tsx`
     - `hooks/use-auto-map-review.ts`

3. **`INDEX.md` updates:**
   - Update the `Last Updated` date and coverage text for `ui-application.md`

### Out of Scope

- Creating a new architecture document (this is an update to existing docs)
- Backend architecture changes (ai-runtime.md is not modified — no new Lambda in this spec)

---

## Relevant Areas

- `forge/architecture/ui-application.md`
- `forge/architecture/project-structure.md`
- `forge/architecture/INDEX.md`

---

## Change Requirements

### ui-application.md — Module Structure

Add to the `features/mappings/` section:

```
components/
  AutoMapReviewDrawer.tsx    FS-046 right-side review drawer for Auto-Map suggestions: summary header, bulk actions bar, loading/empty/error/done states
  SuggestionReviewCard.tsx   FS-046 individual suggestion card: target path, new/replace badge, expression comparison, confidence/validation badges, Accept/Edit/Dismiss actions
hooks/
  use-auto-map-review.ts     FS-046 Auto-Map review lifecycle hook: trigger/receive/track suggestions, per-suggestion actions, bulk accept, drawer state
```

### ui-application.md — Showcase AI Integration Pattern

Add Auto-Map Review as the third slice:

```
Current slices:
- Explain Rule (FS-041): explainRuleHttp → HybridAdapter.explainRule → useExplainRule → ExplanationPanel
- Suggest Expression (FS-042): suggestExpressionHttp → HybridAdapter.suggestExpression → useSuggestExpression → SuggestExpressionInline
- Auto-Map Review (FS-046): autoMapSectionHttp → HybridAdapter.autoMapSection → useAutoMapReview → AutoMapReviewDrawer + SuggestionReviewCard
```

Note the difference from previous slices: Auto-Map is a multi-suggestion, section-level review flow that uses a drawer (not inline UI), and manages per-suggestion state (pending/accepted/edited/dismissed).

### ui-application.md — Auto-Map Review Drawer section

Add a new subsection under Mapping Editor Architecture documenting:

- Drawer composition point (route page level, alongside VersionHistoryDrawer)
- Drawer mutual exclusion with other overlay drawers
- `useAutoMapReview` hook contract (inputs, outputs, key behaviors)
- Suggestion review state model (`SuggestionReviewItem`, `SuggestionReviewStatus`)
- Integration with `useMappingEditor` draft model (accept → `updateDraft`, edit → `updateDraft` + navigate)
- Trigger placement (ObjectSummaryPanel)
- Showcase vs RAG-ready design decisions

### ui-application.md — Component Hierarchy

Add to the Mapping Editor component hierarchy:

```
MappingEditorPage
├── ... (existing)
└── (route page level, outside grid)
    ├── VersionHistoryDrawer
    ├── UnsavedChangesOverlay
    └── AutoMapReviewDrawer         (FS-046)
        ├── Summary header
        ├── Bulk actions bar
        └── SuggestionReviewCard[]  (FS-046)
```

### project-structure.md

Add new files under `ui/src/features/mappings/`:

```
components/
  AutoMapReviewDrawer.tsx          FS-046 Auto-Map review drawer (summary, bulk actions, states)
  AutoMapReviewDrawer.test.tsx     FS-046 component tests
  SuggestionReviewCard.tsx         FS-046 suggestion review card (comparison, badges, actions)
  SuggestionReviewCard.test.tsx    FS-046 component tests
hooks/
  use-auto-map-review.ts           FS-046 Auto-Map review lifecycle hook
```

### INDEX.md

Update the `ui-application.md` entry:
- Set `Last Updated` to the date this task is completed
- Expand `Covers` text to mention FS-046 Auto-Map review drawer

---

## Verification Requirements

- `ui-application.md` updated with all new sections
- `project-structure.md` lists all new files
- `INDEX.md` updated with new date and coverage text
- No broken internal references in architecture docs
- Changes are factually accurate relative to implemented code

---

## Acceptance Checks

- `ui-application.md` documents Auto-Map review drawer pattern, hook contract, and component hierarchy
- `project-structure.md` lists `AutoMapReviewDrawer.tsx`, `SuggestionReviewCard.tsx`, `use-auto-map-review.ts`
- `INDEX.md` entry for `ui-application.md` has updated date and coverage text
- Architecture documentation is consistent with implemented code

---

## Blockers / Risks

- This task depends on all other tasks being complete so the architecture doc reflects the actual implemented state. If tasks are completed incrementally, this task should be executed last.

---

## Notes

- Follow the existing architecture doc style — concise, contract-focused, with code snippets where they clarify contracts.
- The architecture update should emphasize the design decision to use the `VersionHistoryDrawer` pattern (right-side fixed drawer) rather than a modal or inline panel, and why this is the preferred approach for multi-suggestion review.
