# TASK

## Title

Update architecture documents for functions directory and implementation patterns

---

## ID

T-11

---

## Spec

FS-004

---

## Spec Rev

Rev: 1

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01
- T-10

---

## Implements

- (architecture task — does not directly implement acceptance examples)

---

## Summary

Update `forge/architecture/mapping-engine.md` to document the function implementation pattern, the file organization under `src/engine/functions/`, how `addDiagnostic` works, and how the `handlesNull` flag interacts with the evaluator's null propagation logic. Update `forge/architecture/project-structure.md` to reflect the new `functions/` directory. Update `forge/architecture/INDEX.md` timestamps.

---

## Scope

### In Scope

- Update `forge/architecture/mapping-engine.md`:
  - Add "Function Implementations" section documenting:
    - File organization under `src/engine/functions/`
    - Implementation pattern (signature + implementation + registration)
    - `addDiagnostic` callback mechanism
    - `handlesNull` interaction with evaluator null propagation
    - Category breakdown with function counts
  - Update module structure diagram to include `functions/` directory
  - Update internal module boundaries table to include `functions/` module
- Update `forge/architecture/project-structure.md`:
  - Add `functions/` directory to `src/engine/` tree
  - Add `tests/engine/functions/` to test tree
- Update `forge/architecture/INDEX.md`:
  - Update `Last Updated` date for both documents

### Out of Scope

- Creating new architecture documents (existing documents cover this area)
- Changing any source code

---

## Relevant Areas

- `forge/architecture/mapping-engine.md`
- `forge/architecture/project-structure.md`
- `forge/architecture/INDEX.md`

---

## Change Requirements

**mapping-engine.md additions:**

1. Add to module structure:
```
  functions/
    index.ts            Barrel — registerAllFunctions() populates a registry with all built-in functions
    source-access.ts    source, item, parent, constant, external, static
    type-conversion.ts  cast
    null-handling.ts    default, coalesce, isNull
    conditional.ts      if, eq, neq, gt, gte, lt, lte, and, or, not
    lookup.ts           valueMap
    string.ts           concat, substring, upper, lower, trim, replace, replaceAll, contains, length
    date.ts             formatDate
    math.ts             add, subtract, multiply, divide, round, abs
```

2. Add to internal module boundaries table:
```
| `functions/` | Built-in function implementations and registration | `types/`, `diagnostics/`, `registry/`, `dsl/` (for EvaluationContext type and resolvePath) |
```

3. Add new section "Function Implementation Pattern" after the Expression Evaluator section:
- Describe the `addDiagnostic` callback on `EvaluationContext`
- Document the implementation contract: receives pre-validated args, returns value, emits diagnostics via context
- Document `handlesNull` interaction: evaluator skips null propagation for handlesNull functions, implementations receive raw args including null
- Document registration pattern: `registerAllFunctions(registry)` populates all built-in functions

**project-structure.md additions:**

Add under `src/engine/`:
```
    functions/          Built-in DSL function implementations (one file per category)
```

Add under `tests/engine/`:
```
    functions/          Unit tests for function implementations
```

---

## Verification Requirements

- Architecture documents are factually accurate after updates
- Module boundary table includes `functions/`
- Module structure includes all function category files
- INDEX.md dates updated
- No formatting or structural issues in markdown

---

## Acceptance Checks

- `forge/architecture/mapping-engine.md` documents the function implementation pattern
- `forge/architecture/mapping-engine.md` module structure includes `functions/` directory
- `forge/architecture/mapping-engine.md` module boundaries includes `functions/` row
- `forge/architecture/project-structure.md` includes `functions/` in `src/engine/` tree
- `forge/architecture/project-structure.md` includes `tests/engine/functions/` in test tree
- `forge/architecture/INDEX.md` has updated dates
- All documents remain valid markdown

---

## Blockers / Risks

- none

---

## Notes

- This task should be done after T-01 and T-10 to ensure the documentation accurately reflects the implemented structure.
- Keep documentation concise — focus on patterns and boundaries, not implementation details of individual functions.
- The `addDiagnostic` mechanism is the most important architectural addition to document, as it establishes the contract between the evaluator and function implementations.
