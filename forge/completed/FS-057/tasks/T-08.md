# TASK

## Title

Create backend-api architecture document and update INDEX

---

## ID

T-08

---

## Spec

FS-057

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- none

---

## Implements

- (architecture documentation — supports all AEs indirectly)

---

## Summary

Create `forge/architecture/backend-api.md` documenting the Phase 1 backend API architecture: Lambda handler patterns, DynamoDB access patterns, S3 content storage conventions, error envelope standard, shared utility structure, and API Gateway route configuration. Update `forge/architecture/INDEX.md` with the new document entry. Update `forge/architecture/project-structure.md` with the new `src/lambda/` subdirectories.

---

## Scope

### In Scope

- `forge/architecture/backend-api.md` — new architecture document covering:
  - API Gateway route structure and CORS configuration
  - Lambda handler conventions (entry point signature, event/response types, shared utilities)
  - DynamoDB table schemas, key designs, and GSIs (as implemented)
  - S3 content storage key patterns
  - Error envelope specification
  - Handler-to-table mapping
  - Environment variable conventions
  - Testing patterns (DynamoDB Local + mocked S3)
- `forge/architecture/INDEX.md` — add row for `backend-api.md`
- `forge/architecture/project-structure.md` — add `src/lambda/shared/`, `src/lambda/project/`, `src/lambda/mapping/`, `src/lambda/schema/` to the project structure tree and rules

### Out of Scope

- Infrastructure-as-code documentation
- Deployment pipeline documentation
- Auth/authz architecture (future)
- AI runtime changes (covered by `ai-runtime.md`)

---

## Relevant Areas

- `forge/architecture/backend-api.md` — new file
- `forge/architecture/INDEX.md` — update existing
- `forge/architecture/project-structure.md` — update existing
- `src/lambda/` — reference for documenting actual structure

---

## Change Requirements

- `backend-api.md` must document:
  1. Purpose and scope (Phase 1 CRUD API surface)
  2. Route table (all 19 routes with method, path, handler file, Lambda name)
  3. Handler conventions:
     - Export pattern: `export async function handler(event): Promise<Result>`
     - Import from `../shared/` for utilities
     - No direct imports from `ui/`
     - Single responsibility per handler
  4. Error envelope format with all standard codes
  5. DynamoDB tables: key schemas, GSIs, access patterns per handler
  6. S3 storage: key patterns, content types
  7. Environment variables: table names, bucket names, region
  8. Testing approach: unit (mocked clients) + integration (DynamoDB Local)
  9. Cross-references to related docs (`phase-1-readiness.md`, `project-structure.md`, `ai-runtime.md`)
- `INDEX.md`: add row with document name, coverage description, and date
- `project-structure.md`: update the `src/lambda/` section to show new directories and remove `[planned — not yet implemented]` annotations for schema/mapping/project

---

## Verification Requirements

- `backend-api.md` is well-structured and complete per the change requirements
- `INDEX.md` has correct row added with current date
- `project-structure.md` accurately reflects the new directory structure
- All file paths referenced in the architecture doc exist (or will exist after T-01–T-07)
- No broken cross-references

---

## Acceptance Checks

- `forge/architecture/backend-api.md` created with all required sections
- `forge/architecture/INDEX.md` updated with new row
- `forge/architecture/project-structure.md` updated with new Lambda directories
- Document is consistent with the actual handler implementations from T-01–T-06
- All cross-references are valid

---

## Blockers / Risks

- none

---

## Notes

- This task can proceed in parallel with implementation tasks (T-01–T-06) since it documents the intended architecture. It should be finalized after implementation is complete to ensure accuracy.
- The architecture document should be concise and actionable — it's a reference for future task agents, not a tutorial. Focus on conventions, constraints, and decisions that would otherwise need to be re-derived.
- Keep the error envelope section in sync with the spec's Proposed Behavior section — they should be identical.
