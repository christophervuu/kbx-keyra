# TASK

## Title

Update architecture documents and project structure for AI runtime

---

## ID

T-08

---

## Spec

FS-031

---

## Spec Rev

Rev: 1

---

## Agent

task

---

## Status

done

---

## Depends On

- T-06

---

## Implements

- (Architecture alignment — does not directly implement acceptance examples)

---

## Summary

Update `forge/architecture/project-structure.md` to reflect all new files and directories created by T-01 through T-07. Review `forge/architecture/ai-runtime.md` for alignment with the actual implementation and update if any design decisions changed during implementation. Update `forge/architecture/INDEX.md` to include the new `ai-runtime.md` entry with the correct last-updated date.

---

## Scope

### In Scope

- Update `forge/architecture/project-structure.md`:
  - Add `src/lib/` directory and `src/lib/ai/` subdirectory with all files
  - Add `src/lambda/ai/` directory with the explain-rule handler
  - Add `tests/lib/ai/` directory with test files
  - Add `tests/lambda/ai/` directory with test files
  - Update any rules or notes about the `src/lib/` directory
- Review `forge/architecture/ai-runtime.md`:
  - Verify module structure matches actual implementation
  - Verify type names and interfaces match actual code
  - Verify adapter pattern description matches actual implementation
  - Update any sections that diverged during implementation
- Update `forge/architecture/INDEX.md`:
  - Add row for `ai-runtime.md` with coverage description and last-updated date
  - Verify existing rows are still accurate

### Out of Scope

- Code changes to `src/` or `tests/`
- Creating new architecture documents beyond the ones in this spec

---

## Relevant Areas

- `forge/architecture/project-structure.md` (updated)
- `forge/architecture/ai-runtime.md` (reviewed and potentially updated)
- `forge/architecture/INDEX.md` (updated)

---

## Change Requirements

### project-structure.md

Add to the `src/` section:

```
src/
  lib/                Shared utilities used across lambdas
    ai/               Shared AI runtime (prompt loading, model invocation, orchestration)
      index.ts        Barrel export — public API surface
      types.ts        Shared types and adapter interfaces
      config.ts       Runtime configuration from environment variables
      prompt-registry.ts  Prompt Registry adapter (DynamoDB + local)
      dsl-asset-loader.ts DSL asset loader (S3 + local)
      prompt-renderer.ts  Template placeholder rendering
      model-client.ts     GitHub Models client wrapper (OpenAI SDK)
      output-parser.ts    Structured output parsing
      invoke-ai.ts        Orchestration entry point: invokeAI()
```

Add to the `tests/` section:

```
tests/
  lib/
    ai/               AI runtime unit and integration tests
  lambda/
    ai/               AI Lambda handler tests
```

Update the rules section if needed to document that `src/lib/ai/` has zero dependency on `src/engine/` and `ui/`.

### INDEX.md

Add a row:

| Document | Covers | Last Updated |
|---|---|---|
| [ai-runtime.md](./ai-runtime.md) | Shared AI runtime — prompt loading, DSL asset resolution, prompt rendering, GitHub Models invocation, structured output parsing, orchestration entry point, adapter pattern for local testing, Lambda handler conventions. | 2026-05-09 |

### ai-runtime.md Review

Compare the architecture document with the actual implementation. Typical areas that may need updates:
- Module file list (did the implementation add or remove files?)
- Type names and interfaces (did naming change during implementation?)
- Adapter pattern details (did the factory function signature change?)
- Configuration environment variables (did any defaults change?)
- Orchestration flow (did the pipeline steps change order or add steps?)

---

## Verification Requirements

- `forge/architecture/project-structure.md` accurately reflects all files created by T-01 through T-07
- `forge/architecture/ai-runtime.md` accurately reflects the implemented architecture
- `forge/architecture/INDEX.md` includes `ai-runtime.md` with correct coverage description
- No inconsistencies between architecture documents and actual code

---

## Acceptance Checks

- `forge/architecture/project-structure.md` lists all new `src/lib/ai/`, `src/lambda/ai/`, `tests/lib/ai/`, and `tests/lambda/ai/` files
- `forge/architecture/ai-runtime.md` is consistent with the implemented code
- `forge/architecture/INDEX.md` has a row for `ai-runtime.md`
- `INDEX.md` `Last Updated` date for `ai-runtime.md` is set to the implementation date

---

## Blockers / Risks

- This task should be executed after T-06 (or after T-07 if T-07 is done) so that the final file list and implementation details are known. If executed too early, a second pass would be needed.

---

## Notes

- The initial `ai-runtime.md` was created by the spec agent as part of the planning package. This task reviews and refines it based on actual implementation.
- When updating `project-structure.md`, follow the existing formatting conventions (indentation, annotation style, rules sections).
- The `INDEX.md` update should set the `Last Updated` date to the date this task is executed, not the spec creation date.
