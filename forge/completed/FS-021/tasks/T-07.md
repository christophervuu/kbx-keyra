# TASK

## Title

Update UI architecture document for FS-021 Mapping Editor redesign

---

## ID

T-07

---

## Spec

FS-021

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01
- T-02
- T-03
- T-04
- T-05
- T-06

---

## Implements

- (architecture maintenance — no specific AE)

---

## Summary

Update `forge/architecture/ui-application.md` to reflect the FS-021 Mapping Editor redesign: new 2-row top bar, two-tier save model, per-panel search, rebalanced panel widths, inline preview strip replacing the 4-tab bottom area, and the new Advanced Testing page route. Also update `forge/architecture/INDEX.md` last-updated date and `forge/architecture/project-structure.md` with new file entries.

---

## Scope

### In Scope

- Update the "Mapping Editor Architecture" section in `ui-application.md`:
  - Replace the 3-row top area description with 2-row (NavBar + context bar).
  - Document the two-tier save model (Apply + Save) semantics and hook contract changes.
  - Update the layout diagram to show new panel proportions (15/35/50).
  - Replace BottomArea description with InlinePreviewStrip.
  - Add "Advanced Testing Page" subsection documenting the new route, page composition, and hook reuse.
  - Update `EditorTopBar` contract to reflect new props.
  - Document per-panel search placement.
  - Document navigation guard behavior.
- Update Routing section to add the `/projects/:projectId/mappings/:mappingId/test` route.
- Update `INDEX.md` last-updated date for `ui-application.md`.
- Update `project-structure.md` with new files (if not already done in T-06).

### Out of Scope

- Changes to other architecture documents (mapping-engine.md, e2e-testing.md).
- Implementation code changes (this is documentation only).

---

## Relevant Areas

- `forge/architecture/ui-application.md`
- `forge/architecture/INDEX.md`
- `forge/architecture/project-structure.md`

---

## Change Requirements

- **`ui-application.md` updates:**
  - Section "Mapping Editor Architecture": update layout diagram ASCII art to reflect 2-row top + 15/35/50 columns + inline preview strip.
  - Replace `BottomArea` entry in component hierarchy with `InlinePreviewStrip`.
  - Add `AdvancedTestingPage` to module structure listing.
  - Document `EditorTopBar` new contract props (projectName, mappingName, deployStatus, saveState, unsavedCount, onSave).
  - Add subsection: "Two-Tier Save Model" describing Apply vs Save semantics, `unsavedRuleCount`, navigation guards, focus-advance behavior (normal Apply → next unmapped; "Apply & Continue" → clicked field).
  - Add subsection: "Inline Preview Strip" describing auto-preview toggle, auto-run-on-apply behavior, flash animation.
  - Add subsection: "Advanced Testing Page" describing route, independent load from localStorage, own PreviewProvider, component reuse, hook wiring.
  - Update "Top Bar Contract" subsection with new props, save state display, and stale badge logic (strictly version-based, no content diff).
  - Document per-panel search placement (source panel uses `useTreeSearch` hook, no filter chips; target panel has search + 4 filter chips with AND logic).
  - Remove or update references to the GlobalToolbar search field.

- **Routing section update:**
  - Add: `/projects/:projectId/mappings/:mappingId/test` → Advanced Testing

- **`INDEX.md`:**
  - Update `ui-application.md` Last Updated to implementation date.

- **`project-structure.md`:**
  - Add new file entries under `ui/src/routes/pages/` and `ui/src/features/mappings/components/` if not already added by T-06.

---

## Verification Requirements

- All updated markdown renders correctly (no broken formatting).
- Architecture descriptions match the implemented behavior from T-01 through T-06.
- INDEX.md date is current.
- No contradictions between architecture doc and implementation.

---

## Acceptance Checks

- `ui-application.md` Mapping Editor section reflects the redesigned layout
- Two-tier save model is documented
- Advanced Testing page route and composition are documented
- Panel width proportions (15/35/50) are reflected in the layout diagram
- InlinePreviewStrip replaces BottomArea in the editor architecture
- Routing section includes the new test route
- `INDEX.md` last-updated date is current
- `project-structure.md` includes new file entries

---

## Blockers / Risks

- Must wait for all implementation tasks to complete to ensure documentation accuracy.

---

## Notes

- This task should be executed last in the sequence. The architecture document should describe the system as-built after T-01–T-06 are done.
- If any implementation deviated from the spec during execution, capture the actual behavior (not the originally planned behavior).
