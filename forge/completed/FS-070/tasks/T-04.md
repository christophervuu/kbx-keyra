# TASK

## Title

Implement suggestion review UX for accept, edit, and dismiss with validation gating

---

## ID

T-04

---

## Spec

FS-070

---

## Spec Rev

Rev: 2

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- T-03

---

## Implements

- `AE-01`
- `AE-03`
- `AE-04`
- `AE-05`

---

## Summary

Enhance suggest-expression UI surfaces to provide explicit review interactions (accept/edit/dismiss) while enforcing validation gating and preserving suggestion-only semantics.

---

## Scope

### In Scope

- Update `SuggestExpressionInline` and related builder integration points for validation-aware rendering.
- Implement canonical inline editable suggestion area for edit-before-apply interaction.
- Enforce strict apply block until suggestion expression validates (no apply-anyway path).
- Ensure dismiss keeps current field expression unchanged.
- Add/update component tests for accept/edit/dismiss and invalid-suggestion UI states.

### Out of Scope

- Backend logic or adapter changes.
- New AI feature buttons/workflows outside suggest-expression.
- Full Mapping Editor layout redesign.

---

## Relevant Areas

- `ui/src/features/mappings/components/SuggestExpressionInline.tsx`
- `ui/src/features/mappings/components/SuggestExpressionInline.test.tsx`
- `ui/src/features/mappings/components/ScalarFieldBuilder.tsx`
- `ui/src/features/mappings/components/ScalarFieldBuilder.test.tsx ?`

---

## Change Requirements

- Suggestion UI clearly shows generated DSL and validation state.
- Accept action applies expression only when current suggestion/edit value is validation-valid.
- Edit flow enables user modification prior to apply and clearly reflects edited value.
- Dismiss always closes suggestion without mutating expression.
- AI suggestion remains clearly presented as assistance/review artifact.

---

## Verification Requirements

- Component tests for valid suggestion accept path.
- Component tests for invalid suggestion path (accept gated, diagnostics visible).
- Component tests for edit-before-apply behavior.
- Component tests for dismiss behavior and no-change guarantee.
- Typecheck for touched UI components.

---

## Acceptance Checks

- UI supports explicit accept/edit/dismiss interactions for suggestions.
- Validation-invalid suggestion cannot be silently one-click accepted.
- Dismiss/failure paths preserve existing draft expression.
- Suggestion surface remains clearly non-persistent assistance.

---

## Blockers / Risks

- none

---

## Notes

- Keep interaction affordances consistent with existing AI inline surfaces where practical.
- T-04 implementation includes: inline editable suggestion review area, explicit suggestion-only assistance label, strict Accept gating on invalid reviewed expressions, and canonical suggest request payload wiring (`mappingId`, no `sourceContext`).
- Cleanup pass resolved prior verification blockers:
  - `ScalarFieldBuilder.tsx` lint issues in touched scope addressed (including hook/ref rule compliance updates + import ordering).
  - Vitest matcher setup normalized via `ui/vitest-setup.ts` (`expect.extend(@testing-library/jest-dom/matchers)`), restoring DOM matcher availability for targeted suites.
