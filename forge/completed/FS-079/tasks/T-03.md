# TASK

## Title

Add deployment UI schema-specific block messaging and remediation CTA.

---

## ID

T-03

---

## Spec

FS-079

---

## Spec Rev

Rev: 2

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- T-01

---

## Implements

- AE-02
- AE-03

---

## Summary

Update deployment UI error handling to render backend-provided CDM deploy-block reasons per schema and provide clear remediation CTA(s), with consistent behavior across deploy and promote attempts.

---

## Scope

### In Scope

- Parse and render schema-level blocking details from deployment API error response.
- Show exact reason per schema and clear remediation CTA text/action.
- Ensure messaging appears for both deploy and promote flows on deployment surface.
- Ensure successful retry path clears prior block messaging state.
- Add UI tests for render/state transitions.

### Out of Scope

- Backend validation logic implementation.
- CDM re-sync backend operation internals.
- Broader deployment page redesign.

---

## Relevant Areas

- `ui/src/features/projects/components/ProjectDeploymentsPage.tsx` ?
- `ui/src/features/mappings/components/*deployment*` ?
- `ui/src/lib/api/http-adapter.ts`
- `ui/src/lib/state/app-error.ts`
- `ui/src/lib/api/types.ts`
- `ui/src/**/__tests__/*.test.tsx`

---

## Change Requirements

- Extend deployment error parsing to support CDM deploy-block payload shape including reference role (`source`/`target`) per schema.
- Render block list with schema identifier and exact reason label.
- Render remediation CTA per reason (e.g., re-sync schema then retry deploy).
- Preserve existing generic error behavior for non-CDM deployment failures.
- Keep copy concise and non-technical.

---

## Verification Requirements

- Add component tests for:
  - schema-specific reason rendering from backend payload (AE-02)
  - remediation CTA visibility for each reason class (AE-02)
  - blocked → fixed → retry success state clearing block UI (AE-03)
- Run typecheck/lint for touched UI areas.

---

## Acceptance Checks

- Deployment UI displays schema-specific block reason(s) from backend response.
- Deployment UI displays remediation CTA(s) tied to reason taxonomy.
- Block messaging is shown consistently for deploy and promote actions.
- Block messaging clears after successful retry.
- Non-CDM deployment errors retain existing UI treatment.
- `forge/architecture/project-structure.md` updated if new files/folders are created.

---

## Blockers / Risks

- Final backend error payload shape may differ and require adapter normalization layer updates.

---

## Notes

- Prefer centralized mapping of reason code → user-facing copy/CTA to avoid duplication.
