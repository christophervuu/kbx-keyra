# TASK

## Title

Add end-to-end runtime tests for prompt selection, rollback, and invalid output handling

---

## ID

T-06

---

## Spec

FS-067

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-02
- T-03
- T-04
- T-05

---

## Implements

- `AE-01`
- `AE-03`
- `AE-04`
- `AE-05`
- `AE-06`
- `AE-07`

---

## Summary

Build integration-style AI runtime/handler tests that verify the full shared infrastructure path: prompt retrieval, version/rollback selection, structured output validation, and normalized failures.

---

## Scope

### In Scope

- Add runtime integration tests with mocked adapters/clients for deterministic prompt/version/output scenarios.
- Add handler integration/contract tests covering success and normalized failures.
- Verify cache refresh correctness when selection state changes.
- Verify environment-scoped active pointer rollback selection behavior and `INVALID_MODEL_OUTPUT` HTTP 500 mapping.

### Out of Scope

- Performance/load benchmarking beyond current test harness.
- UI integration tests.

---

## Relevant Areas

- `tests/lib/ai/*`
- `tests/lambda/ai/*`
- `src/lib/ai/*` (test seams only as required)

---

## Change Requirements

- Tests must be deterministic and directly traceable to AE IDs.
- Coverage must include rollback-selection and invalid-output failure behavior.
- Test harness must avoid dependence on live AWS/provider calls.

---

## Verification Requirements

- Run targeted AI runtime and lambda test suites.
- Confirm touched suites pass under CI-equivalent commands.

---

## Acceptance Checks

- [x] Integration/contract tests cover prompt latest selection, rollback override, and cache refresh behavior.
- [x] Integration/contract tests cover schema-valid success and invalid-output failure behavior.
- [x] Test scenarios are traceable to AE IDs in naming or comments.
- [x] Targeted test suites plus `pnpm typecheck` and `pnpm lint` pass.

---

## Blockers / Risks

- Existing mock fixtures may require refactoring to support new selection metadata fields.

---

## Notes

- Prefer adapter injection patterns already used by `invokeAI()` to keep tests stable.
