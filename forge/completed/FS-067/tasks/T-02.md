# TASK

## Title

Implement `getLatestPrompt(promptId)` with deterministic version selection

---

## ID

T-02

---

## Spec

FS-067

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01

---

## Implements

- `AE-01`
- `AE-07`

---

## Summary

Implement shared prompt retrieval behavior for `getLatestPrompt(promptId)` with deterministic active/latest version selection and cache correctness guarantees.

---

## Scope

### In Scope

- Implement/adjust prompt registry adapter logic for deterministic latest/active selection by promptId.
- Enforce cache TTL behavior and refresh semantics on expiry.
- Ensure selection metadata (promptId/version) is available to invocation path for diagnostics.

### Out of Scope

- Rollback override mechanics beyond latest-selection baseline.
- Handler response mapping changes.

---

## Relevant Areas

- `src/lib/ai/prompt-registry.ts`
- `src/lib/ai/config.ts`
- `src/lib/ai/invoke-ai.ts`
- `tests/lib/ai/*`

---

## Change Requirements

- Selection logic must be deterministic for same prompt state.
- Cache must not serve stale versions past TTL boundaries.
- Retrieval must fail predictably when no prompt record can be selected.

---

## Verification Requirements

- Unit tests for latest selection and deterministic behavior.
- Unit tests for cache hit/miss/expiry behavior.
- Typecheck/lint for touched modules.

---

## Acceptance Checks

- [ ] `getLatestPrompt(promptId)` returns deterministic active/latest prompt selection.
- [ ] Cache semantics (hit, expiry refresh) are tested and pass.
- [ ] Retrieval failures for missing prompt state are deterministic and covered by tests.
- [ ] `pnpm typecheck` and `pnpm lint` pass for touched areas.

---

## Blockers / Risks

- DynamoDB sort/record assumptions must align with real stored data patterns.

---

## Notes

- Keep retrieval interface stable for existing AI handler/runtime consumers.
