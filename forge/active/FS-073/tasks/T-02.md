# TASK

## Title

Add chunked generation strategy for small-to-large Auto-Map scopes

---

## ID

T-02

---

## Spec

FS-073

---

## Spec Rev

Rev: 1

---

## Agent

task

---

## Status

todo

---

## Depends On

- T-01

---

## Implements

- `AE-02`

---

## Summary

Implement section/chunk generation orchestration so Auto-Map can process broad and very large scopes safely, invoking shared AI runtime per chunk and aggregating chunk outcomes into one response envelope.

---

## Scope

### In Scope

- Chunk planning strategy and limits for Auto-Map generation.
- Per-chunk invokeAI orchestration and bounded concurrency.
- Aggregation of chunk outputs and partial-failure handling semantics.
- Tests for large-scope behavior and chunk execution correctness.

### Out of Scope

- Final dedup precedence logic details beyond aggregation handoff.
- UI workspace changes.
- Step Functions architecture migration implementation (if deferred).

---

## Relevant Areas

- `src/lambda/ai/auto-map.ts`
- `src/lib/ai/invoke-ai.ts`
- `tests/lambda/ai/auto-map.test.ts`
- `tests/integration/schema-ingestion/* ?`

---

## Change Requirements

- Auto-Map supports adaptive execution strategy (single section for small scope, chunked for broad/large scope).
- Runtime limits (chunk size, concurrency) are enforced to avoid timeout/token blowups.
- Aggregated response preserves chunk-level warnings/metadata where appropriate.
- No direct full-schema single-prompt fallback path.

---

## Verification Requirements

- Unit tests covering chunk planning and bounded concurrency behavior.
- Tests validating merged multi-chunk response assembly.
- Performance-oriented test(s) or guardrails for large schema simulation.
- Typecheck and touched test-suite pass.

---

## Acceptance Checks

- Large-scope Auto-Map executes through chunked orchestration under configured limits.
- Aggregated response behavior is deterministic.
- No direct full-schema prompting code path exists.
- Tests mapped to `AE-02` pass.

---

## Blockers / Risks

- Chunk-size defaults may require environment tuning per deployment tier.
- Long-running large-schema workloads may later require Step Functions handoff.

---

## Notes

- Keep chunk strategy configurable for safe iteration.