# TASK

## Title

Implement Start From mode section and submission branching UX

---

## ID

T-05

---

## Spec

FS-088

---

## Spec Rev

Rev: 2

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- T-02
- T-03

---

## Implements

- `AE-09`
- `AE-10`
- `AE-11`
- `AE-12`
- `AE-14`

---

## Summary

Add the Start From section with only blank and auto-map options, wire dynamic primary-action labeling, and implement create-submit validation/branching behavior with explicit disabled/future-state handling when auto-map-at-create is unavailable.

---

## Scope

### In Scope

- Render Start From radio options:
  - Blank mapping
  - Auto-map suggestions
- Remove/omit template mode option.
- Make Start From selection required for submit.
- Update footer primary button label by mode.
- Submit branch:
  - Blank mode uses create mapping flow + editor navigation.
  - Auto-map mode invokes available supported path or remains disabled/future.
- Inline validation for missing source/target/start-mode in addition to name.

### Out of Scope

- Backend contract changes.
- Mapping editor auto-map workspace internals.

---

## Relevant Areas

- `ui/src/features/projects/components/CreateMappingPage.tsx`
- `ui/src/features/projects/components/__tests__/CreateMappingPage.test.tsx`
- `ui/src/lib/api/types.ts` ?

---

## Change Requirements

- No fake suggestion generation path allowed.
- If auto-map-at-create unsupported, communicate disabled/future state clearly.
- Preserve current cancel behavior and create-error handling.

---

## Verification Requirements

- Tests for Start From options presence/exclusion of template.
- Tests for dynamic button label.
- Tests for required-field validation gating name/source/target/start mode.
- Tests for blank-path create payload + navigation.
- Tests for unsupported auto-map mode disabled/future handling.

---

## Acceptance Checks

- `AE-09` to `AE-12` and `AE-14` pass in UI tests.
- Primary CTA label changes correctly by mode.
- Submit blocked until all required fields selected.

---

## Blockers / Risks

- Availability detection for auto-map-at-create may depend on finalized orchestration contract (Q2).

---

## Notes

- Keep auto-map create path feature-gated and explicit until backend/UI handoff contract is confirmed.
