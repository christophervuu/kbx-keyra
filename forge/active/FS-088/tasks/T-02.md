# TASK

## Title

Implement mapping details and validation state on single-page create flow

---

## ID

T-02

---

## Spec

FS-088

---

## Spec Rev

Rev: 2

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- T-01

---

## Implements

- `AE-03`
- `AE-11`

---

## Summary

Wire Mapping Details section with required Mapping Name, optional multiline Business Context input, and inline validation behavior consistent with redesigned page surfaces.

---

## Scope

### In Scope

- Add Mapping Name input (required).
- Add Business Context textarea (optional) with provided intent placeholder.
- Implement inline required validation message rendering for missing fields as part of submit gating state.
- Keep entered values stable across validation failures.

### Out of Scope

- Schema card selection logic.
- Add-schema flow.
- Auto-map create branching.
- Backend persistence changes for business context.

---

## Relevant Areas

- `ui/src/features/projects/components/CreateMappingPage.tsx`
- `ui/src/features/projects/components/__tests__/CreateMappingPage.test.tsx`
- `ui/src/features/projects/types.ts` ?

---

## Change Requirements

- Replace old step-1 name-only handling with single-page form state.
- Inline validation must match app style used in redesigned pages.
- Validation model should support required gating for fields beyond name (source/target/start mode) to avoid rework in later tasks.

---

## Verification Requirements

- Tests for required name enforcement and business-context optional behavior.
- Tests for validation-message visibility and clearing when user corrects input.
- UI lint/typecheck and targeted test run.

---

## Acceptance Checks

- `AE-03` and name-related parts of `AE-11` pass via tests.
- Business Context input renders and remains optional.
- Validation error appears inline when Mapping Name missing on submit.

---

## Blockers / Risks

- Potential rework if business context persistence contract (Q1) changes field naming.

---

## Notes

- Store businessContext in local UI state even before persistence contract is finalized.
