# TASK

## Title

Align action resolver and picker behavior with parameter-aware availability rules

---

## ID

T-04

---

## Spec

FS-095

---

## Spec Rev

Rev: 1

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- T-01
- T-03

---

## Implements

- AE-05
- AE-07

---

## Summary

Update resolver and picker interactions so parameter requirements and context constraints are reflected in enabled/disabled state, hidden-by-default behavior, and deterministic reason messages. Preserve current array-context gating for add-input options.

---

## Scope

### In Scope

- Extend action resolver availability checks to include parameter readiness/context constraints.
- Preserve hidden-by-default unavailable action behavior with search reveal and reason text.
- Validate add-input menu gating for `Item()` and `Parent()` under array-scope context only.
- Add resolver and picker tests for availability and reason consistency.

### Out of Scope

- Redesigning picker UI hierarchy.
- Broad filtering/ranking experimentation.
- Backend-driven feature flags for action visibility.

---

## Relevant Areas

- `ui/src/features/mappings/lib/smart-builder-action-resolver.ts`
- `ui/src/features/mappings/lib/*smart-builder*.test.ts`
- `ui/src/features/mappings/components/SmartBuilderPanel.tsx`
- `ui/src/features/mappings/components/SmartBuilderPanel.test.tsx`

---

## Change Requirements

- Resolver output must remain deterministic for the same input context and parameter draft.
- Unavailable actions remain non-prominent by default but discoverable via picker search with clear reasons.
- Array-context add-input gating must be preserved and covered by regression tests.

---

## Verification Requirements

- Unit tests for resolver availability + reason output under parameter/context permutations.
- Component tests for picker search reveal of unavailable actions.
- Component tests for `Item()/Parent()` visibility toggling by context.
- Lint/typecheck pass for touched modules.

---

## Acceptance Checks

- Unavailable actions are hidden by default and surfaced via search with reasons.
- Resolver reflects parameter/context constraints consistently.
- `Item()` and `Parent()` options remain gated to array-scope contexts.
- Tests pass for touched resolver/picker modules.

---

## Blockers / Risks

- Overly strict availability logic can reduce discoverability; reason messaging must remain actionable.

---

## Notes

- Keep behavior aligned with existing user expectation from recent recipe-first refactor.
