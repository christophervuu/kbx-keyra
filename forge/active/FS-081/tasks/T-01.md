# TASK

## Title

Update deployment architecture documents for Sandbox control-plane and Dev/Preprod/Prod runtime model

---

## ID

T-01

---

## Spec

FS-081

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- none

---

## Implements

- AE-01
- AE-02
- AE-03
- AE-04
- AE-05
- AE-06

---

## Summary

Codify the new environment deployment architecture in existing architecture docs, including plane separation, runtime footprint, artifact identity rules, promotion relay without cross-account assume-role, rollback pointer behavior, and preview routing implications.

---

## Scope

### In Scope

- Update `forge/architecture/deployments.md` for Sandbox→Dev/Preprod/Prod model.
- Update `forge/architecture/backend-api.md` with control-plane orchestration and runtime ingestion endpoint responsibilities.
- Update `forge/architecture/infrastructure.md` with per-account runtime footprint and connectivity assumptions.
- Update `forge/architecture/phase-1-readiness.md` environment model constraints (QA→PREPROD + SANDBOX control-plane distinction).
- Update `forge/architecture/INDEX.md` Last Updated fields and notes for changed docs.

### Out of Scope

- Code implementation of endpoints, persistence, or UI behavior.
- Auth/security hardening details.

---

## Relevant Areas

- `forge/architecture/deployments.md`
- `forge/architecture/backend-api.md`
- `forge/architecture/infrastructure.md`
- `forge/architecture/phase-1-readiness.md`
- `forge/architecture/INDEX.md`

---

## Change Requirements

- Replace/extend DEV/QA/PROD runtime model references with DEV/PREPROD/PROD runtime + SANDBOX control-plane context.
- Document control-plane vs runtime-plane ownership boundaries.
- Document minimal runtime footprint components per account.
- Document artifact immutability and same-artifact promotion invariants.
- Document pointer-only rollback contract and runtime-local execution requirement.
- Document direct payload push as MVP transfer mechanism and signed-URL pull as deferred.
- Document payload-size limit requirement and deterministic oversize rejection semantics.
- Document missing-rollback-artifact failure contract (`artifact_not_available_for_rollback`) with remediation guidance.
- Document QA→PREPROD normalization policy (raw preserved for audit, normalized in domain/presentation).
- Document server-preview routing through selected runtime environment and returned artifact identity metadata.

---

## Verification Requirements

- Architecture docs are internally consistent across deployments/backend-api/infrastructure/phase-1-readiness.
- `INDEX.md` reflects meaningful updates with current date.
- Terminology audit confirms QA removed as runtime environment model in updated sections.
- Docs explicitly capture MVP network assumption: HTTPS runtime endpoints reachable from sandbox via internal public allowlisting; private connectivity deferred.

---

## Acceptance Checks

- Architecture docs explicitly describe SANDBOX as control plane and DEV/PREPROD/PROD as runtime targets.
- Promotion and rollback behavior match FS-081 AE-02 and AE-04 semantics.
- Runtime-local execution rule (no sandbox runtime reads) is explicitly captured.
- `forge/architecture/INDEX.md` updated for all modified architecture documents.

---

## Blockers / Risks

- Existing architecture sections may still contain legacy QA assumptions requiring broad doc reconciliation.

---

## Notes

- This is the explicit architecture-impact task required by workflow rules.
