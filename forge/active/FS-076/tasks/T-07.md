# TASK

## Title

Add CDM integration guardrail tests and no-write-call verification

---

## ID

T-07

---

## Spec

FS-076

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-02
- T-03
- T-04
- T-05
- T-06

---

## Implements

- `AE-09`
- `AE-01`
- `AE-06`

---

## Summary

Add cross-layer guardrail coverage to prove CDM flows remain read-only at GitHub API level and to prevent regressions in root-scoped browsing and error-handling behavior.

---

## Scope

### In Scope

- Add backend integration tests covering browse/link/sync happy and failure paths with client call assertions.
- Add explicit assertions that CDM flows never invoke GitHub write methods.
- Add regression checks for CommonDataModels root guard.
- Add regression checks confirming one-directory-level listing behavior.
- Add targeted adapter/contract tests for no-write invariants where appropriate.

### Out of Scope

- UI pixel/layout assertions unrelated to CDM behavior.
- Expanding test coverage to non-CDM publish flows.

---

## Relevant Areas

- `tests/lambda/**/*.test.ts`
- `tests/lib/**/*.test.ts` ?
- `ui/src/lib/api/http-adapter.test.ts`
- test fixtures/mocks for GitHub client modules ?

---

## Change Requirements

- Tests must fail if any CDM flow attempts write-capable GitHub operations.
- Test suite must verify root guard cannot be bypassed with path traversal patterns.
- Error normalization/assertions must preserve actionable behavior for UI consumption.

---

## Verification Requirements

- Run targeted backend integration and adapter tests.
- Run full relevant package test subsets for touched files.
- Typecheck/lint as needed for new test code.

---

## Acceptance Checks

- Automated tests explicitly verify no GitHub write calls in CDM browse/link/sync flows.
- Automated tests cover root path guard against out-of-scope requests.
- Automated tests cover sync failure behavior expected by UI.
- Automated tests cover status-refresh `update-available` behavior without content mutation.
- Test suite passes for all newly added assertions.

---

## Blockers / Risks

- Existing test harness may need additional GitHub client mocking seams for method-call assertions.

---

## Notes

- Keep guardrail tests deterministic and independent of live GitHub network calls.
