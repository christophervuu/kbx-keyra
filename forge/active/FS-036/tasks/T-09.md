# TASK

## Title

Update ui-application.md architecture for linked debugging model

---

## ID

T-09

---

## Spec

FS-036

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

todo

---

## Depends On

- T-06

---

## Implements

- AE-01 (architecture documentation)

---

## Summary

Update `forge/architecture/ui-application.md` to document the linked debugging selection model, the cross-panel interaction contract, the failure explainer module, and the jump-to-rule navigation pattern introduced by FS-036. Update `INDEX.md` last-updated date.

---

## Scope

### In Scope

- Add a new subsection under the Advanced Testing Page section documenting:
  - `DebugSelection` state model and `useLinkedDebugSelection` hook contract
  - Cross-panel linking dimensions (`targetPath`, `ruleIndex`)
  - Which display components accept selection props and what they do
  - Jump-to-rule route state contract
  - Failure explainer module and its pattern-matching approach
- Update the component hierarchy listing to reflect new props and interactions
- Add `use-linked-debug-selection.ts` and `failure-explainer.ts` to the module structure
- Update `INDEX.md` last-updated date for `ui-application.md`

### Out of Scope

- Modifying any implementation code
- Creating new architecture documents (this is an update to an existing document)

---

## Relevant Areas

- `forge/architecture/ui-application.md`
- `forge/architecture/INDEX.md`

---

## Change Requirements

Add a new section in `ui-application.md` after the "Advanced Testing Page" section:

**Linked Debugging Model (FS-036)**

Document:
1. `DebugSelection` type definition and semantics
2. `useLinkedDebugSelection` hook contract (inputs, outputs, auto-clear behavior)
3. Panel interaction matrix: which panels fire selection, which panels receive highlights
4. Linking dimensions: `targetPath` (primary), `ruleIndex` (secondary)
5. Jump-to-rule route state: `location.state.selectedTargetPath` pattern
6. Failure explainer: `explainDiagnostic()` contract, pattern matching approach, extensibility
7. InlinePreviewStrip linked selection (subset: diagnostics ↔ output only)

Update the module structure tree to include:
- `ui/src/features/mappings/hooks/use-linked-debug-selection.ts`
- `ui/src/features/mappings/lib/failure-explainer.ts`

Update `INDEX.md`:
- Change `ui-application.md` last-updated date to reflect FS-036 changes
- Add FS-036 mention to the "Covers" column

---

## Verification Requirements

- Architecture document accurately reflects the implemented linked selection model
- Module structure tree is updated with new files
- INDEX.md date is updated
- No implementation code is changed by this task

---

## Acceptance Checks

- `forge/architecture/ui-application.md` contains a "Linked Debugging Model (FS-036)" section
- Module structure includes `use-linked-debug-selection.ts` and `failure-explainer.ts`
- `forge/architecture/INDEX.md` last-updated date is current
- Document is internally consistent with the spec and implementation

---

## Blockers / Risks

- This task should be executed after T-06 is complete so the architecture document reflects the actual implementation rather than just the spec intent.

---

## Notes

This is an architecture documentation task, not an implementation task. Load the current `ui-application.md` and `INDEX.md` before making changes. Verify alignment with the actual implementation from T-01 through T-08.
