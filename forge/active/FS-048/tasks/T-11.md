# TASK

## Title

Update ui-application.md and project-structure.md for Auto-Map Review Workspace architecture

---

## ID

T-11

---

## Spec

FS-048

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01 (persistence types and utilities)
- T-02 (mode switch)
- T-03 (workspace hook)
- T-04 (staleness)
- T-05 (workspace shell)
- T-06 (suggestion cards)
- T-07 (toolbar)
- T-08 (refresh)
- T-09 (preview)
- T-10 (integration)

---

## Implements

- Architecture documentation coverage for the workspace pattern

---

## Summary

Update `forge/architecture/ui-application.md` to document the Auto-Map Review Workspace architecture: the workspace mode switch pattern, suggestion persistence model, extended lifecycle state machine, refresh/merge strategy, preview integration, and the relationship to the retired drawer pattern. Update `forge/architecture/project-structure.md` to reflect new files. Update `forge/architecture/INDEX.md` date.

---

## Scope

### In Scope

- Replace the "Auto-Map Review Drawer Architecture (FS-046)" section in `ui-application.md` with a new "Auto-Map Review Workspace Architecture (FS-046 → FS-048)" section covering:
  - Workspace mode switch pattern (`EditorView` extension, center panel conditional rendering)
  - Trigger and entry points (ObjectSummaryPanel, re-entry with persisted suggestions)
  - `useAutoMapWorkspace` hook contract (replace `useAutoMapReview` documentation)
  - Suggestion persistence model (sessionStorage, key pattern, hydration, corruption recovery)
  - Extended lifecycle state machine (suggested/accepted/edited/dismissed/stale with transition table)
  - Staleness detection strategy
  - Refresh/regenerate merge logic
  - Preview integration pattern (lazy evaluation, source data dependency)
  - Filter and bulk action model
  - Showcase vs RAG-ready design decisions
  - Drawer retirement note

- Update component hierarchy in the Mapping Editor Architecture section:
  - Remove `AutoMapReviewDrawer` from the hierarchy
  - Add `AutoMapWorkspace` in the center panel conditional
  - Add workspace sub-components

- Update module structure to include new files:
  - `components/AutoMapWorkspace.tsx`
  - `components/WorkspaceHeader.tsx`
  - `components/WorkspaceSuggestionCard.tsx`
  - `components/WorkspaceToolbar.tsx`
  - `components/WorkspaceSuggestionPreview.tsx`
  - `hooks/use-auto-map-workspace.ts`
  - `hooks/use-suggestion-preview.ts`
  - `lib/auto-map-persistence.ts`
  - `lib/auto-map-staleness.ts`

- Update `forge/architecture/project-structure.md` to reflect new files
- Update `forge/architecture/INDEX.md` Last Updated date and coverage description

### Out of Scope

- Changes to `ai-runtime.md` (no backend changes in this spec)
- Changes to `mapping-engine.md`
- Removing old file entries from architecture docs (old files are retained, not deleted)

---

## Relevant Areas

- `forge/architecture/ui-application.md` — primary architecture doc
- `forge/architecture/project-structure.md` — file structure
- `forge/architecture/INDEX.md` — index entry

---

## Change Requirements

- The workspace architecture section must document:
  - The mode switch pattern and how it extends `EditorView`
  - The persistence strategy (sessionStorage, key pattern, tier model)
  - The lifecycle state machine with transition table
  - The merge strategy for refresh operations
  - The staleness detection approach
  - The preview integration pattern
  - Design decisions and rationale for workspace vs drawer
  - Showcase scope vs long-term scope notes

- Component hierarchy must be updated to show the workspace in the center panel position
- Module structure must include all new files
- INDEX.md coverage text must mention FS-048 workspace pattern

---

## Verification Requirements

- Architecture doc accurately reflects implemented behavior
- No broken cross-references
- INDEX.md date is current
- project-structure.md includes all new files

---

## Acceptance Checks

- `ui-application.md` Auto-Map section updated
- Component hierarchy reflects workspace (not drawer)
- Module structure lists all new files
- `project-structure.md` updated
- `INDEX.md` updated

---

## Blockers / Risks

- Must wait for all other tasks to complete to document the implemented state accurately

---

## Notes

- This task documents the implemented state — it should be executed after all implementation tasks are complete
- Keep the existing Showcase AI Integration Pattern section (FS-041/FS-042) but update the Auto-Map entry to reference the workspace pattern instead of the drawer
- The old `useAutoMapReview` hook documentation should be replaced, not left alongside the new documentation
