# TASK

## Title

Implement rollback and server-preview orchestration against runtime environment APIs

---

## ID

T-04

---

## Spec

FS-083

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

todo

---

## Depends On

- T-02

---

## Implements

- AE-03
- AE-05

---

## Summary

Implement control-plane rollback and server-preview orchestration paths, including environment-targeted runtime API calls, pointer-only rollback outcome handling, and preview response metadata propagation.

---

## Scope

### In Scope

- Rollback orchestration command to selected runtime environment with target artifact id.
- Rollback orchestration status persistence and history reflection.
- Server preview orchestration to selected environment runtime endpoint.
- Preview response mapping with environment + artifact metadata for UI consumers.

### Out of Scope

- Deploy/promote orchestration logic.
- UI Deployment Page rendering changes.

---

## Relevant Areas

- `src/lambda/deployment/*rollback*`
- `src/lambda/*preview*` ?
- `src/lib/deployment/*`
- `src/lib/persistence/deployments.ts` ?
- `tests/lambda/**/*rollback*`
- `tests/lambda/**/*preview*`

---

## Change Requirements

- Rollback flow must invoke runtime rollback API and reflect pointer-change outcomes only.
- Rollback failure must preserve current active artifact state in control-plane model.
- Preview flow must route by selected runtime environment and return artifact identity metadata.
- Non-deployed preview path must surface deterministic not-deployed error contract.

---

## Verification Requirements

- Integration tests for rollback success/failure (including unknown artifact target).
- Preview orchestration tests across at least two environments with distinct active artifacts.
- Error envelope tests for deterministic not-deployed and runtime failures.

---

## Acceptance Checks

- Rollback orchestration updates history/status only after runtime acknowledgment.
- Failed rollback attempts do not alter active artifact state.
- Server preview returns environment + artifact metadata tied to selected environment runtime.
- Tests covering AE-03/AE-05 pass.

---

## Blockers / Risks

- Runtime preview API shape may still be in flux from FS-082 implementation details.

---

## Notes

- Keep preview side-effect free; no deploy/pointer mutations allowed in preview path.
- Spec Rev 2 **D4**: Rollback only to artifacts in runtime local storage. `ARTIFACT_NOT_PRESENT` failure if missing. No on-demand import in MVP.
