# TASK

## Title

Add end-to-end backend verification coverage for CDM re-sync re-ingestion

---

## ID

T-06

---

## Spec

FS-077

---

## Spec Rev

Rev: 1

---

## Agent

task

---

## Status

todo

---

## Depends On

- T-02
- T-03
- T-04
- T-05

---

## Implements

- `AE-01`
- `AE-02`
- `AE-03`
- `AE-04`
- `AE-05`
- `AE-06`
- `AE-07`

---

## Summary

Create comprehensive test coverage across resolver, sync orchestration, diff summary, and indexing/retrieval outcomes to verify FS-077 behavior and guard against regression.

---

## Scope

### In Scope

- Integration tests for changed/no-op/failed re-sync flows.
- Resolver-focused tests for cross-folder dependency behavior.
- Tests validating refreshed index/query behavior after successful re-sync.
- Regression checks for safe-failure behavior and deterministic response payloads.

### Out of Scope

- UI-end-to-end tests.
- CI workflow changes beyond existing test command usage.

---

## Relevant Areas

- `tests/lambda/schema/**/*.test.ts`
- `tests/lib/schema/**/*.test.ts`
- `tests/integration/schema-ingestion/**/*.test.ts`

---

## Change Requirements

- Test suite must assert commit-comparison branching (update vs no-op).
- Test suite must assert dependency resolution behavior and failure reasons.
- Test suite must assert diff summary payload and refreshed retrieval state.

---

## Verification Requirements

- Run targeted test suites for touched modules.
- Run repository typecheck + lint for touched backend code.

---

## Acceptance Checks

- Automated coverage exists for AE-01 through AE-07.
- Regressions around dependency resolution and no-op branching are caught by tests.
- Backend test suite passes for touched modules.

---

## Blockers / Risks

- Fixture setup for realistic cross-folder `$ref` chains may be non-trivial.

---

## Notes

- Reuse existing schema-ingestion integration harness where possible.
