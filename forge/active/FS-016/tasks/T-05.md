# TASK

## Title

Update project-structure.md with Schema Library page files

---

## ID

T-05

---

## Spec

FS-016

---

## Spec Rev

Rev: 1

---

## Agent

task

---

## Status

done

---

## Depends On

- T-04 (page assembly must be complete to know final file list)

---

## Implements

- AE-01 (indirectly — ensures architecture documentation reflects the new page)

---

## Summary

Update `forge/architecture/project-structure.md` to reflect the new files added by FS-016 in the `ui/src/features/schemas/` module. This keeps the architecture document current and prevents drift from the actual codebase.

---

## Scope

### In Scope

- Update the `features/schemas/` section in `forge/architecture/project-structure.md` to list:
  - New page components (SchemaLibraryPage, SchemaLibrarySkeleton, SchemaLibraryEmptyState, SchemaLibraryCard, control components)
  - New hooks (use-schema-library)
  - New lib files (schema-filters)
  - New test files
- Verify entries match the actual files created by T-01 through T-04.

### Out of Scope

- Any code changes.
- Changes to `ui-application.md` (not needed — no new architectural patterns introduced).
- Changes to `INDEX.md` (no new architecture documents created).

---

## Relevant Areas

- `forge/architecture/project-structure.md`

---

## Change Requirements

Add entries under the `features/schemas/` section in the project structure document. The section currently lists parsers, tree view, types, and hooks. Extend it with:

```
      components/
        ...existing tree components...
        SchemaLibraryPage.tsx       Schema Library page assembly (FS-016 T-04)
        SchemaLibraryCard.tsx       Individual schema card with badges and navigation (FS-016 T-02)
        SchemaLibrarySearch.tsx     Search input control (FS-016 T-03)
        SchemaLibraryFilters.tsx    Multi-select filter toggles (FS-016 T-03)
        SchemaLibrarySortControl.tsx Sort field/direction control (FS-016 T-03)
        ActiveFilterChips.tsx       Removable active filter chips (FS-016 T-03)
        SchemaLibraryNoResults.tsx  Empty filter results message (FS-016 T-03)
        SchemaLibrarySkeleton.tsx   Loading skeleton cards (FS-016 T-04)
        SchemaLibraryEmptyState.tsx Zero-schemas empty state (FS-016 T-04)
        __tests__/
          SchemaLibraryCard.test.tsx
          SchemaLibraryControls.test.tsx
          SchemaLibraryPage.test.tsx
      hooks/
        ...existing hooks...
        use-schema-library.ts       Data + filter/sort orchestration hook (FS-016 T-01)
        use-schema-library.test.ts  Hook tests (FS-016 T-01)
      lib/
        ...existing lib...
        schema-filters.ts           Pure filter/sort utilities (FS-016 T-01)
        schema-filters.test.ts      Filter/sort unit tests (FS-016 T-01)
```

Verify the actual files match this list. Adjust if T-01 through T-04 used different naming or organization.

---

## Verification Requirements

- `forge/architecture/project-structure.md` accurately lists all new files created by FS-016.
- No orphaned entries (all listed files exist).
- No missing entries (all new files are listed).
- Document formatting is consistent with existing entries.

---

## Acceptance Checks

- `project-structure.md` updated with all FS-016 files
- Entries are in the correct section (`features/schemas/`)
- Formatting follows existing conventions (indentation, annotations)
- No files created by T-01–T-04 are missing from the document

---

## Blockers / Risks

- Must run after T-01–T-04 to know the final file list. If file names changed during implementation, this task must reconcile.

---

## Notes

- This is a documentation-only task. No source code changes.
- Check the actual filesystem before updating to ensure accuracy.
- The `ui-application.md` does not need updates because FS-016 follows existing feature module patterns (no new architectural decisions).
