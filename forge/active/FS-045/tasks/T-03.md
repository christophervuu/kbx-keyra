# TASK

## Title

Unit tests for auto-map Lambda handler

---

## ID

T-03

---

## Spec

FS-045

---

## Spec Rev

Rev: 1

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01
- T-02

---

## Implements

- `AE-01`, `AE-02`, `AE-03`, `AE-04`, `AE-05`, `AE-06`, `AE-07`, `AE-08`
- `AE-09`, `AE-10`, `AE-11`, `AE-12`
- `AE-13`, `AE-14`
- `AE-15`

---

## Summary

Create a comprehensive unit test suite at `tests/lambda/ai/auto-map.test.ts` covering all acceptance examples from the spec. Follow the established test patterns from `tests/lambda/ai/explain-rule.test.ts` and `tests/lambda/ai/suggest-expression.test.ts`: mock `invokeAI` via `vi.hoisted()`, mock the engine's `parse()` for validation scenarios, and test all input validation, success, error mapping, validation enrichment, and header cases.

---

## Scope

### In Scope

- New file `tests/lambda/ai/auto-map.test.ts`
- Mock `invokeAI` from `src/lib/ai/index.js`
- Mock `parse` from `src/engine/index.js` (or the specific engine module) for validation test scenarios
- Test cases for all 15 acceptance examples (AE-01 through AE-15)
- Verify `invokeAI` is called with correct promptId and variables
- Verify validation enrichment on success responses
- Verify JSON and CORS headers

### Out of Scope

- Integration tests against real GitHub Models (that's manual/local testing, T-04)
- Tests for the shared AI runtime (already covered in `tests/lib/ai/`)
- Tests for the engine's `parse()` function (already covered in engine tests)

---

## Relevant Areas

- `tests/lambda/ai/auto-map.test.ts` (new)
- `tests/lambda/ai/explain-rule.test.ts` (pattern reference)
- `tests/lambda/ai/suggest-expression.test.ts` (pattern reference)
- `src/lambda/ai/auto-map.ts` (tested module)

---

## Change Requirements

Create `tests/lambda/ai/auto-map.test.ts` with:

1. **Mock setup** following the established pattern:
   ```typescript
   const invokeAIMock = vi.hoisted(() => vi.fn());
   const parseMock = vi.hoisted(() => vi.fn());

   vi.mock('../../../src/lib/ai/index.js', () => ({
     invokeAI: invokeAIMock,
   }));

   vi.mock('../../../src/engine/index.js', () => ({
     parse: parseMock,
     // May need to mock defaultRegistry or registerAllFunctions
   }));
   ```

2. **Helper** `createEvent(body: string | null): APIGatewayProxyEvent` matching existing pattern

3. **Test cases** (each mapped to an AE):

   **Input validation tests:**
   - AE-01: Valid request with all fields → 200 with rules and validation
   - AE-02: Missing `targetSection` → 400
   - AE-03: Missing `sourceContext` → 400
   - AE-04: Empty string `targetSection` → 400
   - AE-05: Empty string `sourceContext` → 400
   - AE-06: Missing `businessContext` → 200, `invokeAI` called with `businessContext: ""`
   - AE-07: Null body → 400
   - AE-08: Invalid JSON body → 400

   **AI error mapping tests:**
   - AE-09: `PROMPT_NOT_FOUND` → 404
   - AE-10: `MODEL_RATE_LIMITED` → 429
   - AE-11: `MODEL_ERROR` → 500
   - AE-12: Unexpected exception → 500 with synthetic error

   **Validation tests:**
   - AE-13: Expression with parse errors → rule has `validation.valid = false` with diagnostics
   - AE-14: All expressions pass parse → all rules have `validation.valid = true`

   **Header tests:**
   - AE-15: JSON and CORS headers present on all response types

4. **Additional test case:**
   - Verify `invokeAI` is called with `promptId: 'auto-map'` and correct variable names

---

## Verification Requirements

- All tests pass with `npx vitest run tests/lambda/ai/auto-map.test.ts`
- No TypeScript errors in the test file
- Test count covers all 15 acceptance examples (some AEs may map to multiple test assertions within a single test)

---

## Acceptance Checks

- `tests/lambda/ai/auto-map.test.ts` exists
- All tests pass
- Every acceptance example (AE-01 through AE-15) has corresponding test coverage
- Tests mock `invokeAI` — no real AI calls are made
- Tests mock `parse` — no real engine parsing in unit tests (controlled validation results)
- Test structure follows the established `vi.hoisted()` + dynamic import pattern
- TypeScript compilation passes for the test file
- No lint errors

---

## Blockers / Risks

- The engine mock may need careful handling if `auto-map.ts` imports `defaultRegistry` or calls `registerAllFunctions`. The mock setup must cover all imports from the engine module.

---

## Notes

- Follow the `suggest-expression.test.ts` pattern most closely, as it has the most test cases and covers both required and optional fields
- Each test should use dynamic `await import(...)` after `vi.resetModules()` in `beforeEach`, matching the existing pattern
- The parse mock should return `{ ast: {}, diagnostics: [] }` for valid expressions and `{ ast: null, diagnostics: [{ severity: 'error', message: '...' }] }` for invalid ones
- Consider testing the edge case where `data.rules` is empty (should return 200 with empty array)
