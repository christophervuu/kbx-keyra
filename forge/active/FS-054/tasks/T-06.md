# TASK

## Title

Update ui-application.md to reflect implemented UI state

---

## ID

T-06

---

## Spec

FS-054

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-03

---

## Implements

- `AE-02`
- `AE-03`

---

## Summary

Apply the findings from T-03 to update `forge/architecture/ui-application.md` so that documented module structure, adapter patterns, state management, routing, component organization, and Mapping Editor architecture accurately reflect the implemented UI application. Correct drift, add missing documentation, and ensure the document serves as a reliable reference for Phase 1 backend planning.

---

## Scope

### In Scope

- Correcting module structure tree to match actual `ui/src/` layout
- Updating adapter pattern documentation to match actual implementations
- Correcting routing table to match actual registered routes
- Updating component organization to reflect all current feature modules
- Correcting engine integration documentation to match actual boundary
- Updating Mapping Editor architecture to match current composition
- Adding documentation for undocumented conventions that have become standard
- Ensuring internal consistency across all sections
- Marking areas where incremental updates have created redundancy or confusion

### Out of Scope

- Redesigning the document structure (though noting where structure could be improved for Phase 1 is acceptable)
- Adding Phase 1 architecture — that goes in T-07's document
- Changing code to match documentation
- Documenting individual component APIs (doc stays at architectural level)
- Resolving conflicts with active in-flight specs (FS-050–053)

---

## Relevant Areas

- `forge/architecture/ui-application.md`
- T-03 audit findings (input)
- `ui/src/` (reference for verification)

---

## Change Requirements

Based on T-03 findings:

1. **Module structure corrections**: Update the module structure tree to reflect actual `ui/src/` layout. Add missing directories/files that are architecturally significant. Remove entries that no longer exist.

2. **Adapter pattern corrections**: Ensure adapter documentation matches actual `ApiAdapter` interface, all implementation classes, and bootstrap behavior. Document all current adapter methods accurately.

3. **State management corrections**: Verify and correct the state management section. If any patterns have evolved beyond what's documented, update.

4. **Routing corrections**: Ensure the route table matches actual `App.tsx` configuration. Add missing routes, remove stale routes.

5. **Component organization corrections**: Update feature module list to include all current features. Correct placement rules if they no longer reflect practice.

6. **Engine integration corrections**: Ensure documented engine boundary and hook patterns match implementation.

7. **Mapping Editor corrections**: Ensure layout, hierarchy, view toggle, and slot model documentation matches current implementation.

8. **Convention documentation**: Add brief documentation for any conventions identified in T-03 that are architecturally significant and not yet documented (e.g., test co-location patterns, naming conventions, hook lifecycle patterns).

9. **Internal consistency pass**: Read through the full document after corrections to identify redundancies, contradictions, or sections that have become confusing through incremental growth.

---

## Verification Requirements

- After update, module structure tree matches actual filesystem
- After update, adapter documentation matches actual type definitions and implementations
- After update, route table matches actual router config
- After update, feature module list matches actual `ui/src/features/` directories
- Document is internally consistent

---

## Acceptance Checks

- All T-03 findings are addressed
- Module structure tree is accurate
- Adapter pattern section covers all current implementations and methods
- Route table is complete and accurate
- Feature module list matches reality
- Document is internally consistent
- No net increase in document confusion (if sections are overgrown, note for future cleanup but don't restructure in this pass)

---

## Blockers / Risks

- `ui-application.md` is 700+ lines — this is the largest document to update
- The document has grown through many incremental updates and may have accumulated redundancy
- Active specs (FS-050–053) may be actively changing areas this doc covers — note conflicts but don't attempt to resolve

---

## Notes

The UI architecture doc has grown substantially and may benefit from structural improvements in a future pass. For this reconciliation, the goal is accuracy — not elegance. If the document is accurate but overgrown, that's acceptable. If sections have become contradictory, resolve the contradiction in favor of current implementation reality.

Key areas likely to need updates:
- Feature module list (schemas, mappings, projects, home are documented — check for others)
- Adapter method inventory (methods added for AI showcase and potentially other flows)
- Hook list completeness (many hooks added across recent specs)
- Array Builder architecture section (heavily evolved through FS-043, FS-051)

Execution notes (2026-05-14):
- Updated `forge/architecture/ui-application.md` using T-03 findings with a minimal-diff accuracy pass.
- Module structure wording corrected from "placeholder pages" to route wrapper pages; added first-class feature coverage for `features/home` and `features/projects` to match implemented architecture emphasis.
- Adapter section extended with explicit `ApiAdapter` contract-surface summary and clarified intentional Phase 0 placeholders (`listTemplates()` and `querySchemaNodes()` return empty arrays in `LocalStorageAdapter`).
- Adapter implementation detail updated to include `HybridAdapter.autoMapSection()` override in module tree.
- State management section clarified canonical concurrency behavior: `useAsyncState` request-id race protection (`requestIdRef`).
- Engine integration section updated to explicitly include `inferExpressionType()` and rule-type normalization (`null|any` -> `'string'`) at UI→engine boundary.
- `useExpressionPreview` behavior clarified with `evaluateExpression()` short-circuit and unified error mapping contract.
- Routing section updated with React Router future flags from `App.tsx` and breadcrumb behavior updated to include `BreadcrumbContext` label registration (raw IDs default, contextual names supported).
- Component organization placement rule reconciled with current implementation reality by replacing absolute no-cross-feature rule with constrained allowed pattern + shared-extraction preference.
- Mapping Editor architecture wording updated to remove stale "Auto-map placeholder" phrasing and reflect active Auto-Map workspace re-entry indicator behavior.
