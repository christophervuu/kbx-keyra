# TASK

## Title

Integrate ParameterValueInput into SourceCard and NestedFunctionBuilder

---

## ID

T-06

---

## Spec

FS-053

---

## Spec Rev

Rev: 2

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- T-04

---

## Implements

- `AE-01`
- `AE-04`
- `AE-05`

---

## Summary

Update remaining surfaces that use `ArgumentSlotInput` or `ArgumentForm` for function parameter editing: `SourceCard` per-step argument rendering and `NestedFunctionBuilder` inline parameter editing. Ensure the new intent-based mode model is used consistently across all function parameter editing surfaces in the Mapping Editor.

---

## Scope

### In Scope

- Update `SourceCard.tsx` per-step argument rendering: verify it uses `ArgumentForm` (which now renders `ParameterValueInput` via T-04) — may need no changes if SourceCard already delegates to `ArgumentForm`
- Update `NestedFunctionBuilder.tsx`: verify it uses `ArgumentConfigurator` (legacy) — assess whether to update to new pattern or leave as-is given it's a legacy surface
- Verify that all inline `ArgumentSlotInput` usages in nested transforms (within `ArgumentSlotInput` itself for nested step args) are also updated or handled
- Ensure the "Change function" interaction in `NestedFunctionBuilder` header is consistent (it already uses a clean "Change" button — verify no X/close pattern exists)
- Update relevant tests

### Out of Scope

- Changes to `GuidedBuilder` or `ArgumentConfigurator` (legacy, Rules View only)
- Changes to `ExpressionBuilderPanel` (Rules View)
- New functional capabilities

---

## Relevant Areas

- `ui/src/features/mappings/components/SourceCard.tsx`
- `ui/src/features/mappings/components/SourceCard.test.tsx`
- `ui/src/features/mappings/components/NestedFunctionBuilder.tsx`
- `ui/src/features/mappings/components/NestedFunctionBuilder.test.tsx`
- `ui/src/features/mappings/components/ArgumentSlotInput.tsx` (nested transform rendering)

---

## Change Requirements

- Verify `SourceCard` delegates to `ArgumentForm` for per-step argument rendering — if so, T-04's changes automatically flow through
- If `SourceCard` has its own inline `ArgumentSlotInput` usages for the initial source card arguments, update those to use `ParameterValueInput`
- For `NestedFunctionBuilder`: it currently uses `ArgumentConfigurator` (legacy builder surface). Decision: leave as-is since it's the legacy path and only used within the deprecated `GuidedBuilder` flow. Add a code comment noting this surface uses the legacy pattern.
- Verify the nested transform rendering within `ArgumentSlotInput` itself (lines 724-900): these are recursive sub-argument forms that render for inline transforms. Since `ArgumentSlotInput` is deprecated, the nested transforms within it are also deprecated. The new `ParameterValueInput` expression mode uses a different flow (full function picker → ArgumentForm rendering).
- Ensure no remaining production surfaces render the old source/literal/expression triple toggle

---

## Verification Requirements

- Verify `SourceCard` per-step arguments render with new mode model (via ArgumentForm)
- Existing `SourceCard.test.tsx` tests pass
- `NestedFunctionBuilder` continues working (no changes expected)
- No production surface shows the old "source | literal | expression" triple toggle in the primary chain builder flow
- TypeScript strict typecheck passes

---

## Acceptance Checks

- SourceCard step arguments render with intent-based modes
- No regression in SourceCard functionality
- NestedFunctionBuilder unaffected or appropriately annotated
- No old triple toggle visible in chain builder flow
- `tsc --noEmit` passes
- `eslint` passes

---

## Blockers / Risks

- Risk: `ArgumentSlotInput` has nested transform rendering that is self-recursive — need to verify this code path is not broken for any remaining consumers

---

## Notes

- This task is primarily about verification and ensuring consistency across all surfaces
- The heavy lifting was done in T-04; this task catches remaining edges
- If `SourceCard` already delegates entirely to `ArgumentForm`, this task may be a verification-only pass
