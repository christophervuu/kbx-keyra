# TASK

## Title

Update ui-application.md architecture for ParameterValueInput and intent-based mode model

---

## ID

T-07

---

## Spec

FS-053

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01
- T-02
- T-03
- T-04
- T-05
- T-06

---

## Implements

- `AE-01`
- `AE-02`
- `AE-04`

---

## Summary

Update `forge/architecture/ui-application.md` to document the new `ParameterValueInput` component, the intent-based mode model, the Options mode pattern, and the deprecation of `ArgumentSlotInput`. Also update `forge/architecture/INDEX.md` last-updated date.

---

## Scope

### In Scope

- Add `ParameterValueInput.tsx` to the module structure listing in ui-application.md
- Document the intent-based mode model (Source/Static/Item/Options/Expression-secondary) as an architectural pattern
- Document the relationship between `PARAMETER_HINTS` and Options mode rendering
- Document the deprecation of `ArgumentSlotInput` (retained for legacy surfaces, new work uses `ParameterValueInput`)
- Note that `ArgumentForm` now delegates to `ParameterValueInput` for each slot
- Update INDEX.md with new last-updated date for ui-application.md
- Add FS-053 to the relevant spec references in ui-application.md component listing

### Out of Scope

- Changes to other architecture documents
- Code changes

---

## Relevant Areas

- `forge/architecture/ui-application.md`
- `forge/architecture/INDEX.md`

---

## Change Requirements

- In the module structure section of ui-application.md, add `ParameterValueInput.tsx` to the mappings components listing with a description: "Intent-based parameter value input (Source/Static/Item/Options/Expression-secondary); replaces ArgumentSlotInput for function parameter editing (FS-053)"
- Add a note to `ArgumentSlotInput.tsx` entry: "DEPRECATED — retained for legacy GuidedBuilder/NestedFunctionBuilder; new surfaces use ParameterValueInput (FS-053)"
- In the `ArgumentForm.tsx` entry, update description to note it now renders `ParameterValueInput` per slot
- Add a new section or subsection documenting the intent-based parameter input mode model:
  - Primary modes: Source, Static, Options (contextual), Item (array context)
  - Secondary: Expression (advanced escape hatch)
  - Options resolution via PARAMETER_HINTS registry
  - Mode → ArgumentSlot emission mapping
- Update INDEX.md `ui-application.md` row: `Last Updated` → 2026-05-14 (or current date at execution time)
- Update INDEX.md `Covers` column to mention FS-053 ParameterValueInput

---

## Verification Requirements

- ui-application.md contains ParameterValueInput documentation
- INDEX.md has updated date
- No broken markdown formatting
- Architecture description accurately reflects the implemented behavior

---

## Acceptance Checks

- `forge/architecture/ui-application.md` documents ParameterValueInput component and mode model
- `forge/architecture/INDEX.md` last-updated date is current
- Documentation is accurate and consistent with implementation from T-01 through T-06

---

## Blockers / Risks

- Depends on all implementation tasks completing first — architecture doc should reflect final state

---

## Notes

- This task should be executed after all implementation tasks are done, to ensure the architecture document accurately reflects what was built
- Keep the documentation concise — follow the existing style in ui-application.md (terse component descriptions with FS references)
