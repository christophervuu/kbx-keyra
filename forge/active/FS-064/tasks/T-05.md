# TASK

## Title

Build deployment UI page with Revisions/Versions sections and deploy action

---

## ID

T-05

---

## Spec

FS-064

---

## Spec Rev

Rev: 1

---

## Agent

ui-task

---

## Status

todo

---

## Depends On

- T-04

---

## Implements

- `AE-01` (deploy revision to DEV flow)
- `AE-03` (deploy version to PROD flow)
- `AE-09` (DEV shows both sections)
- `AE-10` (QA/PROD shows versions only)

---

## Summary

Create the deployment page for a mapping with two sections (Revisions and Versions), environment target picker, and deploy action. Enforce UI-level rules: revisions deployable to DEV only; versions deployable to all environments.

---

## Scope

### In Scope

- New feature module `ui/src/features/deployments/`
- Deployment page component with environment selector (DEV/QA/PROD)
- Revisions section: lists revisions with deploy button (disabled for QA/PROD)
- Versions section: lists versions with deploy button (enabled for all environments)
- Deploy action calling adapter `deployMapping()`
- Success/error feedback
- Route addition for `/mappings/:id/deploy`

### Out of Scope

- Promotion and rollback UI (T-06)
- Badges on other pages (T-07)
- Deployment history view (T-06)

---

## Relevant Areas

- `ui/src/features/deployments/` (new)
- `ui/src/routes/paths.ts`
- `ui/src/App.tsx` (route registration)
- `ui/src/features/mappings/components/` (link to deploy page)

---

## Change Requirements

- Create `DeploymentPage` component with environment tab/selector
- Fetch revisions via existing adapter methods, versions via adapter methods
- Render two list sections with appropriate deploy button state per environment
- Call `deployMapping()` on deploy action; show toast on success
- Add route at `/mappings/:id/deploy`
- Link from editor (e.g., in header or toolbar)

---

## Verification Requirements

- Component tests: DEV target shows both sections enabled; QA target disables revision deploy buttons
- Deploy action calls adapter with correct parameters
- `tsc --noEmit` passes
- Lint passes

---

## Acceptance Checks

- Deployment page renders with environment selector
- DEV: both Revisions and Versions sections have active deploy buttons
- QA/PROD: Revisions section deploy buttons disabled or section hidden
- Successful deploy shows confirmation feedback
- Route navigable from editor
- `tsc --noEmit` and lint pass

---

## Blockers / Risks

- none

---

## Notes

- Follow existing feature module conventions (barrel export, hooks/, components/ structure)
- Reuse existing list/card patterns from project overview
