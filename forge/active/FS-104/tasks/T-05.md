# TASK

## Title

Add objectFields canonical-pattern decomposition and guided reopen restoration

---

## ID

T-05

---

## Spec

FS-104

---

## Spec Rev

Rev: 2

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- T-04

---

## Implements

- `AE-07`
- `AE-08`
- `AE-09`

---

## Summary

Extend `array-decomposer.ts` to recognize exactly KeyRa-generated objectFields canonical expression pattern and restore full guided state (parent path, ordered keys, inclusion, item recipe). Preserve safe Advanced Editor fallback for unrecognized variants.

---

## Scope

### In Scope

- Add canonical objectFields detection branch in decomposer.
- Restore objectFields collection state from canonical AST pattern.
- Restore item field mappings (including fixed values and Smart Builder-compatible expression forms).
- Keep strict recognition scope to exact KeyRa-generated pattern variants only.

### Out of Scope

- Broad semantic matching for arbitrary handwritten equivalent expressions.
- Rewriting non-canonical expressions.

---

## Relevant Areas

- `ui/src/features/mappings/lib/array-decomposer.ts`
- `ui/src/features/mappings/lib/array-decomposer.test.ts`
- `ui/src/features/mappings/hooks/use-array-builder-state.ts` (hydration/fallback glue as needed)

---

## Change Requirements

- Guided reopen round-trip must preserve:
  - parent object selection,
  - selected child keys/order,
  - inclusion settings,
  - item recipe mappings/refinements.
- On failed recognition, preserve raw DSL and existing warning path; no destructive conversion.

---

## Verification Requirements

- Decomposer tests for canonical success and restored order/state.
- Round-trip tests: state → DSL → decompose → equivalent state.
- Tests for structurally modified expressions that intentionally fall back.

---

## Acceptance Checks

- Canonical objectFields expression decomposes successfully.
- Reopen restores key ordering and field recipe settings losslessly.
- Unrecognized expressions stay Advanced Editor with fallback behavior unchanged.

---

## Blockers / Risks

- Overly permissive matching may incorrectly “recognize” unsafe modified expressions.

---

## Notes

- Keep decomposition narrowly pattern-based for Rev 1 safety.
