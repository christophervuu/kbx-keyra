# TASK

## Title

Expand automated test coverage for caching, stale revalidation, deduplication, and adapter parity

---

## ID

T-08

---

## Spec

FS-103

---

## Spec Rev

Rev: 2

---

## Agent

ui-task

---

## Status

todo

---

## Depends On

- T-03
- T-04
- T-05
- T-06
- T-07

---

## Implements

- `AE-01`
- `AE-02`
- `AE-03`
- `AE-04`
- `AE-06`
- `AE-07`
- `AE-08`
- `AE-09`
- `AE-10`
- `AE-11`
- `AE-12`
- `AE-13`
- `AE-14`
- `AE-15`
- `AE-16`
- `AE-17`
- `AE-18`
- `AE-19`

---

## Summary

Add deterministic unit/integration/E2E coverage proving query cache behavior, stale-while-revalidate UX, request deduplication, mutation invalidation, sensitive-data exclusions, backend switch cache clearing, and parity across adapter modes.

---

## Scope

### In Scope

- Unit tests for key factory and query policy defaults.
- Integration tests with mocked adapters/request spies for request-count assertions.
- UI acceptance tests for initial vs cached return vs refresh error rendering.
- Adapter-parity E2E additions in `tests/e2e` for cache behavior in `localStorage` and `httpBackend` modes.

### Out of Scope

- Broad unrelated test refactors.

---

## Relevant Areas

- `ui/src/**/*.test.tsx`
- `ui/src/**/*.test.ts`
- `tests/e2e/specs/*`
- `tests/e2e/fixtures/*`
- `tests/e2e/mock-server/*`

---

## Change Requirements

- Tests assert exact adapter request counts for dedupe scenarios.
- Tests differentiate initial error vs refresh error rendering.
- Tests assert cache clear on backend context switch.
- Tests validate Mapping Editor unsaved overwrite protection and sensitive-data exclusion behavior.
- Tests verify no concurrent duplicate adapter requests for same canonical query key under Strict Mode behavior.
- Tests verify metadata-only mutation responses invalidate/refetch canonical detail instead of speculative merge.

---

## Verification Requirements

- Full UI test suite pass.
- E2E parity run pass in both adapter modes.
- Typecheck/lint/build pass for touched areas.

---

## Acceptance Checks

- Automated coverage exists for all FS-103 acceptance examples.
- Request spy tests prove one request for concurrent identical consumers.
- Cached return navigation tests prove no full initial skeleton regressions.
- Adapter parity tests validate equivalent cache semantics in both modes.

---

## Blockers / Risks

- Flaky timing-dependent tests if refresh windows are not controlled deterministically.

---

## Notes

- Use deterministic fake timers and query client test helpers to avoid nondeterministic refresh behavior.
