# TASK

## Title

Add end-to-end Explain Rule verification coverage across valid, invalid, and non-mutation scenarios

---

## ID

T-03

---

## Spec

FS-069

---

## Spec Rev

Rev: 1

---

## Agent

task

---

## Status

todo

---

## Depends On

- T-01
- T-02

---

## Implements

- `AE-01`
- `AE-02`
- `AE-03`
- `AE-05`

---

## Summary

Create integrated verification coverage that exercises Explain Rule behavior from request contract through UI-visible outcomes, with explicit checks for valid explanation, invalid DSL handling, canonical contract path confidence, and non-mutation guarantees.

---

## Scope

### In Scope

- Add/adjust integration-style tests (backend and/or adapter-layer) validating explain-route contract behavior across success and failure conditions.
- Ensure at least one verification path asserts rule-state immutability around explain action.
- Ensure verification references acceptance examples directly.

### Out of Scope

- New feature implementation beyond test coverage.
- Non-Explain AI feature tests.

---

## Relevant Areas

- `tests/lambda/ai/explain-rule.test.ts`
- `ui/src/lib/api/http-adapter.test.ts`
- `ui/src/features/mappings/hooks/use-explain-rule.test.ts`
- `ui/src/features/mappings/components/ScalarFieldBuilder.test.tsx`
- `tests/* ?` (integration location as applicable)

---

## Change Requirements

- Verification suite must cover:
  - valid explain success,
  - invalid DSL graceful behavior/failure semantics,
  - no mutation of expression/mapping state,
  - prompt/structured-contract path confidence signals.
- Test naming and assertions should be deterministic and map back to AE IDs.

---

## Verification Requirements

- Execute targeted backend and UI tests covering explain-rule flows.
- Run `pnpm typecheck` in touched workspaces.
- Document any temporary gaps if blocked by parallel FS-066/067/068 changes.

---

## Acceptance Checks

- Automated tests explicitly cover `AE-01`, `AE-02`, `AE-03`, `AE-05`.
- Invalid DSL behavior is asserted as graceful explanation or useful normalized failure (`AE-02`).
- At least one test asserts no change in rule expression/state before vs after explain (`AE-03`).
- Targeted test suites and typecheck pass for touched areas.

---

## Blockers / Risks

- Test instability if parallel specs alter explain response contract during execution.

---

## Notes

- Keep assertions at behavioral contract level to reduce brittleness while preserving deterministic checks.
