# TASK

## Title
Update EditorTopBar for draft model (unsaved badge, view changes trigger, save behavior)

---

## ID
T-11

---

## Spec
FS-039

---

## Spec Rev
Rev: 1

---

## Agent
ui-task

---

## Status
todo

---

## Depends On
T-04, T-10

---

## Implements
AE-05, AE-06

---

## Summary
Update `EditorTopBar` to reflect the new draft model: replace the old `unsavedCount` badge with a `unsavedChangeCount` badge derived from the draft rules map, add a "View unsaved changes" button that opens the `UnsavedChangesOverlay`, and ensure Save button behavior works with the new draft-merging save flow.

---

## Scope

### In Scope
- Replace `unsavedCount` prop with `unsavedChangeCount: number`
- Add `onViewUnsavedChanges` callback prop
- "View unsaved changes" button: visible when `unsavedChangeCount > 0`, opens overlay
- Badge displays number of changed fields (not rule count)
- Save button: disabled when `unsavedChangeCount === 0` or `saveStatus === 'saving'`
- Save button: calls `onSave` (existing prop, behavior unchanged)
- `saveStatus` display unchanged

### Out of Scope
- Internal overlay rendering (T-10)
- Draft map implementation (T-04)

---

## Relevant Areas
- `ui/src/features/mappings/components/EditorTopBar.tsx`
- `ui/src/routes/pages/MappingEditor.tsx` (composition wiring)

---

## Change Requirements
Update `EditorTopBar` props: remove `unsavedCount`, add `unsavedChangeCount: number` and `onViewUnsavedChanges: () => void`. Render a "View changes" button (with an eye or diff icon) next to the save button when `unsavedChangeCount > 0`. Clicking it fires `onViewUnsavedChanges`. The badge on this button (or next to it) shows the count. Save button remains as-is but its disabled state now keys off `unsavedChangeCount === 0`.

---

## Verification Requirements
- "View changes" button appears when unsavedChangeCount > 0
- "View changes" button hidden when unsavedChangeCount === 0
- Badge shows correct count
- Save button disabled when count is 0
- Save button enabled when count > 0
- TypeScript compiles, lint passes

---

## Acceptance Checks
- `EditorTopBar` uses new prop contract
- "View changes" button appears/disappears correctly
- Save disabled/enabled logic correct
- TypeScript strict compiles cleanly
- Lint passes

---

## Blockers / Risks
none

---

## Notes
The composition layer in `MappingEditor.tsx` will need to wire `unsavedChangeCount` from `useMappingEditor` and manage the overlay open/close state.
