# TASK

## Title

Implement async readiness orchestration, CDM sync invariants, audit events, and end-to-end verification suite

---

## ID

T-12

---

## Spec

FS-105

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-04
- T-07
- T-09
- T-10
- T-11

---

## Implements

- `AE-13`
- `AE-18`
- `AE-24`
- `AE-30`

---

## Summary

Finalize FS-105 system behavior by implementing async post-version readiness orchestration and CDM re-sync invariants, then validate the full lifecycle with audit/activity coverage and comprehensive automated verification.

---

## Scope

### In Scope

- Implement/verify async post-processing orchestration for:
  - index generation,
  - impact materialization,
  - sample compatibility recalculation.
- Ensure status-separation semantics (`versionStatus` vs downstream statuses).
- Enforce CDM re-sync behavior as immutable new-version creation only.
- Add schema lifecycle and mapping-upgrade audit/activity event set.
- Add/expand automated tests across layers:
  - backend,
  - persistence,
  - adapters,
  - migration,
  - e2e,
  - performance targets.

### Out of Scope

- New product analytics dashboards.

---

## Relevant Areas

- `src/lambda/schema/*`
- `src/lambda/*` event emission points
- `src/lib/persistence/*`
- `template.yaml` + orchestration resources
- `ui` + `tests/e2e/*`

---

## Change Requirements

- Event payloads include schema/version/revision/mapping/project/actor/timestamp and affected-count metadata where applicable.
- Verification suite explicitly covers migration parity and CDM read-only enforcement.
- Performance checks align with FS-105 targets.

---

## Verification Requirements

- Full automated test run for touched suites.
- E2E scenario validating sequence: create/edit/version/use/upgrade/archive.
- Orchestration tests showing index failure does not invalidate immutable ready version.

---

## Acceptance Checks

- CDM re-sync generates immutable new versions without mutable-edit path.
- Downstream index/impact/sample status failures do not demote immutable version readiness.
- Required event types are emitted and test-validated.
- E2E path validates explicit upgrade and non-GitHub user-schema lifecycle behavior.
- Performance checks report against FS-105 thresholds.

---

## Blockers / Risks

- Test runtime may increase substantially; ensure selective fixtures and deterministic data setup.

---

## Notes

- This is the final integration/verification gate task for FS-105.
