# TASK

## Title

Implement inferred issue aggregation and mark-reviewed schema transitions

---

## ID

T-02

---

## Spec

FS-090

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01

---

## Implements

- AE-04
- AE-05

---

## Summary

Add backend logic for calculating inferred review issue summaries and applying explicit mark-reviewed transitions. This enables deterministic review panel counts and status updates.

---

## Scope

### In Scope

- Define/implement issue aggregation from field-level inference metadata.
- Add service/API action for Mark as reviewed with confirmation-safe state transition.
- Ensure transition preserves inferred lineage and only promotes to ready when non-blocking.

### Out of Scope

- UI interaction wiring for issue drilldown.
- Full sample diff engine.

---

## Relevant Areas

- `src/lambda/schema/*`
- `src/lib/persistence/schema-metadata.ts`
- `src/lib/persistence/schema-nodes.ts` ?
- `ui/src/lib/api/http-adapter.ts` (client contract only, if needed for endpoint call)

---

## Change Requirements

- Provide deterministic issue categories/count payload consumed by review panel.
- Implement mark-reviewed transition that records `reviewedAt` and updates `reviewState/status` according to spec rules.
- Maintain safe handling when blocking schema errors remain.

---

## Verification Requirements

- Unit tests for issue aggregation scenarios.
- Unit/integration tests for mark-reviewed transitions with and without blocking errors.
- Lint/typecheck/tests for touched backend packages.

---

## Acceptance Checks

- Review issue summary payload can be produced for inferred schemas with known warning categories.
- Mark-reviewed transition updates fields exactly per contract and preserves source lineage.
- Tests cover unresolved-nonblocking and blocking-error paths.

---

## Blockers / Risks

- Warning derivation capabilities may be limited by current inference metadata granularity.

---

## Notes

- If full per-field warning coverage is unavailable, return best-effort counts and document unsupported categories.
