# TASK

## Title

Redesign assistive action row with updated semantics

---

## ID

T-04

---

## Spec

FS-040

---

## Spec Rev

Rev: 1

---

## Agent

ui-task

---

## Status

todo

---

## Depends On

- T-01

---

## Implements

- `AE-07`
- `AE-08`
- `AE-10`

---

## Summary

Redesign the Builder action row at the bottom of `ScalarFieldBuilder`. Rename "Clear" to "Reset draft" with confirmation behavior for non-trivial expressions. Rename the destructive "Clear mapping" to "Remove mapping" and reposition it as a secondary action. Update AI placeholder button tooltips with descriptive future-state text. Update all ARIA attributes and test IDs.

---

## Scope

### In Scope

- Rename "Clear" button label to "Reset draft" with `RotateCcw` icon (from Lucide).
- "Reset draft" behavior:
  - When expression is trivial (empty or a bare `source("...")` with no transforms): reset immediately, no confirmation.
  - When expression is non-trivial: show a confirmation prompt ("Reset draft? Your current expression will be cleared.") before resetting.
  - Reset action: set expression to empty string, reset builder state to default empty Value mode.
  - Enabled when expression is non-empty OR builder state is dirty.
- Rename "Clear mapping" (red destructive button) to "Remove mapping".
- Reposition "Remove mapping" below the main action row as a secondary text-link-style action (smaller, less prominent) to reduce confusion with "Reset draft".
- Add a confirmation dialog for "Remove mapping": "Remove mapping for {targetPath}? This will delete the saved rule." with confirm/cancel.
- Update AI action tooltips from `AI_COMING_SOON` to descriptive per-action text:
  - Suggest: "AI-powered expression suggestions — available in a future release"
  - Explain: "AI-powered explanation — available in a future release"
  - Fix: "AI-powered fix suggestions — available in a future release"
- Update `aria-disabled="true"` and add descriptive `aria-label` to each AI action button.
- Update `data-testid` values:
  - `clear-mapping-btn` → `remove-mapping-btn`
  - Add `reset-draft-btn`
  - Keep `ai-suggest-btn`, `ai-explain-btn`, `ai-fix-btn`
- Ensure keyboard accessibility for all buttons.
- Ensure "Remove mapping" confirmation uses `role="alertdialog"`.

### Out of Scope

- Actual AI endpoint integration.
- Changes to Apply/Save button gating (T-03).
- Pinned feedback area (T-02).
- Unsaved diff (T-05).

---

## Relevant Areas

- `ui/src/features/mappings/components/ScalarFieldBuilder.tsx` (modified — action row section)
- `ui/src/features/mappings/components/ScalarFieldBuilder.test.tsx` (updated)

---

## Change Requirements

1. **Reset draft button:**
   - Icon: `RotateCcw` (Lucide).
   - Label: "Reset draft"
   - `data-testid="reset-draft-btn"`
   - `aria-label="Reset current draft expression"`
   - Enabled when `expression.trim().length > 0` or builder state is dirty.
   - Confirmation logic:
     - Trivial check: expression matches `/^source\("[^"]*"\)$/` (bare source reference) or is empty → no confirmation.
     - Non-trivial: show inline confirmation (a prompt bar or small dialog). Use a simple state toggle (`confirmingReset`) that shows "Reset draft?" + Confirm/Cancel inline.
   - On confirm or immediate reset: call `handleExpressionChange('')`, reset builder state (this may need a new `onReset` callback from `UnifiedExpressionBuilder` or simply trigger via `key` prop change on the builder).

2. **Remove mapping button:**
   - Only shown when `currentStatus === 'mapped'` and `onClearMapping` is provided (same visibility rule as current "Clear").
   - Label: "Remove mapping"
   - Style: secondary text-link style, smaller font, positioned below the main action row (e.g., a separate div below the main button row).
   - `data-testid="remove-mapping-btn"`
   - `aria-label="Remove saved mapping for {targetPath}"`
   - On click: open a confirmation dialog (`role="alertdialog"`):
     - Title: "Remove mapping"
     - Message: "Remove mapping for {targetPath}? This will delete the saved rule."
     - Actions: "Remove" (destructive, red) + "Cancel"
   - On confirm: call `onClearMapping(selectedTargetPath)`.

3. **AI action buttons:**
   - Tooltips updated to per-action descriptive text.
   - Each button gets `aria-label="{action} — {tooltip text}"`.
   - Visual appearance remains the same (disabled, muted).

4. **Action row layout change:**
   ```
   Main row: [Suggest] [Explain] [Fix] | [Reset draft] | <spacer> | [Next unmapped →] [Apply]
   Secondary row (below, conditional): Remove mapping
   ```

---

## Verification Requirements

- Component tests:
  - "Reset draft" button is visible when expression is non-empty.
  - Clicking "Reset draft" on a trivial expression resets immediately.
  - Clicking "Reset draft" on a non-trivial expression shows confirmation prompt.
  - Confirming reset clears expression and builder state.
  - Canceling reset preserves expression.
  - "Remove mapping" is visible only when `currentStatus === 'mapped'`.
  - Clicking "Remove mapping" shows confirmation dialog with `role="alertdialog"`.
  - Confirming "Remove mapping" calls `onClearMapping`.
  - AI buttons have updated tooltips and `aria-label` attributes.
- `pnpm tsc --noEmit` passes.
- `pnpm lint` passes.

---

## Acceptance Checks

- "Clear" button is renamed to "Reset draft" with `RotateCcw` icon
- Reset draft shows confirmation for non-trivial expressions (AE-07)
- "Clear mapping" is renamed to "Remove mapping" and repositioned as secondary action
- "Remove mapping" shows confirmation dialog (AE-10)
- AI buttons have descriptive tooltips (AE-08)
- All buttons have correct ARIA attributes
- All test IDs updated
- Tests pass
- `pnpm tsc --noEmit` clean
- `pnpm lint` clean
- `forge/architecture/project-structure.md` updated if new files or folders were created

---

## Blockers / Risks

- The "Reset draft" confirmation UX needs to be lightweight enough not to disrupt flow. An inline prompt (not a modal dialog) is recommended to keep the interaction fast.

---

## Notes

- The old `clear-mapping-btn` test ID is replaced by `remove-mapping-btn`. Any E2E tests (FS-019) that reference the old test ID will need updating. Check `tests/e2e/` for references.
- The `SuggestionPill` component and related imports may have already been removed in T-02/T-03. If they are still present, this task should not touch them — that's T-03's responsibility.
