# TASK

## Title

Create persistence-model architecture document and update project-structure

---

## ID

T-09

---

## Spec

FS-058

---

## Spec Rev

Rev: 1

---

## Agent

task

---

## Status

todo

---

## Depends On

- none

---

## Implements

- `AE-11`
- `AE-12`

Indirectly supports all AEs by documenting the architectural decisions.

---

## Summary

Create `forge/architecture/persistence-model.md` documenting the Phase 1 DynamoDB table schemas, S3 object layout conventions, access patterns, metadata-vs-blob rules, versioning behavior, and the `src/lib/persistence/` module boundaries. Update `forge/architecture/INDEX.md` to add the new document and update `forge/architecture/project-structure.md` to include the new `src/lib/persistence/` directory.

---

## Scope

### In Scope

- `forge/architecture/persistence-model.md` (new)
- `forge/architecture/INDEX.md` (update — add row)
- `forge/architecture/project-structure.md` (update — add `src/lib/persistence/` section)

### Out of Scope

- Code implementation (T-01 through T-08)
- IaC documentation
- OpenSearch documentation (covered by FS-056's architecture document)

---

## Relevant Areas

- `forge/architecture/persistence-model.md` (new)
- `forge/architecture/INDEX.md` (update)
- `forge/architecture/project-structure.md` (update)

---

## Change Requirements

- Create `forge/architecture/persistence-model.md` with the following sections:
  1. **Purpose and scope** — what this document covers
  2. **DynamoDB table definitions** — all 5 tables with key schemas, GSIs, attribute descriptions
  3. **S3 object layout** — key patterns, content types, lifecycle
  4. **Access patterns** — table of all supported access patterns mapped to DynamoDB operations
  5. **Metadata vs blob rules** — when to use DynamoDB vs S3
  6. **Versioning model** — mapping version auto-increment, snapshot storage, prune behavior
  7. **Module architecture** — `src/lib/persistence/` structure, public API, client configuration
  8. **Environment configuration** — env vars, local development endpoints
  9. **Constraints and limits** — 400KB item limit, BatchWriteItem 25-item limit, etc.
  10. **Cross-references** — links to FS-056, FS-057, product spec Section 15

- Update `forge/architecture/INDEX.md`:
  - Add row: `[persistence-model.md](./persistence-model.md)` | Phase 1 persistence layer — DynamoDB table schemas, S3 object layout, access patterns, metadata-vs-blob rules, versioning model, `src/lib/persistence/` module architecture | 2026-05-14

- Update `forge/architecture/project-structure.md`:
  - Add `src/lib/persistence/` section under the `src/lib/` area with subdirectory listing

---

## Verification Requirements

- `persistence-model.md` exists and contains all 10 sections
- `INDEX.md` has the new row
- `project-structure.md` includes `src/lib/persistence/` with accurate subdirectory listing
- No broken cross-references
- Document is consistent with the spec's table definitions and S3 patterns

---

## Acceptance Checks

- `forge/architecture/persistence-model.md` exists with all required sections
- `forge/architecture/INDEX.md` updated with new entry and correct date
- `forge/architecture/project-structure.md` updated with `src/lib/persistence/` directory structure
- All table definitions in the architecture doc match the spec
- S3 key patterns in the architecture doc match the spec

---

## Blockers / Risks

- none

---

## Notes

- This task can be executed in parallel with any other task since it only writes to `forge/` files.
- The architecture document should be authoritative for the persistence layer — if FS-056 or FS-057 have inconsistencies with this document, this document wins (as the dedicated storage model reference).
- Keep the document concise and focused on decisions and contracts, not implementation details.
