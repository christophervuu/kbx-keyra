# TASK

## Title

Update ui-application.md architecture document for Schema Detail page

---

## ID

T-09

---

## Spec

FS-015

---

## Spec Rev

Rev: 1

---

## Agent

task

---

## Status

done

---

## Depends On

- T-02
- T-04
- T-05

---

## Implements

- (architecture maintenance — no specific AE)

---

## Summary

Update `forge/architecture/ui-application.md` to document the Schema Detail page architecture: the editing pattern, hook contracts, and the schemas feature module's expanded structure. Update `INDEX.md` last-updated date.

---

## Scope

### In Scope

- Add a "Schema Detail Page Architecture" section to `forge/architecture/ui-application.md`
- Document:
  - Page composition pattern (route page → feature page component → sections)
  - `useSchemaDetail` hook contract
  - `useSchemaEditor` hook contract and editing state machine
  - `useSchemaUsage` hook contract
  - Tree editing pattern (immutable operations, reconstruct, save)
  - Action visibility rules
- Update the schemas feature module file listing to reflect new components/hooks/lib files
- Update `INDEX.md` last-updated date for `ui-application.md`

### Out of Scope

- Creating a new architecture document (the existing `ui-application.md` is the right home)
- Modifying any source code

---

## Relevant Areas

- `forge/architecture/ui-application.md` — document to update
- `forge/architecture/INDEX.md` — date update

---

## Change Requirements

- Add section after "Mapping Editor Architecture" (or as a peer section):

  ```
  ## Schema Detail Page Architecture
  ```

  Content should cover:
  - Page composition: route-level thin wrapper + `SchemaDetailPage` feature component
  - Hook architecture:
    - `useSchemaDetail(schemaId)` — data loading, parsing, error handling
    - `useSchemaEditor(parsedSchema, schemaId)` — edit mode state, local tree copy, operation dispatch, save
    - `useSchemaUsage(schemaId)` — usage derivation across projects/mappings
  - Editing pattern:
    - Immutable tree operations in `schema-editor-ops.ts`
    - Tree-to-JSON-Schema reconstruction in `tree-to-json-schema.ts`
    - Local edits accumulated until explicit save
    - Save flow: reconstruct → updateSchema → re-parse → exit edit mode
  - Action visibility matrix (origin × scope × format)
  - Direct localStorage usage for UI preferences (banner dismissal) vs. adapter for domain data

- Update `INDEX.md`: change `ui-application.md` last-updated to `2026-05-02` (or current date at execution time)

---

## Verification Requirements

- Architecture document accurately reflects implemented patterns
- `INDEX.md` date is current
- No broken markdown formatting
- Section headings follow existing document conventions

---

## Acceptance Checks

- `forge/architecture/ui-application.md` has a Schema Detail Page section
- Hook contracts are documented
- Editing pattern (immutable ops + reconstruction) is described
- `INDEX.md` last-updated date reflects the update
- Document is well-formatted markdown

---

## Blockers / Risks

- This task should be executed after T-02, T-04, T-05 are complete so the architecture description reflects actual implementation.

---

## Notes

Execute this task last. The architecture document should reflect what was actually built, not just what was planned. If implementation deviated from the spec during T-02–T-08, capture the actual patterns here.
