# TASK

## Title

Update ui-application.md architecture for expression builder

---

## ID

T-12

---

## Spec

FS-011

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

todo

---

## Depends On

- T-01

---

## Implements

- (Architecture documentation — enables future specs)

---

## Summary

Update `forge/architecture/ui-application.md` to document the expression builder architecture: the dual-mode pattern (builder/editor), new hooks (`useExpressionBuilder`, `useExpressionPreview`), engine `parse()`/`evaluate()` integration pattern, function catalog pattern, and the selected-rule state flow between Panel 3 and Panel 4.

---

## Scope

### In Scope

- Update the "Module Structure" section with new files:
  - `ui/src/features/mappings/components/ExpressionBuilderPanel.tsx`
  - `ui/src/features/mappings/components/RawDslEditor.tsx`
  - `ui/src/features/mappings/components/GuidedBuilder.tsx`
  - `ui/src/features/mappings/components/ExpressionPreview.tsx`
  - `ui/src/features/mappings/components/FunctionReferencePanel.tsx`
  - `ui/src/features/mappings/components/AutocompleteDropdown.tsx`
  - `ui/src/features/mappings/hooks/use-expression-builder.ts`
  - `ui/src/features/mappings/hooks/use-expression-preview.ts`
  - `ui/src/features/mappings/hooks/use-dsl-autocomplete.ts`
  - `ui/src/features/mappings/hooks/use-dsl-validation.ts`
  - `ui/src/features/mappings/lib/dsl-tokenizer.ts`
  - `ui/src/features/mappings/lib/function-catalog.ts`
  - `ui/src/features/mappings/lib/expression-generator.ts`
  - `ui/src/features/mappings/lib/ast-decomposer.ts`
  - `ui/src/features/mappings/lib/autocomplete-utils.ts`
- Update "Engine Integration" section:
  - Document `parse()` re-export and usage pattern for inline validation
  - Document `evaluate()` re-export and usage pattern for expression preview (single-expression evaluation)
  - Document `defaultRegistry` re-export and function metadata access
  - Add `useExpressionPreview()` as a second canonical engine hook pattern
- Add new section: "Expression Builder Architecture" under "Mapping Editor Architecture":
  - Dual-mode pattern: builder (step-by-step) and editor (raw text)
  - Mode toggle with bidirectional conversion (decomposition logic)
  - Expression state flow: selectedRuleIndex → load expression → edit → debounced commit back
  - Guided builder step model (source → transform → arguments → preview)
  - Raw editor overlay pattern (textarea + highlight overlay + error decorations)
  - Autocomplete context detection pattern
  - Function catalog static data pattern
  - Integration points: Panel 3 selected rule, Panel 1 source tree click
- Update "Editor Data Flow" section:
  - Add expression builder state flow to the diagram
  - Document `selectedRuleIndex` state ownership (page-level vs hook-level)
  - Document how expression updates flow: expression builder → debounced updateRule → validation re-triggers
- Update barrel export documentation if needed

### Out of Scope

- Updating other architecture documents (mapping-engine.md, project-structure.md)
- Changing code — this is documentation only
- Documenting FS-012 integration (that spec will update architecture when it lands)

---

## Relevant Areas

- `forge/architecture/ui-application.md` — primary file to update
- `forge/architecture/INDEX.md` — update "Last Updated" date for ui-application.md

---

## Change Requirements

- Add "Expression Builder Architecture" subsection under "Mapping Editor Architecture" in `ui-application.md`
- Document:
  - Component hierarchy: `ExpressionBuilderPanel` → mode toggle → `RawDslEditor` | `GuidedBuilder` + `FunctionReferencePanel` + `ExpressionPreview`
  - Hook contracts: `useExpressionBuilder()` inputs/outputs, `useExpressionPreview()` inputs/outputs
  - Engine integration: parse() for validation, evaluate() for preview, defaultRegistry for function metadata
  - State flow diagram: rule selection → expression load → edit → validate → commit (debounced)
  - Mode toggle rules: builder→editor (trivial), editor→builder (decomposition + fallback)
  - Integration contract: `selectedRuleIndex` flows from page state, `onSelectNode` from schema tree
- Update Module Structure tree with new files
- Update Engine Integration section with parse/evaluate/registry patterns
- Update `INDEX.md` Last Updated date

---

## Verification Requirements

- `forge/architecture/ui-application.md` contains the new "Expression Builder Architecture" section
- Module Structure accurately reflects new files
- Engine Integration section documents parse/evaluate/registry usage
- `INDEX.md` Last Updated date is current
- No markdown formatting errors (headings, tables, code blocks)

---

## Acceptance Checks

- `ui-application.md` updated with expression builder architecture
- New hooks documented with contract summaries
- Engine parse/evaluate/registry integration documented
- Module Structure tree is accurate and complete for FS-011 additions
- `INDEX.md` date updated
- Document is internally consistent with existing sections

---

## Blockers / Risks

- none

---

## Notes

- This task can run relatively early (after T-01 establishes the architectural decisions) — it documents the planned architecture, which subsequent tasks implement
- If implementation decisions diverge from what's documented here during T-02–T-11 execution, the implementing task should update the architecture document as part of its completion
- Follow the existing documentation style and heading hierarchy in ui-application.md
