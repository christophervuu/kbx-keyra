# TASK

## Title

Create DeploymentSummaries projection persistence and update pipeline

---

## ID

T-02

---

## Spec

FS-106

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01

---

## Implements

- AE-01
- AE-07

---

## Summary

Implement control-plane projection table and update flows that power correct global/project deployment filtering, sorting, and pagination over the full mapping population.

---

## Scope

### In Scope

- Add `integrations-keyra-deployment-summaries` table and required indexes.
- Add projection row upsert/update logic for all required trigger events.
- Add mismatch marker logic for projection-update failures.

### Out of Scope

- Aggregate GET handler HTTP endpoints (handled in T-03).

---

## Relevant Areas

- `src/lib/persistence/deployment-summaries.ts` (new)
- `src/lib/persistence/types.ts`
- `src/lambda/deployment/*` (projection update integration)
- `template.yaml`
- `tests/lib/persistence/*deployment-summaries*`

---

## Change Requirements

- Implement projection schema fields and index keys from spec.
- Initialize projection rows for never-deployed mappings.
- Update projection on mapping create/version create/deploy/promote/rollback/retry/failure/delete/archive/reconcile.

---

## Verification Requirements

- Unit tests for projection derivation and field updates.
- Integration tests for event-triggered projection updates.
- Failure-path tests where projection update fails after runtime success.

---

## Acceptance Checks

- Projection table and indexes exist and are queryable.
- Projection updates happen for all listed trigger classes.
- Projection failures are detectable and recoverable.

---

## Blockers / Risks

- Projection write path must stay idempotent under retries.

---

## Notes

- Projection is eventually consistent UI read model, never runtime authority.
