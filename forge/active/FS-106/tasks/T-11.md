# TASK

## Title

Apply plane-specific infrastructure naming and cross-account AWS_IAM invocation model

---

## ID

T-11

---

## Spec

FS-106

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01

---

## Implements

- AE-12

---

## Summary

Update infrastructure and runtime/control-plane integration to plane-qualified resource naming and selected cross-account transport model: assume-role + runtime API Gateway `AWS_IAM` + SigV4 + bounded direct payload push.

---

## Scope

### In Scope

- Plane-qualified Lambda and DynamoDB physical naming updates.
- Runtime API authorization mode `AWS_IAM` and SigV4 invocation setup.
- Environment-specific runtime role assumption policies.
- DEV/PREPROD/PROD greenfield resource layout and bucket names.

### Out of Scope

- Deferred transport/security patterns (presigned pull, PrivateLink, etc.).

---

## Relevant Areas

- `template.yaml`
- runtime invoke client modules
- deployment environment config modules
- IAM policy templates/resources
- infrastructure tests

---

## Change Requirements

- Ensure no Lambda/DynamoDB names include DEV/PREPROD/PROD suffixes.
- Ensure control-plane and runtime responsibilities are distinguishable by name.
- Implement selected V1 cross-account invocation boundary and keep 5MB payload cap.

---

## Verification Requirements

- Infrastructure tests for naming patterns and plane distinction.
- Integration tests for successful/denied AWS_IAM cross-account invocation.

---

## Acceptance Checks

- Resource names distinguish control-plane and runtime responsibilities.
- Runtime API calls require assume-role + SigV4 and succeed only with least-privilege permissions.
- S3 buckets match required environment-specific names.

---

## Blockers / Risks

- Resource renaming may require careful stack update sequencing.

---

## Notes

- Runtime resource names are reused across runtime accounts, not environment-suffixed.
