# TASK

## Title

Implement global and project deployment overview UI with projection-backed filters

---

## ID

T-09

---

## Spec

FS-106

---

## Spec Rev

Rev: 2

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- T-03

---

## Implements

- AE-01
- AE-12

---

## Summary

Deliver top-level `/deployments` and completed project deployments pages using shared components and projection-backed APIs, including full-population filters, deterministic sorting, pagination, and drill-down behavior.

---

## Scope

### In Scope

- Add `/deployments` route and primary navigation entry.
- Replace project page placeholder with full implementation.
- Shared deployment table/status components for global and project pages.
- Filter/search/pagination/summaries using backend projection data.

### Out of Scope

- Mapping-level mutation actions in overview tables.

---

## Relevant Areas

- `ui/src/App.tsx`
- `ui/src/routes/paths.ts`
- `ui/src/routes/pages/ProjectDeployments.tsx`
- `ui/src/routes/pages/Deployments.tsx` (new)
- `ui/src/features/deployments/components/*`
- `ui/src/features/deployments/hooks/*`
- `ui/src/lib/api/http-adapter.ts`
- `ui/src/lib/query/*`
- `ui/src/features/deployments/**/*.test.tsx`

---

## Change Requirements

- One row per mapping, including never deployed.
- Default sort by lastActivityAt desc then names asc.
- Include failed/attention filters and summary counts.
- Remove SANDBOX/QA/STAGING labels/options from these pages.

---

## Verification Requirements

- UI tests for filter/sort/pagination correctness and drill-down.
- Accessibility tests for status indicators/table interactions.

---

## Acceptance Checks

- Global/project pages are overview + drill-down only.
- Filtering and sorting remain correct across paginated data.
- Project page no longer shows placeholder.

---

## Blockers / Risks

- Backend API contract changes must be stable before UI merge.

---

## Notes

- Share status components and row rendering between global and project pages.
- Verification (2026-07-07):
  - `pnpm --dir ui exec vitest run src/features/deployments/hooks/use-deployment-overview.test.tsx src/features/deployments/components/DeploymentOverviewPage.test.tsx src/routes/routes.test.tsx src/components/layout/layout.test.tsx src/lib/api/http-adapter.test.ts src/lib/query/query-keys.test.ts src/lib/query/query-invalidation.test.ts` ✅
  - `pnpm --dir ui typecheck` ✅
