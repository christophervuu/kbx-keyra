# TASK

## Title

Adopt hardened accept/edit/dismiss workflow across Phase 2 AI feature surfaces

---

## ID

T-03

---

## Spec

FS-074

---

## Spec Rev

Rev: 2

---

## Agent

ui-task

---

## Status

todo

---

## Depends On

- T-01
- T-02

---

## Implements

- `AE-01`
- `AE-02`
- `AE-04`
- `AE-05`

---

## Summary

Integrate the hardened review contract into active Phase 2 AI features so each surface uses explicit accept/edit/dismiss behavior and preserves the no-auto-commit rule.

---

## Scope

### In Scope

- Feature-surface adoption for Suggest Expression, Smart Fix, Auto-Map suggestions, and other active AI suggestion UIs in scope.
- Explicit adoption across all in-scope Phase 2 AI surfaces: FS-069, FS-070, FS-071, FS-072, FS-073.
- Explicit action wiring for Accept/Edit/Dismiss semantics.
- Lifecycle behavior alignment on generate/refresh/retry/failure paths to prevent silent mutation.
- Explain Rule alignment to generated/discard-only behavior (no rule mutation action).

### Out of Scope

- Batch accept behavior (handled separately).
- Architecture document updates.

---

## Relevant Areas

- `ui/src/features/mappings/hooks/use-suggest-expression.ts`
- `ui/src/features/mappings/hooks/use-auto-map-workspace.ts`
- `ui/src/features/mappings/hooks/use-mapping-editor.ts`
- `ui/src/features/mappings/components/SuggestExpressionInline.tsx`
- `ui/src/features/mappings/components/AutoMapWorkspace.tsx`
- `ui/src/features/mappings/components/WorkspaceSuggestionCard.tsx`
- `ui/src/features/mappings/components/*SmartFix*/*.tsx ?`
- `ui/src/features/mappings/components/*validation*/*.tsx ?`
- `ui/src/features/mappings/**/*.test.tsx`

---

## Change Requirements

- Ensure accept/apply handlers are explicit user actions and mutation-capable.
- Ensure edit-only and dismiss actions are non-mutating until explicit apply.
- Universally block apply for invalid suggestions until edited to valid.
- Universally hard-block stale suggestion acceptance in V1.
- Ensure generate/refresh/failure flows cannot produce implicit draft mutation.
- Align behavior copy/labels with generated-not-committed semantics.

---

## Verification Requirements

- UI integration tests for accept/edit/dismiss interactions per surface.
- Regression tests for no mutation on dismiss and non-apply paths.
- Manual feature walkthrough across targeted Phase 2 AI surfaces.

---

## Acceptance Checks

- Targeted AI surfaces implement explicit Accept/Edit/Dismiss behavior.
- No silent mapping mutation occurs from generation lifecycle events.
- Generated outputs remain non-committed until explicit accept/apply.
- Tests mapped to `AE-01`, `AE-02`, `AE-04`, and `AE-05` pass.

---

## Blockers / Risks

- Feature parity differences between FS-069..FS-073 may require scoped exceptions.

---

## Notes

- Keep per-feature behavior differences explicit in tests rather than implicit in code.
- Drift review (Rev 2): task remains aligned; scope clarified to full FS-069..FS-073 adoption and Explain Rule generated/discard-only behavior.
