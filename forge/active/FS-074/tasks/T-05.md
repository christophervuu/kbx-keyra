# TASK

## Title

Add cross-feature no-auto-commit verification suite for AI suggestion workflows

---

## ID

T-05

---

## Spec

FS-074

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-03
- T-04

---

## Implements

- `AE-02`
- `AE-03`
- `AE-05`

---

## Summary

Create deterministic automated verification that enforces the global “suggest, never auto-commit” rule across Phase 2 AI features, including per-item and batch workflows.

---

## Scope

### In Scope

- Cross-feature tests proving no mutation without explicit accept/apply action.
- Tests covering generate/refresh/retry/failure non-mutation behavior.
- Batch accept regression checks for eligibility and skip invariants.
- Telemetry/audit verification for required event emission and correlation-ID continuity.
- Shared test helpers for pre/post mapping-config state assertions where useful.

### Out of Scope

- UI visual redesign.
- Architecture document content updates.

---

## Relevant Areas

- `ui/src/features/mappings/**/*.test.tsx`
- `ui/src/features/mappings/hooks/*.test.ts`
- `tests/lambda/ai/*.test.ts ?`
- `tests/*adapter*/*.test.ts ?`

---

## Change Requirements

- Add repeatable assertions for “no explicit accept/apply => no mapping mutation”.
- Cover success/failure/retry/refresh paths that previously risked implicit mutation.
- Cover batch accept mixed eligibility behavior and skipped-item immutability.
- Verify event emission for: `suggestion_generated`, `suggestion_viewed`, `suggestion_edited`, `suggestion_accepted`, `suggestion_dismissed`, `apply_blocked_invalid`, `apply_blocked_stale`.
- Verify correlation IDs can link UI event -> API request -> persisted mutation records.

---

## Verification Requirements

- Run touched unit/integration tests with deterministic pass criteria.
- Ensure `pnpm typecheck` remains clean.

---

## Acceptance Checks

- Cross-feature no-auto-commit regression suite exists and passes.
- Batch and non-batch workflows are both covered.
- Tests map to `AE-02`, `AE-03`, and `AE-05`.

---

## Blockers / Risks

- Test harness variance across features may require shared helper refactors.

---

## Notes

- Prefer stable, black-box behavior assertions over implementation-detail coupling.
- Drift review (Rev 2): task remains aligned; telemetry/audit verification added per resolved spec decisions.
