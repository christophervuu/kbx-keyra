# TASK

## Title

Create shared CDM label/status presentation primitives for schema surfaces

---

## ID

T-02

---

## Spec

FS-078

---

## Spec Rev

Rev: 1

---

## Agent

ui-task

---

## Status

todo

---

## Depends On

- T-01

---

## Implements

- `AE-01`
- `AE-02`

---

## Summary

Introduce shared UI helpers/components (or equivalent shared render utilities) for CDM origin labeling and sync badge rendering so all schema surfaces consume one canonical presentation source.

---

## Scope

### In Scope

- Add reusable CDM origin label presentation (`CDM (KBXT/KBX-Canonicals)`).
- Add reusable sync-status badge/icon/text mapping for CDM states.
- Replace duplicated per-component hardcoded mappings where practical.

### Out of Scope

- Surface-specific action policies.
- Re-sync interaction wiring.

---

## Relevant Areas

- `ui/src/features/projects/components/SchemaCard.tsx`
- `ui/src/features/schemas/components/SchemaLibraryCard.tsx`
- `ui/src/features/schemas/components/SchemaGitStatus.tsx`
- shared UI helper location under `ui/src/features/schemas/` or `ui/src/components/` ?
- `ui/src/features/**/__tests__/*.test.tsx`

---

## Change Requirements

- Shared rendering primitive(s) must be the canonical source for CDM origin/status copy.
- Existing surfaces must stop using conflicting hardcoded text for CDM origin/status.
- Visual output must keep concise copy and accessible icon+text semantics.

---

## Verification Requirements

- Component tests verifying shared primitive output for each CDM status.
- Regression tests proving Project Overview, Schema Library, and Schema Detail-derived status blocks render identical label text for same inputs.
- Typecheck/lint for touched UI modules.

---

## Acceptance Checks

- CDM origin label is rendered consistently via shared primitive(s) on all targeted surfaces.
- CDM status text/icon mapping is consistent across all targeted surfaces.
- Tests covering status permutations pass.
- Typecheck and lint pass.

---

## Blockers / Risks

- Existing style differences between card and detail surfaces may require small wrapper adaptations while preserving canonical copy.

---

## Notes

- Prioritize copy and behavior consistency over pixel-perfect visual unification.
