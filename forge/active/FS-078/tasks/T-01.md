# TASK

## Title

Normalize CDM status and origin contract across API/UI boundaries

---

## ID

T-01

---

## Spec

FS-078

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

todo

---

## Depends On

- none

---

## Implements

- `AE-01`
- `AE-02`
- `AE-07`

---

## Summary

Align schema contract normalization so CDM origin and sync status values resolve consistently across list/detail/re-sync responses before UI rendering applies shared labels and badges.

---

## Scope

### In Scope

- Define/confirm canonical CDM sync statuses consumed by UI (`synced`, `update-available`, `sync-failed`).
- Add deterministic normalization/mapping for legacy or variant status values in backend responses (canonical source).
- Ensure CDM origin display source data remains stable for canonical label rendering.

### Out of Scope

- UI component visual implementation details.
- Broad non-CDM lifecycle model changes.

---

## Relevant Areas

- `ui/src/lib/types/domain.ts`
- `ui/src/lib/api/types.ts`
- `ui/src/lib/api/http-adapter.ts` ?
- `src/lambda/schema/*.ts` ?
- `src/lib/persistence/schema-metadata.ts` ?
- schema contract tests in `tests/lambda/schema/**/*.test.ts` and/or `ui/src/lib/api/**/__tests__` ?

---

## Change Requirements

- Contract must represent CDM statuses needed by FS-078 acceptance checks.
- Transitional status values must map deterministically to canonical CDM UX states in backend response shaping.
- UI consumption contracts must expose canonical enum only (no UI legacy-status branching).

---

## Verification Requirements

- Contract/unit tests for normalization behavior.
- Typecheck/lint for touched backend/API/UI contract modules.
- Targeted tests proving same schema record status resolves consistently from relevant endpoints.

---

## Acceptance Checks

- Canonical CDM status values are exposed at UI consumption boundary.
- Legacy/unknown statuses are mapped to deterministic fallback behavior.
- Origin + status contract is consistent between schema list/detail/re-sync payload paths.
- Typecheck and lint pass for touched modules.

---

## Blockers / Risks

- FS-076/FS-077 in-progress contract changes may require coordination to avoid drift.

---

## Notes

- Keep this task contract-focused so UI tasks can rely on stable shared status/origin semantics.
