# TASK

## Title

Rename AdvancedTestingPage component to TestLabPage with updated UI copy and test-ids

---

## ID

T-03

---

## Spec

FS-032

---

## Spec Rev

Rev: 1

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- none

---

## Implements

- AE-02
- AE-06
- AE-09

---

## Summary

Rename the `AdvancedTestingPage.tsx` feature component file to `TestLabPage.tsx`, update all internal function names, test-ids, and visible UI copy from "Advanced Testing" to "Test Lab". This is the core rename of the feature component.

---

## Scope

### In Scope

- Rename file: `ui/src/features/mappings/components/AdvancedTestingPage.tsx` → `ui/src/features/mappings/components/TestLabPage.tsx`
- Rename exported component: `AdvancedTestingPage` → `TestLabPage`
- Rename internal component: `AdvancedTestingInner` → `TestLabInner` (if present)
- Update test-ids:
  - `advanced-testing-page` → `test-lab-page`
  - `advanced-testing-topbar` → `test-lab-topbar`
- Update any visible text that says "Advanced Testing" to "Test Lab" within the component
- Update the feature barrel (`ui/src/features/mappings/index.ts`) if it re-exports `AdvancedTestingPage`
- Update any imports of `AdvancedTestingPage` in other files (the route page wrapper in T-01 will reference the new name)

### Out of Scope

- Changing page functionality, layout structure, or hook wiring
- Renaming the route page wrapper — that is T-01
- Updating `InlinePreviewStrip` link text — that is T-04
- Updating test files — that is T-05

---

## Relevant Areas

- `ui/src/features/mappings/components/AdvancedTestingPage.tsx` (rename to `TestLabPage.tsx`)
- `ui/src/features/mappings/index.ts` — feature barrel
- `ui/src/routes/pages/MappingAdvancedTesting.tsx` (or `MappingTestLab.tsx` after T-01) — import path

---

## Change Requirements

After this task:

- `AdvancedTestingPage.tsx` no longer exists; `TestLabPage.tsx` exists at the same path
- The named export is `TestLabPage` (not `AdvancedTestingPage`)
- All test-ids within the component use `test-lab-` prefix
- No references to "Advanced Testing" remain in the component's visible UI copy or JSDoc comments
- The feature barrel (if applicable) re-exports `TestLabPage` instead of `AdvancedTestingPage`
- The route page wrapper import is updated to reference `TestLabPage` from the new file path

---

## Verification Requirements

- `pnpm typecheck` passes in `ui/` (once T-01 is also complete for the route wrapper import)
- `pnpm lint` passes in `ui/`
- No occurrences of `AdvancedTestingPage` or `AdvancedTestingInner` remain in any `.tsx` or `.ts` file
- No occurrences of `advanced-testing-page` or `advanced-testing-topbar` test-ids remain in production code

---

## Acceptance Checks

- `TestLabPage.tsx` exists with correct component name and test-ids (AE-09)
- `AdvancedTestingPage.tsx` no longer exists
- Test-ids `test-lab-page` and `test-lab-topbar` are used in the component
- "Back to Editor" link still navigates correctly (AE-06)
- No "Advanced Testing" text appears in the component
- Feature barrel updated

---

## Blockers / Risks

- none

---

## Notes

- The component is ~340 lines. The rename is primarily a search-and-replace operation within the file plus the file rename itself.
- The "Back to Editor" link URL is constructed from route params and should not change — verify it still works.
- The `h-[calc(100vh-3.5rem)]` height calculation should be verified once T-02 changes the layout. If the full-bleed layout changes available height, this may need adjustment. Flag if the height no longer fills correctly.
