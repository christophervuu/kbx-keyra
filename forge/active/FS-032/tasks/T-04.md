# TASK

## Title

Update InlinePreviewStrip link labels, test-ids, and URL construction for Test Lab

---

## ID

T-04

---

## Spec

FS-032

---

## Spec Rev

Rev: 1

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- T-01

---

## Implements

- AE-01
- AE-07

---

## Summary

Update `InlinePreviewStrip.tsx` to change all "Open Advanced Testing" link text to "Open Test Lab", rename associated test-ids from `advanced-testing` to `test-lab`, and update the URL construction in `ConnectedInlinePreviewStrip.tsx` to use the `/test-lab` path segment.

---

## Scope

### In Scope

- In `InlinePreviewStrip.tsx`:
  - Change toolbar link text from `"Open Advanced Testing"` to `"Open Test Lab"`
  - Change error status bar link text from `"Open Advanced Testing →"` to `"Open Test Lab →"`
  - Rename test-id `strip-advanced-testing-link` → `strip-test-lab-link`
  - Rename test-id `strip-status-bar-advanced-testing-link` → `strip-status-bar-test-lab-link`
  - Update any JSDoc comments or inline comments referencing "Advanced Testing"
- In `ConnectedInlinePreviewStrip.tsx`:
  - Update URL construction from `/test` to `/test-lab`: change `` testingPageUrl={`/projects/${projectId}/mappings/${mappingId}/test`} `` to `` testingPageUrl={`/projects/${projectId}/mappings/${mappingId}/test-lab`} ``

### Out of Scope

- Changes to InlinePreviewStrip functionality or behavior
- Changes to the preview execution hooks or PreviewProvider
- Renaming the test file — that is T-05

---

## Relevant Areas

- `ui/src/features/mappings/components/InlinePreviewStrip.tsx`
- `ui/src/features/mappings/components/ConnectedInlinePreviewStrip.tsx`

---

## Change Requirements

After this task:

- All visible link text in `InlinePreviewStrip.tsx` says "Test Lab" instead of "Advanced Testing"
- Test-ids use `test-lab` instead of `advanced-testing`
- The `testingPageUrl` prop value uses `/test-lab` path segment
- No occurrences of "Advanced Testing" remain in either file (production code, comments, or JSDoc)

---

## Verification Requirements

- `pnpm typecheck` passes in `ui/`
- `pnpm lint` passes in `ui/`
- No occurrences of `advanced-testing` (as substring) remain in `InlinePreviewStrip.tsx` or `ConnectedInlinePreviewStrip.tsx`

---

## Acceptance Checks

- Toolbar link text reads "Open Test Lab" (AE-01)
- Toolbar link test-id is `strip-test-lab-link` (AE-01)
- Error status bar link text reads "Open Test Lab →" (AE-07)
- Error status bar link test-id is `strip-status-bar-test-lab-link` (AE-07)
- URL navigates to `/test-lab` path (AE-01)

---

## Blockers / Risks

- none

---

## Notes

- The InlinePreviewStrip is a large component (~800 lines). Changes are limited to specific link elements and their surrounding comments. Use targeted search for "Advanced Testing" and "advanced-testing" to find all occurrences.
