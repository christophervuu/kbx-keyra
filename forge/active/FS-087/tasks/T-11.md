# TASK

## Title

Run end-to-end verification pass for shared schema model and remove residual scope language

---

## ID

T-11

---

## Spec

FS-087

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-05
- T-06
- T-07
- T-08
- T-09
- T-10

---

## Implements

- `AE-01`
- `AE-02`
- `AE-03`
- `AE-04`
- `AE-05`
- `AE-05a`
- `AE-06`
- `AE-07`
- `AE-08`
- `AE-09`
- `AE-09a`
- `AE-10`
- `AE-11`
- `AE-12`
- `AE-12a`
- `AE-13`
- `AE-14`
- `AE-15`

---

## Summary

Execute final verification across UI and backend to confirm the shared Schema Library model works end-to-end, existing mappings remain stable, migrations are safe, and residual scope-based terminology/logic has been removed.

---

## Scope

### In Scope

- Run full validation suite required by touched areas (tests, lint, typecheck).
- Execute targeted manual verification for key user flows.
- Perform final repository search to confirm removal of user-facing scope terms where required.
- Document verification outcomes and any residual follow-up items.

### Out of Scope

- New feature work beyond FS-087.
- Non-regression cleanup not required for acceptance.

---

## Relevant Areas

- `ui/src/features/schemas/**/*`
- `ui/src/features/projects/**/*`
- `ui/src/features/mappings/**/*`
- `ui/src/lib/api/**/*`
- `src/lambda/schema/**/*`
- `src/lambda/project/**/*`
- `src/lib/persistence/**/*`
- test suites under `ui/src/**/__tests__/*`, `tests/**/*`

---

## Change Requirements

- Verify all acceptance examples against implemented behavior.
- Confirm no user-facing scope/access wording remains in targeted schema surfaces.
- Confirm deployment snapshot behavior unaffected and explicit-reference based.

---

## Verification Requirements

- Automated: lint + typecheck + targeted unit/integration tests for touched backend/UI.
- Manual: Schema Library, Project linked schemas, Add/Upload schema, Mapping schema selection, unlink behavior, legacy data normalization scenarios.
- Regression: existing mapping load and execution path with pre-existing schema IDs.

---

## Acceptance Checks

- All FS-087 acceptance examples are satisfied and evidenced.
- No regressions in existing mapping resolution paths.
- Verification report is attached to task notes and blockers are explicit if any remain.

---

## Blockers / Risks

- Risk: migration edge cases may only surface with real persisted datasets; include representative fixtures and manual checks.

---

## Notes

- Verification report (2026-06-07):
  - Updated UI schema/project surfaces to remove residual promote/scope-era behavior and terms:
    - Removed schema-detail promote action flow (`Promote to Global`) and callback wiring.
    - Removed feature-local `SchemaScope` / `SchemaCardData.scope` model usage in project feature types/hooks/tests.
    - Updated upload-source repository status label to `Uploaded schema — not connected to a repository`.
    - Normalized legacy test fixtures away from `origin: 'local'` in touched schema/project UI tests.
  - Automated verification run:
    - `pnpm --dir ui test src/features/schemas/components/__tests__/SchemaDetailEditing.test.tsx src/features/schemas/components/__tests__/SchemaGitStatus.test.tsx src/features/schemas/components/__tests__/SchemaActions.test.tsx src/features/schemas/components/__tests__/SchemaDetailPage.test.tsx src/features/schemas/components/__tests__/CdmCrossSurfaceConsistency.test.tsx src/features/projects/components/__tests__/SchemaCard.test.tsx src/features/projects/components/__tests__/SchemaManagementSection.test.tsx src/features/projects/components/__tests__/ProjectOverviewPage.test.tsx` ✅ (119/119)
    - `pnpm --dir ui typecheck` ✅
  - Repository checks:
    - No remaining `action-promote`, `onScopePromoted`, or `promote-global` references in `ui/src`.
    - No remaining feature-local `SchemaScope` / `scope: 'project-level'` usage in `ui/src/features/projects`.
    - Remaining "Project-Level" text appears only in a negative assertion (`queryByText`) proving scope labels are absent.
- If any acceptance check fails, keep task status non-done and open follow-up tasks rather than silently deferring.
