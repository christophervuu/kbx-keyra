# TASK

## Title

Refactor schema UI terminology and badges to remove access-scope concepts

---

## ID

T-05

---

## Spec

FS-087

---

## Spec Rev

Rev: 1

---

## Agent

ui-task

---

## Status

done

---

## Depends On

- T-01
- T-03

---

## Implements

- `AE-07`
- `AE-08`
- `AE-09`

---

## Summary

Update schema-facing UI components and copy to remove Global/Project/Local scope semantics and present origin/format/linked status terminology consistent with the shared Schema Library model.

---

## Scope

### In Scope

- Remove/de-emphasize scope badges and scope filters from schema cards/detail/library surfaces.
- Update labels/copy to use Schema Library, Linked schema(s), CDM, Uploaded, Inferred, JSON/XML/XSD.
- Ensure JSON format labels render as `JSON`.
- Update associated component tests and snapshots.

### Out of Scope

- Backend handler updates.
- Project/mapping schema selection behavior changes (handled by separate tasks).

---

## Relevant Areas

- `ui/src/features/schemas/components/**/*`
- `ui/src/features/schemas/hooks/**/*`
- `ui/src/features/schemas/lib/**/*`
- `ui/src/features/projects/components/LinkedSchemasDialog.tsx` ?
- `ui/src/features/projects/components/SchemaCard.tsx` ?
- schema-related tests under `ui/src/features/schemas/**/__tests__/*`

---

## Change Requirements

- Remove user-visible copy for `Global`, `Project-Level`, `Local schema` as access concepts.
- Preserve origin indication (CDM/Uploaded/Inferred) where surfaced.
- Normalize format rendering (`JSON`, `XML`, `XSD`) and remove `JSON Schema` wording in user-facing badges.

---

## Verification Requirements

- Component tests for schema badges/labels/copy.
- Manual pass over key surfaces: Schema Library, Schema Detail, linked schemas dialog/list surfaces.

---

## Acceptance Checks

- No user-facing schema scope labels remain as access concepts.
- JSON schemas are displayed as `JSON`.
- Origin badges remain available and accurate.

---

## Blockers / Risks

- Risk: existing tests strongly assert old label text and require broad updates.

---

## Notes

- Ensure terminology changes remain consistent with `ui-application.md` architecture language after architecture update task.
- Implemented UI terminology refactor across schema library/detail/project schema-card surfaces:
  - Removed scope-as-access UI concepts from library filters/chips/cards/detail surfaces.
  - Normalized format badges/labels to `JSON` / `XSD` / `Inferred` (removed user-facing `JSON Schema` wording on touched surfaces).
  - Preserved origin visibility with shared-library terminology (`CDM`, `Uploaded`, `Inferred`) and legacy alias display compatibility (`published|local -> Uploaded`).
- Updated affected tests for terminology and no-scope model, including schema library controls/cards/page/detail/presentation primitives, schema filters/hook behavior, project schema card, and CDM cross-surface consistency checks.
- Verification executed:
  - `pnpm --dir ui test src/features/schemas/components/__tests__/SchemaLibraryCard.test.tsx src/features/schemas/components/__tests__/SchemaLibraryControls.test.tsx src/features/schemas/components/__tests__/SchemaLibraryPage.test.tsx src/features/schemas/components/__tests__/SchemaDetailPage.test.tsx src/features/schemas/components/__tests__/SchemaPresentationPrimitives.test.tsx src/features/schemas/hooks/__tests__/use-schema-library.test.ts src/features/schemas/lib/__tests__/schema-filters.test.ts src/features/projects/components/__tests__/SchemaCard.test.tsx src/features/schemas/components/__tests__/CdmCrossSurfaceConsistency.test.tsx` ✅ (174/174)
  - `pnpm --dir ui typecheck` ✅
