# TASK

## Title

Update architecture documents for shared Schema Library and linked-schema model

---

## ID

T-10

---

## Spec

FS-087

---

## Spec Rev

Rev: 2

---

## Agent

task

---

## Status

done

---

## Depends On

- T-01
- T-02
- T-03
- T-04
- T-09

---

## Implements

- `AE-11`
- `AE-12`
- `AE-13`

---

## Summary

Update existing architecture reference documents to codify the new shared Schema Library + project-linked schemas model, revised terminology, compatibility strategy, and any backend/AWS integration outcomes.

---

## Scope

### In Scope

- Update architecture docs that currently encode or imply scope-based schema access.
- Update `forge/architecture/INDEX.md` with last-updated notes and summary.
- Capture final decisions for schema metadata model, project linkage, migration strategy, and deployment/integration implications.

### Out of Scope

- Adding a brand-new architecture document (existing schema/backend/ui/persistence coverage already exists).
- Implementing product code changes.

---

## Relevant Areas

- `forge/architecture/ui-application.md`
- `forge/architecture/backend-api.md`
- `forge/architecture/persistence-model.md`
- `forge/architecture/schema-ingestion.md` ?
- `forge/architecture/deployments.md` ?
- `forge/architecture/infrastructure.md` ?
- `forge/architecture/INDEX.md`

---

## Change Requirements

- Replace architecture language that frames schema access via global/project scope.
- Document shared availability + linked relevance model as canonical.
- Document migration/backward-compat approach and any retained legacy-field policy.
- Record backend/AWS areas confirmed unaffected (if any) explicitly.

---

## Verification Requirements

- Architecture docs remain internally consistent and match implemented behavior/tasks.
- `INDEX.md` entries and recent update notes reflect all changed docs and dates.

---

## Acceptance Checks

- Architecture references clearly encode the FS-087 schema model.
- INDEX is current and accurate for touched docs.
- No architecture doc continues to treat scope as user-facing access control concept.

---

## Blockers / Risks

- Risk: if code implementation diverges during execution, architecture updates must follow final implemented truth, not draft intent.

---

## Notes

- This is the explicit architecture update task required by workflow rules for architecture-impacting specs.
- Updated architecture docs to codify final FS-087 implemented behavior:
  - `ui-application.md`: added explicit FS-087 shared Schema Library + linked-schema model section; documented compatibility normalization (`linkedSchemaIds` canonical, `schemaRefs` bridge, `local|published -> uploaded`, scope non-authoritative).
  - `backend-api.md`: updated handler/table semantics to canonical linkage and compatibility policies; added FS-087 addendum with audit-confirmed unaffected ownership surfaces (Dynamo/S3/OpenSearch/deployment traceability scope independence).
  - `persistence-model.md`: updated table definitions and addendum to reflect canonical linkage/origin/sync vocabulary and scope de-authoritization; captured no-migration-needed ownership outcomes.
  - `INDEX.md`: refreshed coverage summaries and last-updated dates for touched docs; added FS-087 recent update note.

### Verification

- Reviewed updated architecture docs for consistency against completed FS-087 implementation tasks (`T-03`, `T-04`, `T-05`, `T-06`, `T-07`, `T-08`, `T-09`).
- Confirmed no architecture document in touched set continues to frame scope as user-facing access control.
- Confirmed `INDEX.md` entries and dates are updated for all touched architecture docs.
