# TASK

## Title

Add ComparisonDiffSummary component for comparison output diff

---

## ID

T-07

---

## Spec

FS-037

---

## Spec Rev

Rev: 1

---

## Agent

ui-task

---

## Status

todo

---

## Depends On

- T-01

---

## Implements

- `AE-05` (matching outputs indicator)
- `AE-06` (structural differences display)
- `AE-07` (cannot compute diff message)

---

## Summary

Create the `ComparisonDiffSummary` component that displays the diff between two comparison side outputs. This component adapts the existing `computeDiff` utility for comparison context, showing a match indicator when outputs are identical, a structured diff list when they differ, and a graceful message when diff cannot be computed (one side failed).

---

## Scope

### In Scope

- Create `ui/src/features/mappings/components/comparison/ComparisonDiffSummary.tsx`
- Render diff between left and right comparison outputs:
  - **Match state:** green "Outputs match" indicator with check icon
  - **Diff state:** summary count ("N differences found") + diff entry list with path, type (added/removed/changed), and values
  - **Cannot compute:** gray "Cannot compute diff — one side has no output" message
  - **Idle:** no diff section shown (both sides must complete)
- Use `computeDiff` from `ui/src/lib/utils/json-diff.ts` for diff computation
- Style diff entries with color coding: green for added, red for removed, amber for changed
- Add component tests

### Out of Scope

- Diff computation logic changes (reuse existing `computeDiff`)
- Editable expected output (unlike existing `DiffDisplay`, this is read-only)
- Side panel rendering (that's T-06)

---

## Relevant Areas

- `ui/src/features/mappings/components/comparison/ComparisonDiffSummary.tsx` (new)
- `ui/src/lib/utils/json-diff.ts` — `computeDiff`
- `ui/src/lib/types/diff.ts` — `DiffEntry`
- `ui/src/features/mappings/components/preview/DiffDisplay.tsx` — visual reference (not reused directly)

---

## Change Requirements

Component props:

```ts
interface ComparisonDiffSummaryProps {
  leftOutput: Record<string, unknown> | null;
  rightOutput: Record<string, unknown> | null;
  leftLabel: string;
  rightLabel: string;
  overallStatus: ComparisonState['overallStatus'];
}
```

Rendering rules:

- `overallStatus === 'idle' | 'executing'`: render nothing (or a subtle placeholder)
- `leftOutput === null || rightOutput === null`: render "Cannot compute diff — one side has no output" in a gray info box
- `diffEntries.length === 0`: render green "Outputs match ✓" indicator
- `diffEntries.length > 0`: render summary header ("N differences found") + scrollable list of diff entries

Diff entry rendering:
- Each entry shows: path, diff type icon (added/removed/changed), values
- Entry descriptions reference `leftLabel` / `rightLabel` instead of generic "expected"/"actual"
  - Added: `"${path}" is present in ${rightLabel} but not in ${leftLabel}`
  - Removed: `"${path}" is present in ${leftLabel} but not in ${rightLabel}`
  - Changed: `"${path}": ${leftLabel} has ${leftValue} → ${rightLabel} has ${rightValue}`
- Long values are truncated (60 chars) with ellipsis

Test IDs: `comparison-diff-summary`, `comparison-diff-match`, `comparison-diff-count`, `comparison-diff-entry`

---

## Verification Requirements

- Component tests covering:
  - Match state: renders green indicator when outputs are identical
  - Diff state: renders correct count and entries for differing outputs
  - Cannot compute: renders message when one output is null
  - Idle state: renders nothing
  - Labels used correctly in entry descriptions
- `pnpm typecheck` passes
- `pnpm lint` passes
- `pnpm test` passes

---

## Acceptance Checks

- All diff states render correctly (match, diff, cannot compute, idle)
- Diff entries reference left/right labels correctly
- Long values are truncated
- `pnpm typecheck` passes
- `pnpm lint` passes
- `pnpm test` passes
- `forge/architecture/project-structure.md` updated if new files were created

---

## Blockers / Risks

- none

---

## Notes

- This component is intentionally separate from `DiffDisplay` because `DiffDisplay` includes an editable expected-output textarea (for the Diff tab), while this component is purely read-only comparison display
- Reuse the `computeDiff` utility directly — no need to modify it
- Follow the existing color conventions from `DiffDisplay`: green for added, red for removed, amber for changed
