# Architecture Index

This file is the entry point for all architecture reference documents in this repository.

Agents must load this file before beginning any spec drafting or task execution. Load the specific documents relevant to the work area — do not load all documents indiscriminately.

This file must be kept current. When an architecture document is created or meaningfully updated, update the entry here.

---

## Documents

| Document | Covers | Last Updated |
| --- | --- | --- |
| [deployments.md](./deployments.md) | Deployment subsystem architecture: SANDBOX control-plane + DEV/PREPROD/PROD runtime model, deploy/promote/rollback semantics, FS-083 orchestration status/reconciliation/config-ownership contracts, artifact relay + payload/retry contracts, retention baseline, FS-082 runtime bootstrap stack contracts (resources, internal routes, data model), FS-093 enrichment snapshot metadata compatibility (`enrichmentRefs`), and FS-096 project value-table snapshot invariants (embedded pinned `resolvedEntries`, promote artifact identity reuse, rollback exact-data restore, and runtime no table-fetch compatibility). | 2026-06-20 |
| [project-structure.md](./project-structure.md) | Repository layout and file/folder conventions for `src/`, `ui/`, and `tests/`. | 2026-05-15 |
| [mapping-engine.md](./mapping-engine.md) | Mapping engine architecture: public API, parser/evaluator, validate/execute pipelines, function registry, diagnostics, constraints, FS-093 enrichment externals contract (caller-supplied `externalSources`, canonical `enrichmentSources` compatibility through derived externals, and no-I/O boundary preservation), and FS-096 project value-table lookup contract (`valueTable(...)`, stable E06x diagnostics, pinned `valueTableRef` + `noMatchBehavior`, inline compatibility, and no-I/O execution boundary). | 2026-06-20 |
| [ui-application.md](./ui-application.md) | UI architecture: adapter/state/routing/component structure, app-shell contracts, dashboard composition conventions, and Mapping Editor evolution through FS-086/087/088/089/090/092/093/094/096/097/098/099 including FS-099 inline Output Rev 3 contracts (JSON-only scope, output-controlled execution lifecycle, preview-context stale isolation, deterministic output-node resolver with unconfigured target fallback, and container coverage-vs-validation separation). | 2026-06-24 |
| [phase-1-readiness.md](./phase-1-readiness.md) | Phase 0→1 readiness baseline, backend constraints implied by UI, transition guidance including FS-081 deployment environment migration constraints, FS-082 runtime bootstrap boundary prerequisites, and FS-083 orchestration readiness constraints. | 2026-06-04 |
| [e2e-testing.md](./e2e-testing.md) | E2E infrastructure, conventions, and CI acceptance-gate posture. | 2026-06-02 |
| [ai-runtime.md](./ai-runtime.md) | Shared AI runtime architecture: prompt/routing/invocation/telemetry/error-normalization contracts and feature-specific runtime addenda, including FS-091 Dynamo-only schema retrieval dependency contract (`RAG_RETRIEVER=dynamodb`), decommissioned `opensearch|shadow` serving modes, environment cap defaults, and cutover parity gate references. | 2026-06-10 |
| [persistence-model.md](./persistence-model.md) | Persistence layer architecture: DynamoDB/S3 schemas, access patterns, metadata/blob boundaries, FS-087 compatibility semantics (`linkedSchemaIds` canonical project linkage, scope as non-authoritative metadata, legacy origin alias normalization), FS-089 canonical schema metadata separation (`ownership`, `readonly`, `dataFormat`, `sourceKind`, readiness status model, inferred review metadata), FS-090 inferred review/sample payload persistence contracts (`reviewState`, deterministic issue summaries, `samplePayloads` metadata + S3 blobs), FS-092 editor-state persistence layering (`config.editorPreferences.defaultSelectedSampleId`, per-user sample/advanced-mode preferences, session-scoped suggestion-state boundary), FS-091 retrieval-field/cap decisions for Dynamo-only serving (`embedding` optional, lexical signals, environment caps, parity gates), FS-093 mapping enrichment persistence contracts (`enrichmentSources` canonical, derived `config.externalSources` compatibility, schema dependency semantics, versioned input sets), and FS-096 project value-table persistence contracts (ValueTables + ValueTableRevisions model, immutable S3 row payload strategy, usage/resolve semantics, pinned revision embedding). | 2026-06-20 |
| [schema-ingestion.md](./schema-ingestion.md) | Schema ingestion/retrieval-preparation architecture: inline + Step Functions flow, module boundaries, Dynamo retrieval-field preparation, query-stage bounded retrieval/rerank semantics, failure handling, and performance targets. | 2026-06-10 |
| [backend-api.md](./backend-api.md) | Backend API architecture: handler conventions, route contracts, error envelopes, and cross-cutting addenda including FS-076 CDM read-only API, FS-078 sync-status normalization, FS-079 deploy guardrails, FS-080 CDM GitHub resilience semantics, FS-081 runtime deployment model contracts, FS-082 runtime internal bootstrap API contracts, FS-083 control-plane remote orchestration contracts, FS-087 shared schema-access + compatibility/ownership outcomes, FS-088 create-mapping contract addendum (`businessContext` optional metadata + create-time auto-map orchestration boundary), FS-089 schema metadata/readiness + selector/usability contract normalization, FS-090 inferred review/sample endpoints (`POST /schemas/:id/mark-reviewed`, `POST /schemas/:id/samples`) with confirmation-gated mutation semantics, FS-092 authoring-route contract addendum (More-menu route-outs, additive `editorPreferences`, sample-add usage, visible-scope auto-map metadata), FS-091 Dynamo-only schema query/retriever mode contracts, FS-093 multi-input enrichment mapping contracts (canonical `enrichmentSources`, compatibility `config.externalSources`, runtime `mappingId + sourceData + externalSources`, required-missing preflight hard-fail, schema delete-guard dependencies), and FS-096 project value-table API contracts (CRUD/revision/usage/diff/import/export/resolve, pinned reference embedding, and runtime no-table-fetch invariant). | 2026-06-20 |
| [infrastructure.md](./infrastructure.md) | Infrastructure architecture: SAM resources, environment config, packaging, local dev, deployment conventions, FS-081 multi-account topology/runtime footprint assumptions, FS-082 runtime bootstrap template parameter/resource contracts, FS-083 reconciliation/config ownership compatibility notes, and FS-091 OpenSearch runtime resource removal posture. | 2026-06-10 |

**Recent update notes**

- **2026-06-24 (FS-099):** updated `ui-application.md` and `INDEX.md` to codify Mapping Editor inline Output Rev 3 contracts: JSON-only scope (XML deferred pending canonical serializer contract), `Fields | Output` segmented panel behavior, output-controlled lifecycle (no hidden execution while Fields is active, dirty-run on Output reopen), preview-context identity and stale-isolation rules, discriminated JSON `RenderableOutput` branch with shared `OutputDisplay` + `JsonOutputView` split, deterministic output-node resolver order with unconfigured-target fallback and terminal `No editable target found`, and container coverage labels derived from descendant mapping coverage independent of validation severity.

- **2026-06-23 (FS-098):** updated `ui-application.md` and `INDEX.md` to codify FS-098 as the canonical Smart Builder contract for scalar authoring: locked invariant that tray membership is availability-only while recipe explicitly owns operand usage/order/transforms, canonical panel hierarchy (`Inputs`/`Build Output`/`Refine Result`), deterministic explicit-operand generation with no all-tray fallback, incomplete recipe lifecycle preserving last valid expression while blocking save, target-scoped session tray behavior, and strict deep-array handoff actions to `ArrayBuilder` with Advanced DSL fallback for unsupported/non-lossless expressions.

- **2026-06-22 (FS-097):** updated `ui-application.md` and `INDEX.md` to codify Mapping Editor shared tray + conditional-builder refinement contracts: method-independent Input Tray shell ordering, direct-default unmapped scalar flow, source routing for add-to-tray vs fill-current-value, phase-one tray density constraints (no Manage view; scroll threshold >5 rows or >320px), session-only staged-row persistence, mapping-scoped target session-key behavior for tray/draft restoration, conditional→non-conditional confirmation with explicit discard semantics and tray preservation, and strict guided/Advanced boundary for unsupported nested or non-lossless conditional expressions.

- **2026-06-20 (FS-096):** updated `ui-application.md`, `backend-api.md`, `persistence-model.md`, `mapping-engine.md`, and `deployments.md` to codify project value-table contracts: project-scoped table management route and Mapping Editor integration, explicit direction + pinned revision + adoption workflow, API surface for value-table CRUD/revision/usage/diff/import/export/resolve, immutable S3-backed revision row storage with Dynamo metadata/index model, engine `valueTable(...)` + stable E06x/W003 diagnostics + no-match behavior semantics, deployment/runtime snapshot invariants for embedded `resolvedEntries` (immutable deploy, promote identity reuse, rollback exact restore), and explicit runtime no project-table fetch behavior.

- **2026-06-15 (FS-094):** updated `ui-application.md` to codify Smart Input Tray Builder replacement contracts for scalar target authoring: `SmartBuilderDraft` + input-kind model, catalog/resolver enabled-first + disabled-reason behavior, slot-aware staged-field event routing owned by MappingEditor/SmartBuilder, strict guided output validity against registered DSL with raw-editor escape-hatch boundary, save/validation canonical boundaries (draft-only until global Save; engine validation authoritative), deterministic insertion-order behavior with reorder deferred in Rev 1, retained deep-array authoring via explicit ArrayBuilder handoff seam, and non-decomposable expression Advanced-mode fallback with complex-expression banner.

- **2026-06-12 (FS-093):** updated `backend-api.md`, `persistence-model.md`, `ui-application.md`, `mapping-engine.md`, and `deployments.md` to codify multi-input enrichment mapping contracts: canonical `enrichmentSources` with derived `config.externalSources` compatibility behavior, runtime input contract (`mappingId`, `sourceData`, `externalSources`), required enrichment preflight hard-fail policy, schema dependency/delete-guard semantics for enrichment refs, guided UI terminology and input-set persistence/migration contracts, and deployment snapshot metadata compatibility for enrichment references.

- **2026-06-11 (FS-092):** updated `ui-application.md`, `backend-api.md`, and `persistence-model.md` to codify Mapping Editor grid-first redesign contracts: authoring-route boundary separation from Test Lab/Deployment, View Issues + More-menu secondary route-outs on editor surface, additive mapping `editorPreferences.defaultSelectedSampleId` semantics, per-user sample/Advanced-Mode preference persistence boundaries, and visible-scope auto-map/suggestion-lifecycle contract clarifications.

- **2026-06-10 (FS-091):** updated `ai-runtime.md`, `schema-ingestion.md`, `backend-api.md`, `persistence-model.md`, and `infrastructure.md` to codify Dynamo-only schema retrieval and OpenSearch decommission contracts: canonical runtime mode `RAG_RETRIEVER=dynamodb` with fail-closed rejection of `opensearch|shadow`, ingestion/query architecture aligned to Dynamo lexical retrieval + optional bounded in-Lambda rerank, retrieval cap defaults by environment (DEV/QA/PROD), parity gate canon (`Jaccard@10`, `NDCG@10 delta`), and infrastructure inventory/env-output posture after OpenSearch resource removal.

- **2026-06-09 (FS-090):** updated `ui-application.md`, `backend-api.md`, and `persistence-model.md` to codify Schema Detail FS-090 contracts: two-column + responsive stacked layout ordering, inferred review panel + explicit mark-reviewed transition semantics, ownership-aware action vocabulary cleanup (no schema-detail sync/re-sync/publish/promote actions), sample payload lifecycle model (`samplePayloads` metadata + `schemas/{schemaId}/samples/{sampleId}/payload.{json|xml}` blobs), and backend add-sample/mark-reviewed endpoint semantics including explicit confirmation gating for schema mutation.

- **2026-06-09 (FS-089):** updated `ui-application.md`, `backend-api.md`, and `persistence-model.md` to codify simplified schema model contracts across UI/API/persistence: explicit schema metadata separation (`ownership`, `readonly`, `dataFormat`, `sourceKind`, readiness `status`), add-schema user-created-only upload/paste pathways with deterministic input-kind classification, persistent inferred review callout + explicit review transition (`needs_review -> ready` + `reviewedAt`), Create Mapping selector status gating (`needs_review` warning-selectable, `error` visible/non-selectable), CDM default availability in selector/list pathways, and explicit deactivation of legacy scope/sync/publish vocabulary as active Schema Library model semantics.

- **2026-06-08 (FS-088):** updated `ui-application.md` and `backend-api.md` to codify Create Mapping single-page setup architecture and finalized create-contract constraints: required/optional field model (`businessContext` optional), in-card add-schema reuse flow, start-mode branching (`blank` vs `auto-map`), create-time auto-map orchestration boundary (`POST /mappings` then explicit `/ai/auto-map` keyed by `mappingId`), non-blocking auto-map failure handling, and explicit AI review-only/non-auto-commit semantics.

- **2026-06-08 (FS-087):** updated `ui-application.md`, `backend-api.md`, and `persistence-model.md` to codify shared Schema Library semantics and compatibility strategy: canonical project linkage via `linkedSchemaIds`, legacy `schemaRefs` as read-time bridge, canonical origin normalization (`local|published -> uploaded`), scope de-authoritization, unlink dependency hard-block behavior, and explicit backend/AWS unaffected ownership outcomes (schemaId-centric Dynamo/S3/OpenSearch + scope-independent deployment traceability).

- **2026-06-07 (FS-086):** updated `ui-application.md` to supersede FS-085 Project Overview right-rail defaults and codify the current baseline as a single-column mappings-first workspace (no right rail, no Deployment Activity card, no always-visible schema grid), with linked schemas accessed via a lightweight project-summary interaction and simplified format labels.

- **2026-06-06 (FS-085):** updated `ui-application.md` to codify an interim Project Overview mappings-dominant workspace (header -> mappings primary -> right rail secondary), single-column mapping deployment-state presentation with normalized `Changed since deploy` wording, right-rail Deployment Activity/Schemas card responsibilities (including empty-state + manage-schemas scroll handoff), and hierarchy-based breadcrumb patterns with route-reality clickability constraints (`Projects`/`Mappings` structural segments). This right-rail default direction is superseded by FS-086.

- **2026-06-06 (FS-084):** updated `ui-application.md` to codify sidebar-first app shell contracts (no top-nav on primary routes), client-only sidebar collapse persistence (`keyra:app-sidebar-collapsed`), focused-route compatibility without dual top+side navigation, and Home dashboard architecture aligned to the FS-084 canonical visual target (`forge/active/FS-084/keyra_dashboard_simple.html`) with Projects-dominant + Recent activity-secondary hierarchy and metrics-strip removal.

- **2026-06-04 (FS-083):** updated `deployments.md`, `backend-api.md`, `infrastructure.md`, and `phase-1-readiness.md` to codify control-plane remote orchestration contracts: explicit orchestration status lifecycle, promote-as-full-payload semantics, 5 MB payload hard limit with dual-layer rejection (`PAYLOAD_TOO_LARGE` / `DEPLOY_ARTIFACT_TOO_LARGE`), timeout reconciliation via runtime status polling, local-artifact-only rollback (`ARTIFACT_NOT_PRESENT`), persisted admin settings as canonical endpoint config source (env-var fallback), and QA->PREPROD normalization without destructive migration.

- **2026-06-04 (FS-082):** updated `deployments.md`, `infrastructure.md`, `backend-api.md`, and `phase-1-readiness.md` to codify runtime bootstrap stack contracts: per-environment resource inventory, `template.yaml` parameter/output contract, runtime internal route surface (`/internal/deploy|rollback|execute|health|status`), ActiveSnapshots + DeploymentHistory data-model split, local schema-payload copy invariant, HttpApi/direct-body transport defaults, and MVP index/log-retention posture.

- **2026-06-03 (FS-081):** updated `deployments.md`, `backend-api.md`, `infrastructure.md`, and `phase-1-readiness.md` to codify SANDBOX control-plane + DEV/PREPROD/PROD runtime model, direct-push artifact transfer (signed-pull deferred), payload-size/idempotent retry contracts, pointer-only rollback with missing-artifact remediation, runtime-local execution/preview invariants, and QA->PREPROD normalization policy.

- **2026-06-03 (FS-080):** updated `backend-api.md` and `ui-application.md` to codify CDM GitHub read resilience contracts: backend failure taxonomy (`rate-limited`, `unauthorized-forbidden`, `not-found-path-mismatch`, `timeout-transient`) + stable error codes, bounded retry/backoff+jitter behavior, browse cache TTL defaults (local 30s/dev 60s/prod 300s) with outage-only stale-grace guidance, explicit `retry-after` propagation, sync no-silent-failure invariant, and structured telemetry/request-lineage fields.
- **2026-06-03 (FS-079):** updated `deployments.md`, `backend-api.md`, and `ui-application.md` to codify CDM deployment guardrail + provenance contracts: deploy/promote pre-check gating, stable CDM block reason taxonomy (`unsynced`, `update-failed`, `metadata-incomplete`, `ingest-not-ready`, `schema-missing`), and dual-location traceability persistence.
- **2026-06-03 (FS-078):** updated `ui-application.md` and `backend-api.md` for canonical cross-surface CDM UX consistency (origin label, sync-status vocabulary, badge semantics, action-policy alignment, and backend-owned legacy normalization).
- **2026-06-03 (FS-076):** updated `backend-api.md`, `ui-application.md`, and `phase-1-readiness.md` to document CDM read-only integration foundations (root guard, one-level browse semantics, source metadata persistence/index projection, idempotent duplicate-link behavior, manual re-sync + status-refresh, and no-write GitHub invariants).

---

## Maintenance Rules

- Add a row when a new architecture document is created.
- Update the `Last Updated` date when a document is meaningfully changed.
- Do not remove rows without an explicit architecture task authorizing the removal.
- Documents are created by the spec agent during planning and updated by the task agent during execution of architecture tasks.

---
## Reference Specifications

| Document | Path | Covers |
|----------|------|--------|
| Product & Technical Spec | `specs/PRODUCT-TECHNICAL.md` | Full product requirements, screen specs, engine overview, backend architecture |
| DSL Specification | `specs/KEYRA-DSL-SPECIFICATION.md` | DSL grammar, function catalog, type system, error codes |
| DSL Arrays | `specs/KEYRA-DSL-ARRAYS.md` | Array scoping, map/filter/find, nested contexts |
