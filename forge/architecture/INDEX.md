# Architecture Index

This file is the entry point for all architecture reference documents in this repository.

Agents must load this file before beginning any spec drafting or task execution. Load the specific documents relevant to the work area — do not load all documents indiscriminately.

This file must be kept current. When an architecture document is created or meaningfully updated, update the entry here.

---

## Documents

| Document                                       | Covers                                                                                                                                                                                                    | Last Updated |
| ---------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ |
| [deployments.md](./deployments.md)             | Deployment subsystem architecture — Deployments + DeploymentCurrent DynamoDB tables, S3 snapshot layout, deployability rules matrix (revision→DEV only, version→any), API routes (deploy/promote/rollback/list/current), staleness computation module (`src/lib/deployment/staleness.ts`), promotion and rollback semantics, Lambda handler structure, and UI adapter method contracts. | 2026-06-01   |
| [project-structure.md](./project-structure.md) | Repository layout — where source code, UI code, tests, scripts, and specs live. File and folder conventions within `src/`, `ui/`, and `tests/`, including Phase 0 implemented-vs-planned annotations for lambda domains and test placement guidance aligned to current repository reality. | 2026-05-15   |
| [mapping-engine.md](./mapping-engine.md)       | Mapping engine public API, internal module boundaries, function registry pattern, execution + validate pipelines, parser/evaluator architecture, type system, constraints, and error handling philosophy; includes documented root exports and explicit Phase 0 XSD simplification notes. | 2026-05-14   |
| [ui-application.md](./ui-application.md)       | UI application architecture — adapter pattern, state management (AsyncState), engine integration boundary/hooks, Mapping Editor layout/view architecture, routing, component organization, and Phase 0 constraints. Includes FS-048 Auto-Map Review Workspace architecture (mode switch `target|rules|automap`, `useAutoMapWorkspace`, persistence/staleness/refresh/preview model, and retirement of drawer composition). FS-051: Unified Builder Visual Shell — shared header/feedback/validation/content/action-row shell for both builders; Builder/Editor toggle pattern; overflow menu (⋮); capability-driven action row; completion status in both builders; ValidationSummaryRow shared component. FS-053: ParameterValueInput intent-based parameter input model (Source/Static/Item/Options/Expression-secondary); Options mode via PARAMETER_HINTS; ArgumentSlotInput deprecated. FS-055/FS-065/FS-068: HttpAdapter bootstrap selection (`VITE_API_URL` => `HttpAdapter`), canonical backend AI/schema-query routing, explicit `FEATURE_NOT_ENABLED` transitional semantics, deterministic local offline AI policy, and deprecated one-cycle HybridAdapter bridge + guardrail constraints. FS-070: Suggest Expression canonical review model — backend-owned context retrieval, validation-aware hook/component states, inline edit-before-apply UX, strict apply gating until valid, and suggestion-only non-persistent semantics. FS-071: Smart Fix canonical review/apply model — rule-diagnostic request contract, validation-gated accept/edit/dismiss UX, stale mismatch rerun flow, and non-persistent draft-only apply semantics. FS-072: AI Validation canonical review model — manual-only advisory report flow, Rules-view panel placement, issue-to-rule resolution/fallback semantics, and explicit deterministic-vs-AI boundary. FS-073: Auto-Map canonical workspace contract alignment — single endpoint mode semantics (`section|whole`), retrieval/chunk/validation/dedup metadata-aware summary handling, and explicit no-context empty-state success behavior. FS-074: cross-feature suggestion review hardening — explicit generated-vs-committed distinction, explicit Accept/Edit/Dismiss mutation boundaries, deterministic batch eligibility/skip accounting, universal invalid/stale apply blocks, and Explain Rule generated/discard-only behavior. | 2026-06-02   |
| [phase-1-readiness.md](./phase-1-readiness.md) | Phase 0 → Phase 1 backend readiness baseline — ApiAdapter boundary inventory, Phase 0 simplifications requiring resolution, backend design constraints implied by current frontend, canonical AI transition to HttpAdapter, FS-066 shared AI foundation alignment (routing/limits/telemetry/error normalization + backend-only guardrails), and FS-068 canonical Phase 2 adapter updates (route-complete AI mapping, deterministic local offline semantics, transitional `FEATURE_NOT_ENABLED`, deprecated one-cycle HybridAdapter retention with guardrails). Includes engine integration points, reconciliation summary, and actionable open questions for Phase 1 planning. | 2026-06-02   |
| [e2e-testing.md](./e2e-testing.md)             | E2E test infrastructure — Playwright configuration, page object conventions, fixture patterns, storage helpers, selector strategy, performance budget, and CI integration.                                                  | 2026-05-15   |
| [ai-runtime.md](./ai-runtime.md)               | Shared AI runtime — prompt loading, DSL asset resolution, prompt rendering, canonical prompt ID/alias policy, deterministic prompt selection (including environment-scoped active-pointer rollback), centralized routing/limits, invocation guards, GitHub Models invocation, structured output parsing/validation contracts, telemetry contract, error normalization contract, orchestration entry point, Lambda thin-handler conventions, and canonical backend-only consumption policy (including browser-side provider guardrails). Includes FS-070 Suggest Expression canonical runtime contract: backend-owned schema-context assembly with bounded truncation/summarization, identity-based request contract (`mappingId`/target fields), and validation-before-success response semantics with review metadata (`validation`, `readyToApply`, `context`). Includes FS-071 Smart Fix canonical runtime contract: rule-diagnostic request payload, bounded context assembly (~64KB/~8k), backend validation-before-success, and stale apply-guard metadata (`ruleVersion`/`ruleHash`). Includes FS-072 Validate Mappings canonical runtime contract: single-mapping request boundary, optional sample-data input constraints (JSON/XML text, max 1 MB, reject-over-limit), structured issue report schema/enum enforcement, and advisory-only semantics. Includes FS-073 Auto-Map canonical runtime contract: retrieval-backed OpenSearch-first context assembly, single `/ai/auto-map` mode contract (`section|whole`), large-schema chunking (50–100/chunk, max 4 in-Lambda concurrency), deterministic dedup precedence, validation-enriched suggestions, and no-context success semantics with telemetry metadata. FS-074 addendum: cross-feature suggestion-review hardening guarantees (review-only output semantics, no implicit commit instructions, and telemetry/correlation continuity requirements for auditability). | 2026-06-02   |
| [persistence-model.md](./persistence-model.md) | Phase 1 persistence layer — DynamoDB table schemas (Projects, Mappings, SchemaMetadata, SchemaNodes, MappingRevisions, MappingVersions), S3 object layout, access patterns, metadata-vs-blob rules, draft/revision/version model, `src/lib/persistence/` module architecture. | 2026-06-01   |
| [schema-ingestion.md](./schema-ingestion.md)   | Schema ingestion pipeline — upload → parse → store → index flow, inline vs Step Functions path, DynamoDB/S3/OpenSearch usage, module boundaries, batch sizing, query architecture, error handling, verification coverage, and performance targets. | 2026-05-15   |
| [backend-api.md](./backend-api.md)             | Phase 1 backend API architecture — Lambda handler conventions, API Gateway route table (revision/version endpoints included), DynamoDB/OpenSearch access patterns for schema query, S3 content storage, standardized error envelope (including request correlation IDs, TIMEOUT code, and AI `INVALID_MODEL_OUTPUT` semantics), shared utility structure, error resilience flow contract, FS-066/FS-067 AI handler API addendum (shared normalizeAIError envelope mapping + backend-only invocation policy), FS-068 canonical UI adapter contract alignment for AI routes (`/ai/explain-rule`, `/ai/suggest-expression`, `/ai/auto-map`, `/ai/smart-fix`, `/ai/validate-mappings`) and transitional `FEATURE_NOT_ENABLED` behavior, FS-070 suggest-expression API contract addendum (identity-based request payload, validation-aware success payload, and invalid-suggestion response semantics), FS-071 smart-fix API contract addendum (rule-scoped diagnostics payload, stale conflict semantics, validation-gated success model), FS-072 validate-mappings API contract addendum (single-mapping V1 boundary, sample-data limits, canonical category/severity enums, advisory report semantics), FS-074 suggestion-review hardening clarifications (suggestion-only response semantics, explicit apply-guard metadata, invalid/stale blocked-apply behavior, and request-lineage continuity), environment variables (including degraded-fallback gate), CORS configuration, and testing approach (unit + DynamoDB Local integration). | 2026-06-02   |
| [infrastructure.md](./infrastructure.md)       | Phase 1 infrastructure — SAM template, resource definitions (DynamoDB, S3, API Gateway, Lambda, OpenSearch Serverless, Step Functions), naming conventions, environment configuration, local development setup, Lambda packaging, cold start strategy, deployment conventions. | 2026-05-31   |

**Recent update note (2026-06-02):** FS-074 updated `ui-application.md`, `ai-runtime.md`, and `backend-api.md` to codify cross-feature hardened suggestion-review semantics (generated-vs-committed distinction, explicit Accept/Edit/Dismiss mutation boundaries, deterministic batch eligibility/skip accounting, universal invalid/stale apply blocks, Explain Rule generated/discard-only behavior, and audit/correlation traceability requirements). Previous (2026-06-02): FS-073 updated `ai-runtime.md` and `ui-application.md` for canonical Auto-Map retrieval-backed behavior: single `/ai/auto-map` endpoint with `mode: section|whole`, OpenSearch-first context retrieval (no direct full-schema prompting path), large-schema chunking defaults (50–100 targets/chunk, max 4 in-Lambda concurrency), Step Functions over-budget delegation posture, deterministic dedup precedence (validation → confidence → target-type tie-break), validation-enriched suggestion contract, metadata/telemetry surfacing (retrieval/chunk/validation/dedup), and no-context success empty-state semantics. Previous (2026-06-02): FS-072 updated `ai-runtime.md`, `backend-api.md`, and `ui-application.md` for canonical AI Validation behavior: `POST /ai/validate-mappings` single-mapping V1 boundary, strict sample-data constraints (JSON/XML text, max 1 MB, reject-over-limit), structured issue report contract (`summary`, `issues[]`, `affectedRules`, `recommendation`), canonical enums (`correctness|completeness|maintainability|risk`, `info|warning|error`), Rules-view advisory panel semantics, rule-link resolution/fallback behavior, and explicit deterministic-vs-AI authority boundary. Previous (2026-06-02): FS-071 updated `ai-runtime.md`, `backend-api.md`, and `ui-application.md` for canonical Smart Fix behavior: rule-diagnostic request contract, backend-owned bounded context assembly (~64KB/~8k token-equivalent), validation-before-success suggestion responses (`readyToApply` gating), explicit Accept/Edit/Dismiss review semantics, hard stale conflict behavior (`ruleVersion`/`ruleHash`) with re-run-on-latest flow, and non-persistent draft-only apply semantics. Previous (2026-06-02): FS-070 updated `ai-runtime.md`, `backend-api.md`, and `ui-application.md` for canonical NL→DSL Suggest Expression behavior: backend-owned context retrieval/assembly, bounded context truncation policy (~64KB/~8k token-equivalent), validation-before-success response contract (syntax/function/target-type compatibility), validation-aware UI review states, inline edit-before-apply UX, strict apply gating until valid, and explicit suggestion-only non-persistent semantics. Previous (2026-06-02): FS-069 updated `ai-runtime.md`, `ui-application.md`, and `phase-1-readiness.md` to reflect Explain Rule canonical Phase 2 behavior: backend prompt-registry + structured-output contract path, read-only non-mutation semantics, generated-assistance UI labeling, and optional explain metadata (`confidence`, `limitations[]`). Previous (2026-06-02): FS-068 updated `ui-application.md`, `phase-1-readiness.md`, and `backend-api.md` for canonical Phase 2 AI adapter alignment: route-complete `HttpAdapter` AI mappings (`/ai/explain-rule`, `/ai/suggest-expression`, `/ai/auto-map`, `/ai/smart-fix`, `/ai/validate-mappings`), deterministic local offline policy (`Not available in offline mode`), transitional `FEATURE_NOT_ENABLED` behavior for temporarily unavailable backend capability, and deprecated one-cycle `HybridAdapter` retention with guardrails blocking new callsites. Previous (2026-06-02): FS-067 updated `ai-runtime.md` and `backend-api.md` to document implemented prompt registry/structured-output contracts: canonical prompt IDs with alias policy, deterministic prompt selection with environment-scoped active-pointer rollback (`PROMPT_REGISTRY_ACTIVE_POINTER_ENV`), centralized response contracts, and canonical `INVALID_MODEL_OUTPUT` HTTP 500/non-retryable handler envelope mapping. Previous (2026-06-02): FS-066 updated `ai-runtime.md`, `backend-api.md`, and `phase-1-readiness.md` for the shared AI foundation standard: centralized tier routing defaults/allowlisted overrides, invocation guard + limit enforcement contracts, telemetry event contract (`ai.invoke.start|success|failure`), shared AI error normalization to canonical backend envelopes, and explicit backend-only invocation policy with browser-side provider guardrails. Previous (2026-06-02): FS-065 updated `ai-runtime.md`, `ui-application.md`, `backend-api.md`, and `phase-1-readiness.md` to reflect reconciled canonical AI integration: `HttpAdapter`-only backend consumption in backend mode, explicit `FEATURE_NOT_ENABLED` gating for deferred methods, retirement of legacy shortcut paths (including HybridAdapter reintroduction as non-canonical), and OpenSearch-first schema query with temporary gated/instrumented PK-scoped degraded fallback (`SCHEMA_QUERY_DEGRADED_FALLBACK`). Previous (2026-06-01): FS-064 added `deployments.md` (deployment subsystem architecture: Deployments/DeploymentCurrent tables, S3 snapshot layout, deployability rules, staleness model, promotion/rollback semantics, Lambda handler and UI adapter contracts). Previous (2026-06-01): FS-063 updated `persistence-model.md` and `backend-api.md` for draft/revision/version semantics: split `MappingRevisions` + `MappingVersions` model, revision snapshot S3 layout (`mappings/{mappingId}/revisions/r{N}.json`), no-op hash detection + selective prune rules, and backend route/table mapping updates for revision/version endpoints. Previous (2026-05-31): FS-062 implementation completion aligned `infrastructure.md` with executed SAM/local-setup decisions: local bucket/env alias conventions (`STORAGE_BUCKET` + compatibility aliases), `docker compose` + `scripts/setup-local.sh` local flow, and bundle-size verification (`scripts/check-bundle-sizes.sh`) in the deployment pipeline. Previous (2026-05-15): FS-057 updated `backend-api.md` to reflect implemented Phase 1 route inventory (19 routes), handler conventions, shared lambda utilities, DynamoDB/S3 access patterns, environment variables, and integration-test strategy. FS-056 architecture docs refreshed to reflect completed schema query and integration/performance verification coverage (`schema-ingestion.md` + `project-structure.md`). FS-055 also updated `ui-application.md` for HttpAdapter-first bootstrap (`VITE_API_URL` → `HttpAdapter`), shared HTTP client error/retry normalization contract, and placeholder gating semantics; updated `phase-1-readiness.md` AI transition section to reflect HybridAdapter deprecation and new bootstrap behavior.

---

## Maintenance Rules

- Add a row when a new architecture document is created.
- Update the `Last Updated` date when a document is meaningfully changed.
- Do not remove rows without an explicit architecture task authorizing the removal.
- Documents are created by the spec agent during planning and updated by the task agent during execution of architecture tasks.

---
## Reference Specifications

| Document | Path | Covers |
|----------|------|--------|
| Product & Technical Spec | `specs/PRODUCT-TECHNICAL.md` | Full product requirements, screen specs, engine overview, backend architecture |
| DSL Specification | `specs/KEYRA-DSL-SPECIFICATION.md` | DSL grammar, function catalog, type system, error codes |
| DSL Arrays | `specs/KEYRA-DSL-ARRAYS.md` | Array scoping, map/filter/find, nested contexts |
